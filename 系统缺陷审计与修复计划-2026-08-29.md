# 系统缺陷审计报告与修复计划（2026-08-29）

> 本文档是系统合规缺陷的权威记录与持续推进总控。每次执行修复后更新「§6 执行日志」。
> 审计依据（5 份规范文档，位于工作区根目录与 四性检测/）：
> 1. 中国大陆会计档案组卷规则真实业务详报.md（DA/T 39-2008、79号令、重庆/安徽规则）
> 2. 四性检测/中国国央企与民企会计数字档案管理核心要点及全流程深度分析报告.md（79号令、财会〔2025〕9号、财会〔2024〕11号）
> 3. 四性检测/会计档案元数据与四性检测官方规范（…）.md（DA/T 94-2022 元数据 95 项 + 四性检测）
> 4. 四性检测/电子档案四性检测落地执行清单（对标DA_T 70—2018 官方标准）.md（48 项检测清单）
> 5. 四性检测/电子档案"四性检测"官方认可实现方案与司法采信依据.md（司法采信要件）
>
> 审计方法：规范提取检查清单 → 4 路代码实证（录入界面 / XML 与文件本体 / 四性检测全项 / 组卷档号销毁移交），全部结论有 file:line 证据。

---

## 1. 两个核心问题的结论

### Q1 原始凭证等归档资料是否具备填写界面？——没有，缺口很大

| 级别 | 新建（录入） | 编辑（归档前） | 归档后修改 |
|---|---|---|---|
| 原始凭证(srcDoc*) | ❌ 113 类型字段集只有配置/展示/检索，无录入表单；上传仅 OCR 带出类型/文号 3 字段 | ❌ PATCH 白名单不含 srcDoc*，录错改不了 | ❌ |
| 件（四类） | ⚠️ 必须传文件才能建件，无纯元数据建档；校验编辑器仅 4 字段 | ⚠️ 仅 17 字段白名单、只对草稿卷内件开放；收集池散件无编辑入口 | 🔒 后端硬锁（合规），但改了不留痕 |
| 卷（V1-V20） | ⚠️ 建卷可填 7 项 | ⚠️ 8/20 字段可改；V11 总页数/V12 立档单位/V15 检查人/V16 检查日期/V18 数字化状态/V20 备注无处填 | ⚠️ 无锁：已移交卷题名/期限可直改，且无审计 |
| 盒（B1-B29） | ❌ 移交自动建盒，无人工录入 | ❌ 盒域无任何写端点 | ❌ 装盒人/整理人/审核人/备考/双套关联约 8 项零落点 |

证据：MetadataEntryModal.tsx:66-90（白名单无 srcDoc*）、recordService.ts:292-310、RecordService.java:1047-1070、VolumeService.java:133-157、boxService.ts:10（"本域暂不提供写端点"）。

### Q2 XML 是否归档了？——能进能存能下载，但是"死字节"

1. 不解析、不验签：数电票 XML 内含税务数字签名进库即被无视；M64 电子签名无采集点
2. mime 错标：用友附件链路只认 .pdf/.ofd，XML 一律 application/octet-stream（YonyouSyncService.java:399-400），反被格式检测误报
3. 无哈希固化：finance:digitalHash 模型字段存在但全后端无人写
4. 不进全文检索：OCR 只认 PDF/图片（OcrService.java:89）
5. 用友链路更根本问题：凭证本体不从用友下载原件，是本地 PDFBox 自渲染 PDF（VoucherPdfRenderer.java），原始 JSON 报文不留存——触碰"禁止中间环节格式转换"红线

---

## 2. 缺陷全景（P0 合规命门）

### P0-A 真实性技术底座整体缺失
| # | 缺陷 | 证据 |
|---|---|---|
| 1 | 无文件哈希：全后端对文件字节零 SHA-256；digitalHash 无人写；GD-1-02 种子即停用 | 后端 MessageDigest 仅用于日志链（OperationLogService.java:173）与 AppKey（OpenPushService.java:871） |
| 2 | 无电子签名/时间戳；且存在**造假点**：审计检索页 alert 伪造验签报告（PortalAuditSearch.tsx:118）、打包页假哈希（packageEngine.ts:308-318）、仿真数据假哈希（simulationData.ts:229）、mock 已验签（data.ts:121,309,357,496） | 合规系统严禁假证据 |
| 3 | 归档信息包不存在：ams_package 死表（V1__init.sql:156-163 零引用）；打包页下载按钮无 onClick（ArchivePackagePage.tsx:379-385）；封装说明 checksum 填档号拼接串（packageManifest.ts:107-110） | GB/T 44555、DA/T 48 不落地 |

### P0-B 四性检测真实覆盖率约 25%（真 12/假 1/无 35，强校验仅 6）
| # | 缺陷 |
|---|---|
| 4 | 安全性维度 0/13：无病毒扫描/载体检测；配置页 ClamAV 开关是死配置（InspectionConfigPage.tsx:793-800） |
| 5 | 长期保存环节零触发：全库 @Scheduled 仅 YonyouScheduler.java:34、BorrowController.java:183；CQ-* 检测项永不执行 |
| 6 | 归档环节不检测：检测收拢在移交环节（VolumeService.java:724-731）；auto-archive 通道止于 confirm 可无限期未检（OpenPushService.java:451-493） |
| 7 | 人工复检断在 UI 层：后端 /inspection/review 完整但前端 reviewInspection() 无调用方 |
| 8 | 报告无检测人（operator 硬编码空串 InspectionService.java:331）、不随档归档（仅 PG 行）、可被改写（review() UPDATE 历史结论列 :628-632） |
| 9 | 配置页约 30 项死配置；必填项 18 可选项仅 8 个有执行映射，其余静默跳过（InspectionService.java:52-61,213-214） |
| 10 | phase 参数无校验：未知 phase → 空集恒真 → 出具"全部通过"假合格报告（InspectionService.java:299,361-364） |

### P0-C 录入与审计留痕
| # | 缺陷 |
|---|---|
| 11 | 原始凭证录入界面缺失（见 Q1 矩阵） |
| 12 | 元数据修改不进审计：PUT /records/{id}/metadata、PUT /volumes/{id} 均未接 oplog，无旧值/新值（DA/T 94 附录 E.7 不满足） |
| 13 | 归档后锁定不一致：件级有真锁（RecordService.java:1036-1040），卷级无锁（VolumeService.update 无状态校验）；无修改审批机制 |

## 3. 缺陷全景（P1 业务刚性）
| # | 缺陷 | 证据 |
|---|---|---|
| 14 | 档号 B 段伪盒号：confirm 时每卷烧一个 BOX 流水嵌入档号（VolumeService.java:370-372），与真实盒号 BOX-年-类-序号（:759）完全脱节 | 档号语义错误 |
| 15 | 档号配置双向断链：UI 存 code-custom.* 五项后端从不读（CodeConfigTab.tsx:87-107）；后端消费 archive-code-config 的 serialDigitsVol/serialDigitsBox/separator/categoryPrefix 四键无 UI（VolumeService.java:875-888）；永久被 pad 成 00Y（:894）；UI 总览与实际产物不一致；门类 KU 与惯例 KJ 不符 | 配置页是摆设 |
| 16 | 组卷刚性规则服务端无强制：addItems 加件零校验（VolumeService.java:247-283）；confirm 只查状态非空（:348-357）；不跨月仅前端推荐维度；合并/转卷才有 requireCompatible（:682-691） | 刚性靠自觉 |
| 17 | 凭证类型分段（收/付/转/财政拨款）无支持：perTypeRules 无 separateByVoucherCategory | volumeGroupingStore.ts:114-124 |
| 18 | 鉴定销毁缺法定要件：缺销毁清册生成（一式两份报备案）、三方签批链、共同监销记录、不可恢复性验证、未结清债权债务抽出校验；现有 到期测算→登记→单人评审→永久删除（deleteNode permanent=true 已确认是真物理删除） | AppraisalService.java 全文 |
| 19 | 移交清册不生成文件："生成清册"只是 pending→prepared 状态跃迁（TransferService.java:171-173）；接收方签收零检测（:177-182） | DA/T 70 YJ 系列不满足 |

## 4. 缺陷全景（P2 完善项）
20. 后端上传无格式闸口（mime 取客户端自报 RecordController.java:119；exe 可入池）
21. OCR/全文索引不覆盖 XML/OFD
22. 检测报告 LIMIT 100 无分页（InspectionService.java:661）；QuickCheck 名为快速清点实为检测中心（实体盘点不存在）
23. 盒号 boxes.size()+1 生成（VolumeService.java:758-759），删盒后有重号风险

---

## 5. 修复任务拆分（五批总计划）

### 第一批 · 真实性底座（合规命门）
- **T1 哈希固化全链路**：上传/推送/用友三入口统一计算 SHA-256 → finance:digitalHash + PG 固化登记（node_id/sha256/size/mime/时间）；激活 GD-1-02、CQ-1-01；新增定期固化巡检 @Scheduled（重算比对、不符告警出报告）＝BC-1-1
- **T2 数电票 XML 归档地位**：入口 XML 识别与 mime 修正（含用友附件映射）；XML 解析提取发票要素+签名信息写 srcDoc*；XML 文本进全文索引（复用 ocrText 管道）；XML↔版式主附绑定（复用 linkRecordParent）
- **T3 造假点清除**：删伪造验签 alert、打包页假哈希改真 SHA-256（WebCrypto，诚实标注语义）或移除、仿真数据假哈希清理

### 第二批 · 四性检测补强
- **T4** required-fields 18 项全映射；phase 白名单校验（未知 phase 400）；operator 落库（检测人）；报告分页
- **T5** 归档环节检测节点恢复（confirm 时跑 gd 口径，不合格阻断）+ auto-archive 通道接检测（失败落推送日志）
- **T6** 人工复检 UI 接通（QuickCheckPage → reviewInspection）+ 复检写新行不改历史 + 检测报告随档归档（报告文件入 Alfresco 并与卷关联 checkReportRef）
- **T7** 配置页死配置清理（约 30 项：接不上引擎的移除，界面只留真生效项；检测节点段移除或接通）

### 第三批 · 录入界面与审计留痕（P0-C，后续执行）
- **T8 原始凭证录入界面**：PATCH 白名单扩 srcDoc*（前后端）+ MetadataEntryModal 按 113 类型字段集动态渲染 + 收集池散件编辑入口
- **T9** 卷级缺项（V11/V12/V15/V16/V18/V20）+ 盒级人工字段（B20-B23/B26-B29）录入：盒域补写端点+装盒/上架表单
- **T10** 元数据修改审计（oplog 记旧值/新值）+ 卷级归档后锁拉齐 + 纯元数据建件入口

### 第四批 · 组卷/档号/销毁/移交刚性（P1，后续执行）
- **T11 档号修正**：去 B 伪盒号段（回归 全宗-KJ·类·年-期限-案卷号-件号）、永久 Y 不 pad、code-custom 与 archive-code-config 双链合并为一套配置一套消费、盒号走流水表
- **T12 组卷服务端强制**：addItems/confirm 加类别/年度/期限一致性 + 凭证类同月校验；perTypeRules 增收付转分段
- **T13 鉴定销毁法定化**：销毁清册生成导出、三方签批链、监销记录、未结清债权债务校验字段
- **T14 移交清册真实生成**（法定字段齐全、可导出/打印、随档留存）+ 接收方检测入口

### 第五批 · 封装与加固（P2，后续执行）
- **T15** 归档信息包真实现（启用 ams_package + 真 ZIP + DA/T 48 封装 XML + 包级 SHA-256）
- **T16** 后端上传格式闸口 + mime 嗅探；OCR/索引覆盖 OFD

### 第六批 · 界面风格优化（2026-08-29 用户追加，纯前端样式，可与三/四批并行）
> 方案全文：根目录《界面风格优化方案-2026-08-29.md》（依据 参考1.png/参考2.png 紫光系统提炼；不动配色，只改字体/比例/密度/信息完整性）
- **T17** 设计 token + 共享组件改造（字阶基线 14px、行高 48px、表头 600 灰底、分页底置；DataTable/DetailTable/按钮/弹窗壳/全局字体栈）
- **T18** 高频主干页面走查（门户四模式/组卷工作台/快速检测/件卷列表/分类视图）
- **T19** 流程页+配置页走查（录入弹窗/借阅/移交/鉴定/回收站/各配置页）
- **T20** 驾驶舱与低频页收尾 + 全站视觉走查验收（改前/改后截图入日志）

> 电子签名验签/可信时间戳真接入（CA、国家授时中心）涉及外部服务采购，不在代码修复范围；先把数据结构与留痕位做实（哈希、签名信息字段、报告要素），留好接入点。病毒扫描同理（留接口抽象，未接入时明示而非假装有开关）。

---

## 6. 执行日志

| 日期 | 批次 | 内容 | 验证 | 状态 |
|---|---|---|---|---|
| 2026-08-29 | 计划 | 五批 16 任务计划落定；用户批准本次执行第一批+第二批 | — | ✅ |
| 2026-08-29 | 第一批 T1-T3 | 见下明细 | 冒烟 33/33 + 回归 10/10 + 11/11 | ✅ |
| 2026-08-29 | 第二批 T4-T7 | 见下明细 | 同上 + 前端 174/174 + tsc+build | ✅ |
| 2026-09-05 | 第三批 T8-T10 + #24 | 见下明细 | 新冒烟 38/38 + 回归 33/33 + 10/10 + 13/13 + rbac 34/34 | ✅ |
| 2026-09-05 | 第六批 T17-T20 | 见下明细（与第三批同日执行，纯前端） | 前端 174/174 + tsc + build | ✅ |
| 2026-09-05 | 第四批 T11-T14 | 见下明细 | 新冒烟（批次四五）38/38 + 全部 5 套回归通过 | ✅ |
| 2026-09-05 | 第五批 T15-T16 | 见下明细（与第四批同日执行） | 同上 | ✅ |
| 2026-09-05 | 字阶收口（新方案 P1-P4） | 《全局字体排版一致性设计方案-2026-09-05.md》全量实施：888 处小字/任意值归并 + Typography 组件族 + 扫描护栏 | check-typography 0 error/0 warning + tsc + 174/174 + build + 页面实测 14/12/16 收敛 | ✅ |

### 第一批明细（T1-T3 真实性底座）

**T1 哈希固化全链路**
- 新增 `util/HashUtil`（SHA-256 小写 hex + 卷级聚合口径 aggregateSha256，全系统唯一口径）
- 新增 `db/migration/V11__fixity.sql`：`ams_record_fixity` 固化登记表（node_id/sha256/size/mime/登记人/巡检字段）+ 激活 GD-1-02/CQ-1-01 + 新增检测项 GD-1-04、YJ-1-02（hash-verify 重算比对）、YJ-1-03（volume-hash-verify 卷级聚合复核）
- 新增 `fixity/FixityService`：register（四入口覆盖：RecordService.create 手工上传/OpenAPI 推送/用友同步 + SourceDocService.create 子件）、verify（下载重算逐位比对）、aggregateForVolume、patrol（@Scheduled cron `ams.fixity.cron` 默认每周日 02:30，每轮最久未验 300 件，异常落 cq 报告+操作日志）、backfill（存量遍历补登记）、status 统计
- 新增 `fixity/FixityController`：GET status / POST verify/{nodeId} / POST patrol / POST backfill（quick-check 功能码授权）
- `VolumeService.confirm` 赋号时写卷级聚合摘要 finance:digitalHash；`unconfirm` 作废清除
- `InspectionService`：hash-registered 改查 PG 固化表；新增 hash-verify 件级执行器（真下载重算比对）与 volume-hash-verify 卷级执行器
- 实测：上传即登记、重算一致、backfill 存量登记 17 件、巡检 33 件全一致

**T2 数电票 XML 归档地位**
- 新增 `sourcedoc/XmlInvoiceParser`：XXE 全关防御性解析（禁 DOCTYPE/外部实体）、命名空间剥离、中文/英文标签同义词表、购买方/销售方父路径定向（消歧统一社会信用代码）、签名元素存在性探测、纯文本提取（全文索引原料）
- `RecordService.create`：XML 内容识别（mime/扩展名双判）→ 票面要素回填 srcDoc*（调用方显式传入优先）、纯文本写 finance:ocrText（复用全文索引管道）、extFields 合并追加 `_xmlParsed`/`_signaturePresent` 标记；数电票兜底类型 vat-electronic-invoice；解析失败按原始字节归档不阻断
- `YonyouSyncService` 附件 mime 推断修正：.xml→application/xml（原错标 octet-stream 导致格式检测误报）、补图片/tif/txt 映射
- 前端 `VoucherUploadModal`：XML 文件跳过 OCR（OCR 只认 PDF/影像），预置 原始凭证/数电票 类型、置信度封顶 60 强制人工确认
- 实测：数电票 XML 上传 → 类型/发票号码/销售方/价税合计自动回填、签名标记在、固化登记一致

**T3 造假点清除**
- `PortalAuditSearch`：伪造「数字签名验证报告」alert → 真实 JSON 导出下载（操作日志哈希链）
- `packageEngine.computeChecksum`：位移假哈希 → WebCrypto 真 SHA-256（async，与后端同口径）
- `packageManifest`：digest 由档号拼接串 → 文件清单本体真 SHA-256（scope=fileList 诚实标注）；generateManifestXML 改 async
- `packageStore.generatePackages`：适配 async
- `ArchivePackagePage`：死按钮治理——「下载」改真实下载封装说明 XML；「移交」改禁用态+诚实提示（真实移交数据包属 T15）；预览不再截断谎称
- `simulationData`：卷 digitalHash 假值→空串；组件 hash 假值→空串；signatureVerified→false
- `data.ts`：12 处 mock signatureVerified:true → false

### 第二批明细（T4-T7 四性检测补强）

**T4 检测引擎小修**
- required-fields 映射：新增 保管期限/密级 2 项真实映射；8 个无件级语义字段（档案馆名称/全宗号/目录号/案卷号/件号/附件数/页数/摘要）从配置页移除；loadPlan 自愈过滤存量未知标签（不再静默漏检）
- phase 白名单：gd/yj/cq 之外一律 400（堵死「未知 phase → 空集恒真 → 假合格报告」漏洞）
- operator 落库：run/runBatch/runVolume 全链路透传检测人（原硬编码空串）
- 报告分页：GET /inspection/reports 支持 page/size 返回 {items,total,page,size}（原 LIMIT 100 截断）

**T5 归档环节检测节点恢复**
- `VolumeService.confirm`：赋号前执行 gd 口径检测，不合格 409 阻断（卷保持草稿不赋号，明细落报告）
- auto-archive 推送通道经 confirm 自然接入检测（失败落推送日志，卷留草稿待整改）
- 移交环节维持 gd∪yj 全口径复核（双环节检测链闭合：归档 gd → 移交 gd+yj）
- 修正引擎默认格式白名单补 text/plain（与配置页默认含 TXT 对齐）

**T6 复检与报告随档归档**
- `review()` 重写：复检写**新报告行**（detail_json 记 reviewOf/维度/结论/原因/复检人/前后结论对照），历史行不再 UPDATE（检测记录不可篡改，司法采信要件）；节点当前四性态跟进复检结论
- 报告随档归档：finishReport 后报告 JSON 写入 Alfresco `/{全宗}/_检测报告/{年}/`（cm:content），detail_json.reportFileNode 回填，快速检测页可下载
- 前端 `QuickCheckPage`：复检弹窗（维度/结论/原因必填）、复检行紫色徽标与前后结论对照展示、报告文件下载按钮、服务端分页底栏、统计口径标注（本页/全库）

**T7 配置页死配置清理**
- `InspectionConfigPage` 整页重写：仅保留引擎真实消费的 6 键（hashEnabled / metadataRequiredCheck+requiredFields / formatWhitelist / sensitiveCheck+sensitiveKeywords）+ 检测项库启停
- 移除约 30 项死配置（模板切换/哈希算法/签名标准/CA/区块链/日志时长/文件数量大小开关/包结构/主附件/编码/格式转换/渲染验证/DPI/字体/病毒开关/脱敏/权限/传输加密/动态水印/检测节点段）
- 新增「能力状态」诚实标注：已实现（哈希固化/巡检/断号查重/审计链）与待接入（CA 验签/时间戳/病毒扫描/格式转换）分色展示，不再假装有开关

### 顺手修复（冒烟实测发现）
- `AlfrescoNodeClient.renameNode` + `VolumeService.moveWithRenameRetry`：8 处结构性移动（加件/拆件/拆卷/拆分/合并/转卷/归盒/退回）同名冲突自动改名重试（原 409 直接失败；自动组卷同名卷归盒必现）

### 验证记录（第一、二批）
- 后端 mvn package（JDK21）通过；V11 flyway 迁移成功
- 前端 tsc --noEmit 通过；vitest 174/174 通过；build 成功
- 新冒烟 smoke-fixity-inspection-2026-08-29.py **33/33**
- 回归 smoke-grouping-transfer-2026-08-25.py **10/10**、smoke-2026-08-25.py **11/11**
- smoke_rbac.py 33/34：C2（admin 读审计日志应 403）失败为**既有问题**（本次未触碰任何权限代码，git diff 可证）→ 记入缺陷 #24，批次三排查

### 新登记缺陷（第一、二批执行中发现，非本批范围）
- **#24 三员硬分立存疑**：admin 可读取审计日志（应仅审计员，smoke_rbac C2）→ 已于批次三确诊并修复（根因见第三批明细）
- **#25 档号 B 段伪盒号仍在**（Z001-KU·01·2026-D10-B014-0014 的 B014 与真盒号 BOX-2026-KP-001 脱节）→ 按计划第四批 T11 修复

### 第三批明细（T8-T10 + 缺陷 #24，2026-09-05 执行）

**T8 原始凭证录入界面（srcDoc* 白名单打通）**
- 后端 `RecordService.updateMetadata` 白名单扩 9 键：docTypeCode/docTypeName/documentNo/counterpartyName/counterpartyTaxId/srcDocSummary/amountUpper/businessCategory/extFields（落 finance:srcDoc*）；`attachedBillCount` 数值键；toView 透出 srcDocSummary 原值（与 cm:description 区分）
- 前端 `MetadataEntryModal` 重构：原始凭证件分流渲染——票面公共字段（DA/T 95-2022）+ 按 docTypeCode 从 `SOURCE_DOC_TYPE_TREE`/`FIELD_SETS` 解析「113 类型字段集」（fieldSetRefs 展开 − excludeFields + fieldOverrides + 类型特有 extFieldDefs）动态渲染扩展字段，值落 finance:srcDocExtFields JSON（保留 _xmlParsed/_signaturePresent 系统标记）
- 收集池散件编辑入口（T8 第三项）：池工具栏「元数据」按钮（不勾选=本页全部可改件）+ 行内铅笔图标；`MetadataEntryModal` 支持池模式（volume=null + poolRecords），打开前校验仅件数据/待审核状态
- 冒烟：srcDoc* 六键写读回一致、extFields JSON 落库

**T9 卷级缺项 + 盒级人工字段**
- 模型 v2.9（finance-model.xml，bind mount 直挂容器，重启 acs 生效，REST /api/classes 实测 10 属性注册）：卷级 +4（establishingUnit 立档单位 V12 / checker 检查人 V15 / checkDate 检查日期 V16 / volumeRemark 备注 V20）；盒级 +6（boxPacker 装盒人 / boxPackDate 装盒日期 / boxArranger 整理人 / boxAuditor 审核人 / boxAuditDate 审核日期 / dualSetRef 双套制关联）
- 后端 `VolumeService.update` 白名单扩 8 键（totalPages/pageStart/pageEnd V11、V12/V15/V16、scanned V18、remarks V20）；`BoxService.updateMetadata` + `PUT /boxes/{id}` 端点（盒域首个元数据写端点；盒号/类别/年度/期限结构性字段排除，由移交流程维护）
- 前端：卷级表单新增 V11/V12/V15/V16/V18/V20 六字段（含起止页号对、数字化 checkbox）；`BoxInfoEditModal`（实体库房盒详情抽屉「盒信息」按钮，人工字段九项）；boxService/volumeService DTO 与 patch 同构扩展
- 冒烟：卷级六字段写读回一致；移交归盒后盒人工字段写读回一致

**T10 录入与审计留痕**
- 元数据修改审计（DA/T 94-2022 附录 E.7）：updateMetadata/VolumeService.update/BoxService.updateMetadata 三路均返回 {view, changes:[{prop,old,new}]}（服务端比对旧值快照），Controller 落操作日志 action=元数据修改/案卷元数据修改/盒元数据修改，detail 记「属性「旧」→「新」」明细（900 字截断）；冒烟实测审计员可查到三级留痕
- 卷级归档后锁拉齐（与件级同口径）：draft 外仅放行 cabinetNo/shelfNo（实体摆位），其余 409 VOLUME_LOCKED「已固化，如需调整先撤销确认」；冒烟 E11/E12 验证锁与放行
- 纯元数据建档 + 补文件闭环：`POST /records/metadata-only`（无文件建 finance:record，与上传同构校验与 srcDoc*/voucherMeta 字段）+ `POST /records/{id}/content`（仅「仅件数据」且未固化登记可补；补齐即固化登记，二次补 409 CONTENT_EXISTS——内容不可变）。**无内容判定走 ams_record_fixity**（Alfresco 对无内容节点也回默认 content.mimeType，不能作判据——首版实现踩坑已修正）。补齐文件前四性检测 file-present/hash-verify 如实不通过=不可确认组卷，属预期合规约束
- 前端：池工具栏「建档」按钮 + `MetadataCreateModal`（含原始凭证票面字段段、合规约束提示）
- 新冒烟 `smoke-batch3-metadata-2026-09-05.py` **38/38**

**缺陷 #24 三员硬分立修复**
- 根因确诊：非 role-auth-v1 默认放行，而是 2026-08-25 操作日志页把 `OperationLogController.logs` 放宽为 `requireAuditorOrAdmin`——与 PermissionService 自身「sys-log 仅 security_auditor，admin 不豁免」的既定口径冲突
- 修复：/audit/logs 收紧回 `requireAuditor`（仅安全审计员）；影响面核实：操作日志页挂 sys-log 菜单（本就仅审计员可见）、借阅日志全局加载为静默容错（403 被捕获），无 UI 断链
- 验证：smoke_rbac **34/34**（C2 转绿）；smoke-2026-08-25 同步补「admin 查审计日志 → 403」回归断言，其审计日志断言改走审计员会话（原断言固定了被修复前的越权行为，属测试口径更新非产品回归）

### 第六批明细（T17-T20 界面风格优化，2026-09-05 执行）

**T17 设计 token + 共享组件**
- `index.css`（Tailwind v4 CSS-first）：全局字体栈补中文字族（'Geist Variable','PingFang SC','Microsoft YaHei','Noto Sans CJK SC',system-ui）+ 等宽栈（档号/凭证号列）；@theme 增字阶 token（display 20/title 16/tab 15/body 14）与尺寸 token；新增 .btn-primary/.btn-secondary/.btn-danger 工具类（h-9、圆角、禁用态统一）与 .code-mono
- `ui/table.tsx`（全站表格原语，DataTable 与原生表共用）：表头 44px/14px/600/slate-700 灰底；行高 48px（py-3.5+text-sm）；**去列竖线**（2026-08-18 v2 规范的 divide-x 按新方案移除，仅横向浅线）+ **隔行斑马纹**（odd:bg-slate-50/40）
- `DetailTable.tsx`：FieldGrid 标签列 88→110px、标签 600、值 13→14px、break-all→break-words（长文本换行不截断）；DetailRows 同步 14px
- 页面扫替（pages/ + components/ 全量，28 文件）：表头 `px-4 py-3 text-[13px] font-semibold`→`text-sm`、内容格 `px-4 py-3 text-[13px]`→`py-3.5 text-sm`（48px 行高）、嵌套表头 12px→13px——门户四模式/组卷工作台/快速检测/件卷列表/借阅/移交/鉴定/回收站/配置页/统计页一并覆盖

**T18-T20 页面走查**
- 高频主干（门户检索四模式、组卷工作台、快速检测、财务分类视图）：共享原语自动受益 + 扫替命中
- 流程页（元数据录入/建档弹窗——本批重构时已按 13px 输入行高/600 标签基线写）、配置页、驾驶舱与低频页：扫替覆盖全部 text-[13px]/[12px] 表格单元格
- 全局验证：tsc --noEmit 通过；vitest 174/174；build 成功（视觉走查截图验收待前端联调环境启动后补录至本日志）

### 验证记录（2026-09-05 批次三+六）
- 后端 mvn package（JDK 25 编译 release 17）通过；模型 v2.9 经 bind mount 直挂 + docker restart 生效，REST /api/classes/finance_volume 与 finance_archiveBox 实测 10 个新属性注册
- 前端 tsc --noEmit 通过；vitest 174/174；build 成功
- 新冒烟 smoke-batch3-metadata-2026-09-05.py **38/38**
- 回归 smoke-fixity-inspection-2026-08-29.py **33/33**、smoke-grouping-transfer-2026-08-25.py **10/10**、smoke-2026-08-25.py **13/13**（11+2 新断言）、smoke_rbac.py **34/34**（#24 C2 转绿）
- 遗留：第一冒烟轮次失败的残留数据（空草稿卷、池内两件）已清理入回收站/删除

### 新登记缺陷（批次三执行中发现）
- （无新增；#24 已闭环，#25 留待第四批 T11）

### 第四批明细（T11-T14，2026-09-05 执行）

**T11 档号修正（缺陷 #14/#15/#25/#23）**
- 去 B 伪盒号段：`VolumeService.confirm` 不再烧 BOX 流水嵌入档号，卷级档号回归 **{全宗}-KJ·{类别}·{年度}-{期限代码}-{案卷号}**（件号 = 卷号-件号，不变）；与真实盒号（移交归盒流水）解耦
- 门类前缀默认 KU→**KJ**（会计惯例，缺陷 #15）；保管期限代码不再 padLeft——永久 **Y 不补零**（原 00Y）
- 盒号走流水表（缺陷 #23）：`transfer` 自动建盒改经 `ams_code_serial`（scope=BOX）+ `ensureAtLeast` 对齐存量最大盒号（幂等迁移），位数消费 archive-code-config.serialDigitsBox——删盒不再重号
- 双链合一（缺陷 #15「配置页是摆设」）：`CodeConfigTab` 重写——废弃 code-custom.* 五张假卡片（后端从不消费），档号参数（categoryPrefix/separator/serialDigitsVol/serialDigitsBox）直接编辑 `archive-code-config`（useArchiveCodeConfigStore → ams_config，与后端消费同源），档号结构总览与实际产物严格一致（无 B 段、KJ、Y 不补零、页签示例随参数联动）
- 前端 `types/volume.ts`：defaultPaperCodeRule 移除 boxSerial 段、KU→KJ、示例更新（ArchiveCodeRule 仅类型声明自用，无外部消费者）

**T12 组卷刚性服务端强制（缺陷 #16/#17）**
- 新增 `VolumeService.requireGroupingInvariants`（DA/T 42-2022 同卷同类同期限 + 凭证按月装订）：①类别一致（件 archiveType 归一 = 卷 typeCode）②年度一致 ③期限一致（件填了才比）④凭证卷（KP）全卷会计月份唯一；`addItems`（既有+新件预演合并校验）与 `confirm`（赋号前全卷复核）双闸口，违规 409（GROUPING_CATEGORY/YEAR/RETENTION/MONTH）
- `CategoryCodes` 补简称归一（凭证/账簿/报表→01/02/03）与 retentionName 反查（报错文案）
- 缺陷 #17（收付转分段）：核查确认 2026-08-21 已落地 perTypeRules.separateByVoucherCategory（引擎 volumeStore + 配置页 GroupingConfigTab 均在），无需再改

**T13 鉴定销毁法定化（缺陷 #18，79号令第20/21条）**
- V12 迁移：ams_appraisal 增三方签批（sign_applicant/archives/supervisor + 时间）、共同监销记录、不可恢复验证（unrecoverable_verified/note）、未结清债权债务核查（unsettled_check/note）、销毁清册（register_no/register_file_node）
- 单步销毁评审废除：review(decision=destroy) → 400 SIGN_CHAIN_REQUIRED；新端点 `POST /appraisals/{id}/sign`——三方签批链（申请单位→档案管理部门→监销人），顺序强制（越序 409）、已签不可改（409）；申请单位必须勾选「未结清债权债务核查声明」（79号令第20条）+ 核查说明；三方签齐自动转 approved-destroy
- 销毁清册：`POST /{id}/register` 生成法定字段 HTML（清册编号 XH-日期-短id、全宗/档号/题名/年度/期限/件数/鉴定结论/核查结论/三方签章位/监销记录），写入 `/{全宗}/_销毁清册/{年}/` 随档留存（一式两份报备案由打印承担）；`GET /{id}/register-file` 下载；幂等（已生成直接返回）
- 销毁执行法定前置全检：清册已生成 + 监销人已签批 + 共同监销记录必填；执行后**重取节点校验 404** 记不可恢复验证（unrecoverable_verified），日志留痕
- 前端 AppraisalManagePage 重写：签批链状态可视化（展开区显示申请→档案→监销）、签批弹窗（申请含核查声明块）、清册生成/下载按钮、执行弹窗（监销记录必填）、已办结显示不可恢复验证结论

**T14 移交清册真实生成（缺陷 #19，DA/T 70-2022 YJ 系列）**
- V12 迁移：ams_transfer_batch 增 register_no/register_file_node
- `prepare()` 由纯状态跃迁升级为真实清册生成：法定字段 HTML（清册编号 YJ-日期-短id、批次号、全宗、移交/接收双方与经办人、卷清单序号/档号/题名/年度/期限/件数、合计、签章位、检测提示），写入 `/{全宗}/_移交清册/{年}/` 随档留存；幂等复用（退回后重新 prepare 不重复生成）
- `GET /transfers/{id}/register-file` 清册下载；前端 TransferManagePage 接入（清册编号展示、下载按钮、按钮语义更新）
- **接收方检测入口**（原签收零检测）：`receive()` 签收前逐卷执行 gd∪yj 全口径四性检测，任一卷不通过 409 RECEIVE_INSPECTION_FAILED 阻断签收

**T15 归档信息包真实现（缺陷 #3，GB/T 44555 + DA/T 48-2009 + DA/T 93-2022）**
- 新增 `archivepackage/ArchivePackageService` + `ArchivePackageController`（启用 V1 ams_package 死表）：
  - `POST /packages`：真 ZIP（一卷一目录：`{卷号}/元数据.xml`（DA/T 94 风格卷级+件级元数据、文件级 SHA-256）+ `卷内文件/{件档号}-{文件名}`（原格式字节，不转换）+ 根 `封装说明.xml`（DA/T 48 封装说明：包标识/档案对象清单/文件清单/内容聚合摘要））；**包级 SHA-256** 对最终 ZIP 字节计算（存 ams_package.checksum，随交接单在包外传递，避免自引用）；无内容件（纯元数据建档）以 `.无电子文件.txt` 诚实标注
  - ZIP 写入 `/{全宗}/_归档信息包/{年}/` **随档留存并入固化登记表**（fixity.register，信息包本身即归档对象）；`GET /{no}/download` 下载即验真（重算包级摘要比对，结果 X-Package-Checksum-Match 头）；状态机 created→transferred→received（transfer/receive 端点）
- 前端：packageService.ts 新建；packageStore.generatePackages 改走服务端真封装（单元→卷节点收集，本地记录 serverNo/checksum）；ArchivePackagePage 死按钮转真操作（下载信息包 ZIP/移交/包号与摘要展示）

**T16 上传格式闸口 + OFD 全文索引（缺陷 #20/#21）**
- 新增 `util/FileTypeSniffer` 魔数嗅探：可执行类无条件拒绝（PE MZ/ELF/shebang → 415 FORMAT_REJECTED）；白名单判真（PDF/PNG/JPEG/TIFF/ZIP 系 OFD+OOXML/XML/文本），客户端自报 mime 仅辅助不采信；`RecordController.upload` 与补文件 `attachContent` 双入口接入（0x89PNG 等魔数按字节序列匹配，规避 US_ASCII 编码歧义）
- `OcrService.extractOfdText`：OFD（ZIP 容器）`<TextCode>` 节点文本抽取（按条目名排序保持阅读序），写入 finance:ocrText 复用全文索引管道；检测引擎格式白名单已含 application/ofd

**批次四五冒烟 `smoke-batch45-2026-09-05.py` 38/38**：T16 exe 伪装 415/合法 PDF 200；T12 跨月/跨类/跨年/跨期限 409×4 与合规 200；T11 档号无 B 段+KJ+D10、盒号流水格式与无重号；T14 清册编号 YJ-*+法定字段+签收检测；T15 ZIP 结构（封装说明/元数据/卷内文件）+DA/T 48 标识+状态机；T13 签批链五步（单步拒绝/越序 409/未核查 400/顺序签批/签齐转态）+清册 XH-*+法定字段+执行销毁+不可恢复验证
- 过程修复：TransferService list/detail SELECT 补 register 列（row 映射器共用）、prepare/receive 用 fullRow（volume_nodes）；AppraisalService sign/generateRegister/registerContent 改 fullRow（requireRow 三列快照导致签批链状态读到空——冒烟实测抓出）；F13 断言文案修正（「第79号」）

### 第五批验证与全量回归（2026-09-05）
- 后端 mvn package 通过；V12 flyway 迁移成功
- 前端 tsc 通过、vitest 174/174、build 成功
- 新冒烟 smoke-batch45-2026-09-05.py **38/38**
- 回归全部通过：smoke-fixity-inspection **33/33**、smoke-grouping-transfer **10/10**、smoke-2026-08-25 **13/13**、smoke-batch3 **38/38**、smoke_rbac **34/34**

### 字阶收口（P1-P4，《全局字体排版一致性设计方案-2026-09-05.md》，2026-09-05 执行）
- **P1 地基**：index.css 补 `--text-aux`(12px) 凑齐 5 档闭集；新建 Typography 组件族（PageHeader/SectionTitle/PageTabs/StatusBadge/FormRow/EmptyState/AuxText，components/common/Typography.tsx + index.ts 出口）；改造 PaginationBar（12→14）、Header（8 种字号收敛 20/16/14/12）、DrawerPanel（10/11→12）、DetailTable（DetailPanelHeader 副标题 11→12、DetailSection 标题 14/600）、Sidebar 品牌 15→14；`scripts/check-typography.mjs` 扫描护栏入库（白名单按实际目录修正为 pages/archive-stats 与 components/stats）
- **P2-P4 机械清零**：全站归并 **888 处**（13px×323→14、10px×288/11px×217/9px×16/8px×8/10.5px×3/9.5px×1→12、12px×4→text-xs、15px×2→text-tab、text-lg×23→16、统计白名单外 2xl/3xl→20）；`check-typography` 全量 **0 error / 0 warning**
- **护栏挂接**：package.json 增 `check:typography` 与 `smoke:fe`（typography + tsc + vitest + build 四联冒烟）
- **页面实测**（浏览器 DOM 计算样式）：统计驾驶舱与组卷工作台字号分布均收敛于 14px/12px/16px（组卷工作台 14×425、12×182、16×66），闭集外为零；顺带修复组卷工作台池工具栏按钮文字折行回归（flex-wrap + 按钮 nowrap）
- 验证：tsc 通过、vitest 174/174、build 成功

### 用户反馈修复（2026-09-05，批次四五+字阶收口交付后走查）
- **① 日期格式**：卷卡片「日期: 2026-08-01T00:00:00.000+0000 ~ …」→ 后端 `VolumeService.toView` 对 d:date 统一截取 yyyy-MM-dd（dateOnly），前端 `fmtDate` 兜底；实测「日期: 2026-08-01 ~ 2026-08-31」
- **② 徽章折行**（集成接口采集·类别列「会计凭证」折成两行）：全站 148 处 rounded-full 徽章统一补 whitespace-nowrap（StatusBadge 组件原已内置）
- **③ 表格字号语义统一**（财务分类视图等「表格字依旧很小」）：根因是语义层 text-xs 被用作单元格主值（语法护栏不报错）。本次归并：原生 `<td>` 直接类 14 处、DataTable cell 渲染器（ArchiveBoxTreeView 盒表）+ **config/metadataColumnMaps/\*.tsx 列配置映射**（poolColumns/voucherColumns/volumeColumns/boxColumns/archiveItemColumns——全站表格列渲染的统一源，主值 12→14）；徽章/弱占位（slate-400）保留 12px 辅助档
- 浏览器实测：组卷工作台池表格叶子字号 14px×80（主值）+ 12px×40（类别 tag/弱占位，规范允许）；财务分类视图盒表同口径
- 回归：batch45 38/38、fixity 33/33、batch3 38/38；tsc/vitest 174/build/check-typography 全绿

### 新登记缺陷（批次四五执行中发现）
- （无新增）

| 日期 | 批次 | 内容 | 验证 | 状态 |
|---|---|---|---|---|
| 2026-08-29 | 计划追加 | 第六批（界面风格优化 T17-T20）挂入总计划；方案=根目录《界面风格优化方案-2026-08-29.md》 | 方案评审待用户确认 | ✅ 已批准并执行（2026-09-05） |
