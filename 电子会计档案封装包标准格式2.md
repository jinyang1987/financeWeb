# 电子会计档案封装包标准格式

电子会计档案封装包整体格式严格遵循《基于 XML 的电子文件封装规范》（DA/T 48\-2009）、《电子会计档案管理规范》（DA/T 94\-2022）、《电子档案移交接收操作规程》（DA/T 93\-2022）官方标准，分为**外层封装容器格式、元数据格式、内部归档文件版式格式、统一编码格式**四大固定规范格式，无自由自定义格式，所有格式为档案行业法定强制标准。

## 一、外层封装容器格式（整包格式）

日常归档、离线备份、系统内流转场景，默认采用 **标准 ZIP 压缩格式**，通用性强、跨系统可解析，为企业电子会计档案通用封装容器。
正式向档案馆移交、合规归档验收场景，必须采用 **DA/T 48 标准 XML 封装包格式**，为官方唯一认可的进馆封装格式，替代普通压缩包，满足档案馆入库校验要求。

## 二、元数据固定格式

封装包内所有层级元数据（包级、案卷级、文件 / 凭证级），**强制统一使用 XML 格式**，严格遵循 DA/T 48、DA/T 94 规范，固定 UTF\-8 编码，不可更改、不可替换为 JSON、TXT 等其他格式，保障元数据可校验、可迁移、可长期解析。

## 三、内部电子档案版式格式（核心业务文件）

根据 DA/T 94\-2022 强制规定：电子会计档案**归档终版优先采用 OFD 国家标准版式格式**，为长期保存唯一最优合规格式。
暂不具备 OFD 适配能力的单位，可合规使用 **PDF/A 长期保存格式（PDF/A\-1、PDF/A\-2）**，严禁使用普通 PDF、Word、图片、Excel 等可变、非归档版式作为最终归档文件。
原生业务系统生成的源文件（如原始 Excel 报表、业务系统原生凭证文件），需实行**双文件封装**：同时保留原生源文件 \+ 合规归档版式文件（OFD/PDF/A），统一打入同一封装包。

## 四、全局统一编码格式

封装包内所有文件、元数据、说明文档，**强制统一使用 UTF\-8 字符编码**，固定不可修改，彻底规避跨系统解析乱码、格式兼容异常问题。

## 五、补充合规约束

所有封装包格式区分场景固定适配，日常业务归档、离线备份使用 ZIP 容器格式；官方移交、档案馆进馆必须使用标准 XML 封装格式，两种格式内部文件版式、元数据格式规则完全统一，仅外层容器差异化适配，全程符合电子会计档案四性检测与长期保存规范。

需要我帮你**整理成系统开发可直接落地的字段格式枚举表**吗？

> （注：部分内容可能由 AI 生成）
