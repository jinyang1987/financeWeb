# 会计档案管理系统 · 主线业务逻辑全景

> 2026-07-18 全域代码侦察整理（前端 financeWeb + Alfresco 平台侧）
> 用途：团队对齐业务主线与配置链路，标注全部逻辑断点与独立模块，作为《Alfresco 集成总体方案》的现状基线。

---

## 一、系统定位

在 **Alfresco Content Services（开源内容管理平台）** 之上，以自研 React 前端（`financeWeb/`）实现一套符合 79 号令、DA/T 13/42/70/94 的会计档案管理系统。组织/人员/全宗等基础实体已部分落在 Alfresco 仓库，档案业务数据当前由前端仿真数据驱动（mock 阶段）。

---

## 二、主线总览图（逻辑链路，✅=已通 / ⚠=半通 / ✘=断）

```
基础配置链（组织与权限）
  单位管理 ✅(Alfresco Groups comp_*)
   → 组织管理 ✅(Alfresco Groups dept_*)
   → 人员管理 ✅(Alfresco People)
   → 角色管理 ✅(roleStore→菜单过滤+路由守卫+审批角色)
   → 全宗管理 ⚠(fondsService→finance:fonds 节点，模型未部署,当前必失败;
                  archiveStore 内存种子镜像兜底)

档案配置链（业务规则）
  目录配置 ⚠(Express/JSON 后端，仅 3 字段可配,多死字段)
   → 元数据配置 ✅(三层模型,驱动全局列/详情;volume/box 字段是桩)
   → 档号规则 ⚠(仅"赋号时机"真生效,5 项可配置是假编辑;流水号内存态)
   → 三合一表 ✘(静态文档页,零消费)
   → 组卷盒号 ⚠(perTypeRules 真消费;盒号/sortField/carrierMode 不生效)
   → 四性检测配置 ✘(useState 不持久化,检测执行全 mock)
   → 水印配置 ✅(persist,preview 场景真实生效;download/print 无实现)
   → 报告配置 ✘(占位)
   → 驾驶舱配置 ✅(cockpitStore→大屏真实消费)

业务主线
  收集 ✘ 抓取收集中台(纯展示壳) / 集成接口采集(纯展示壳)
        ——不产生任何 ArchiveRecord,主数据全部来自仿真生成器
   ↓
  整理 ✅ 核对工作台(筛选/补传附件/连续性)
        ✅ 组卷工作台(智能组卷/拆卷插入/四性(mock)/确认赋号/移交归盒)
        ✅ 上传链路 UploadModal/VoucherUploadModal→setRecords 真写入
   ↓
  保管 ✅ 财务分类视图(盒→卷→件只读投影,筛选联动)
        ✘ 实体档案库房(独立 mock,与盒/借阅库存三轨并行)
   ↓
  利用 ✅ 借阅全生命周期(检索→借阅车→审批→拆单履约→归还→逾期→台账)
        ⚠ 案卷移交管理(真 transferLog 混入 mock 行,待处理 Tab 恒空)
        ✘ 工作流设计器(孤岛,借阅引擎不读它)
   ↓
  移交 ⚠ 档案打包(真封装引擎,但下载/移交按钮全部无 onClick)
        ✘ 档案移交(纯占位"开发中")
   ↓
  销毁 ✘ 鉴定销毁(全表单死亡;与库房内嵌销毁流程两套平行假 UI)
   
统计与审计
  档案统计 ✅(驾驶舱/库藏/流程/借阅/合规,statsEngine 全真计算)
  审计追踪 ✘(单凭证 mock)  安全审计日志 ⚠(拼接展示,无审计 API)
```

---

## 三、基础配置链（组织与权限）

| 环节 | 数据模型 | 存储落点 | 消费方 | 完成度 |
|------|----------|----------|--------|--------|
| 单位 | UnitItem（comp_* 前缀组） | Alfresco Groups（真实：comp_HQ/comp_south） | FanzongManager 左侧公司树 | ✅ |
| 组织/部门 | OrgTreeNode（dept_* 前缀组，org_root 根） | Alfresco Groups | 人员管理按部门筛选 | ✅ |
| 人员 | PersonnelItem（People + 组成员） | Alfresco People | 组成员管理 | ✅（与登录演示账号是两套体系） |
| 角色 | RoleKey×7 + UserAccount×8 | 前端 types/user.ts + roleStore(localStorage) | Sidebar 过滤、路由守卫、审批路由 | ✅ |
| 登录 | currentUser（authStore persist） | localStorage | 全局操作人、水印身份 | ✅（未接 Alfresco 认证，Basic admin 硬编码） |
| 全宗 | Fonds（code/name/status/custodianCode/companyId） | ⚠ finance:fonds 节点（模型未部署必失败）+ archiveStore 内存种子镜像 | Header 全宗切换、records 前缀过滤 | ⚠ |

**断点（基础链）**：
1. `finance-model.xml` **未部署**（types API 实测 0 条）→ fondsService.createFonds 必失败，全宗 CRUD 实际走内存镜像
2. 全宗编辑弹窗的 enableYear/remark 字段不持久化（fondsService.updateFonds 无对应属性）
3. 赋号引擎 fondsCode 写死 'Z001'，Header 切换 currentFanzongCode 不影响组卷/赋号
4. 人员管理（Alfresco People，zhangsan）与登录演示账号（MOCK_USERS 8 人）是两套互不相通的用户体系
5. 两套全宗 mock 并存：archiveStore 硬编码种子 + services/api.ts fetchFanzongs 遗留

---

## 四、档案配置链（业务规则 → 消费链路）

| 配置 | 模型 | 存储 | 消费方 | 完成度 |
|------|------|------|--------|--------|
| 目录配置 | DirectoryConfig(archiveTypes/years/projects/层级模板/selectedArchiveItems) | Express 3001 / JSON 文件 | Sidebar 动态子菜单、财务视图子分类 | ⚠ 仅 3 字段可配 |
| 元数据 | L1 字段注册表(metadataContexts)→L2 显示配置(metadataDisplayStore)→L3 列渲染(metadataColumnMaps) | localStorage persist | 全局 7+ 页面动态列/详情 | ✅（volume/box 上下文字段是桩；DA/T 94 完整目录仅展示用双轨） |
| 档号规则 | ArchiveCodeRule 段结构 + assignCodeTiming | localStorage(仅时机) | volumeStore.confirmVolume 赋号引擎 | ⚠ 页面 5 项可配置假编辑；流水计数器内存态刷新归零必重号 |
| 三合一表 | 分类体系/归档范围/保管期限 | 页面内常量 | 无（宣称自动填充/校验均未接线） | ✘ 纯文档页 |
| 组卷盒号 | 全局维度开关 + perTypeRules（DA/T 42） | localStorage persist | volumeStore.generateRecommendations 真实消费 | ⚠ 盒号部分本地态；sortField/carrierMode/itemsPerBox/groupByArchiveType 引擎不读 |
| 四性检测配置 | 5 Tab 方案（哈希/签名/格式/敏感词/节点） | 组件 useState（不持久化） | 无（执行全 mock：setTimeout 全 passed） | ✘ 全链路假 |
| 水印配置 | 7 分组策略（场景/内容/样式/豁免/安全/下载） | localStorage persist | InteractivePreview、借阅阅览室（preview 场景真生效） | ✅（download/print 烧录无实现） |
| 报告配置 | 页面内 mock 数组 | 无 | 无 | ✘ 占位 |
| 驾驶舱配置 | 12 模块目录 | localStorage persist | 统计驾驶舱真实消费 | ✅ |

**断点（配置链）**：
- 目录配置面板不回读已保存勾选（每次进页面显示全选，保存即覆盖）；viewDimensions/levelTemplates 等字段无任何读者与编辑入口
- AccountingMetadataPage 页内完整 DA/T 94 目录与 metadataContexts 注册表是两套平行数据；sourceDocFieldStore 持久化但无业务消费方
- 档号流水计数器（box/volume/itemSerialCounters）只在内存，刷新归零，同年同类必重号，且不与既有案卷去重
- 组卷引擎不读 sortField/carrierMode/itemsPerBox/groupByArchiveType（配置页可调但无效）
- 档号配置页 5 个"可配置项"保存是空操作（TODO），引擎永远用 defaultPaperCodeRule
- 四性检测：配置不持久化、执行全 mock、confirmVolume 不做"归档触发检测 100% 通过"校验

---

## 五、业务主线逐环节

### 5.1 收集 ✘（最大断点）
| 页面 | 现状 |
|------|------|
| 抓取收集中台 ArchiveReceiveCenter | 6 数据源+8 映射+5 任务历史全页面私有 mock；「立即抓取」= setTimeout 转圈；「新增数据源/映射/删除」无 handler |
| 集成接口采集 ApiReceivePage | 5 数据源+同步日志+异常队列全 mock；「全部同步」主按钮无 onClick |

**结论：系统里所有"已采集"数据实际全部来自 `src/data/simulationData.ts` 仿真生成器，收集环节与整理环节完全断裂。**

### 5.2 整理 ✅（唯一真闭环）
```
上传（UploadModal / VoucherUploadModal）
  → archiveStore.setRecords（真写入，status='仅件数据'）
  → 核对工作台（年/月筛选、凭证号连续性检测、补传原始凭证→SourceDocument、推送组卷）
  → 组卷工作台（待组卷池→智能组卷 perTypeRules 推荐/手动组卷/拆卷/插入
    → 四性检测(mock setTimeout)→确认组卷(赋号引擎,DA/T 13 档号)→移交至档案保管
    → toCategoryCode 归一化→匹配/创建盒(archiveBoxStore 真联动)→写 transferLog）
```
- 数据一致性守护：simulationData 8 测试 + volumeGroupingFlow 14 测试
- 瑕疵：VoucherUploadModal 上传电子文件也强制 paper 载体（口径与 UploadModal 不一致）；上传元数据随机生成

### 5.3 保管 ✅/✘
- 财务分类视图 ✅：盒→卷→件只读投影，L1→L2→L3 导航 + 字段级筛选联动（2026-07-18 修复）
- 实体档案库房 ✘：**三轨并行** —— archiveBoxStore（真盒，有 location 无可视化）/ DigitalWarehousePanel（自绘仓位格子，槽位卷号体系与盒完全不同）/ borrowStore 实体库存（按卷推导）——物理位置没有统一模型

### 5.4 利用 ✅/⚠/✘
- 借阅全生命周期 ✅（2026-07-18 新建，persist localStorage，20 测试）
- 案卷移交管理 ⚠：真 transferLog 与 MOCK_TRANSFERS 混排；「待处理」Tab 恒空（状态强制 transferred）；「确认签收」按钮对应状态永不出现
- 工作流设计器 ✘：孤岛编辑器，借阅审批链硬编码在 borrowEngine.computeApprovalRoute，不读 wfDesignerStore

### 5.5 移交 ⚠/✘
- 档案打包 ⚠：packageEngine 真分组/校验/manifest XML，但「下载/移交/移交至档案部」按钮全部无 onClick；loadFromArchive 仅挂载时执行（新组卷看不到）
- 档案移交 ✘：纯占位"功能开发中"

### 5.6 销毁 ✘
- AppraisalManagePage：全表单死亡（鉴定意见 onChange 空、签署/生成清册/导出均无 onClick）；到期天数写死且与当前日期矛盾
- DigitalWarehousePanel 内另有一套平行"三方会签销毁"假 UI，功能定位重叠

### 5.7 统计与审计
- 档案统计 ✅：5 页全真（statsEngine 22 测试）
- 审计追踪 ✘：单凭证 mock 链路，"以下为占位数据"穿帮文案
- 安全审计日志 ⚠：手写 5 条 + records.auditLogs + borrowStore.logs 拼接；"区块链验存"为文案

---

## 六、数据存储现状总表

| 数据 | 存储 | 刷新后 |
|------|------|--------|
| 档案记录 records | 内存（archiveStore） | 还原仿真数据 |
| 案卷/盒 | 内存（volumeStore/archiveBoxStore） | 还原仿真数据 |
| 档号流水计数器 | 内存 | 归零（重号风险） |
| 借阅单/日志/借阅车 | localStorage `borrow-lifecycle-v1` | 保留 |
| 目录配置 | Express 3001 JSON 文件 | 保留 |
| 元数据显示/组卷规则/赋号时机/水印/驾驶舱/角色矩阵 | localStorage persist ×6 | 保留 |
| 单位/组织/人员 | Alfresco Groups/People | 保留 |
| 全宗 | ⚠ Alfresco 节点（模型未部署）+ 内存镜像 | 内存镜像还原 |
| 三合一表/四性配置/报告配置/审计追踪/库房/鉴定/收集 | 页面内常量或 useState | 还原 |

---

## 七、逻辑断点与独立模块总清单（按优先级）

### P0 级（主线断裂）
1. **收集→整理断裂**：两个收集页不产生任何 ArchiveRecord
2. **finance 模型未部署**：全宗 CRUD 名义接通实际必失败
3. **档号流水内存态**：重号风险 + 与既有案卷不去重
4. **四性检测全链路假**：配置不持久化、执行 mock、归档无强制校验
5. **打包→移交断裂**：封装包生成后无处可去（按钮无 onClick + 移交页占位）

### P1 级（双轨/孤岛）
6. 实体库房三轨并行（archiveBoxStore / DigitalWarehousePanel / borrowStore 物理位置模型不统一）
7. wf-designer 孤岛（审批引擎不读流程定义）
8. 赋号引擎与当前全宗脱节（写死 Z001）
9. 案卷移交管理「待处理/签收」状态机空转
10. 鉴定销毁两套平行假 UI
11. 人员两套体系（Alfresco People vs MOCK_USERS 登录账号）
12. 元数据 DA/T 94 目录双轨 + volume/box 字段桩 + sourceDocFieldStore 无消费

### P2 级（占位/假编辑）
13. 档号配置页 5 项假编辑；组卷盒号页盒号部分本地态；组卷引擎漏读 4 个配置项
14. 三合一表零消费（期限推断走 volumeStore 硬编码）
15. 目录配置面板不回读勾选 + 多个死字段
16. 水印 download/print 场景无烧录实现
17. 审计追踪单凭证 mock；安全审计"区块链"文案
18. 全宗 enableYear/remark 不落库；recordCount 同步写 0

### 死代码清理清单
`pages/archive-config/FanzongPage.tsx`、`ArchiveLibPage`、`ArchiveScopePage`、`components/UserRolePanel.tsx`、`App.tsx.bak`、`App.tsx.fixed`、`fix_app.cjs`、`stores/voucherFilesStore.ts`（孤儿）、`DirectoryConfigContext.generateFinanceCategoryTree/ProjectTree/TimelineTree`（无调用方）

---

## 八、UI/交互优化点清单

| # | 位置 | 问题 | 建议 |
|---|------|------|------|
| 1 | ApiReceivePage | 「全部同步」主按钮无 onClick，卡片「手动同步」反而是假动画 | 主按钮接真实批量同步或先禁用并标注 |
| 2 | TransferManagePage | 「待处理」Tab 恒空、「确认签收」永不出现 | 补 pending→prepared→received 状态机或隐藏该 Tab |
| 3 | AppraisalManagePage | 可展开可选择但什么都不发生 | 全表单接真实鉴定流或下线该页至库房模块 |
| 4 | ArchivePackagePage | 新组卷后进来数据不刷新 | 进入页面自动 reload 或监听 volumeStore |
| 5 | UploadModal | 拖入真文件给假元数据+固定审计时间，却 toast"已归档入库" | 元数据标记"待补录"并进入核对队列 |
| 6 | VoucherUploadModal | 上传电子文件强制 paper 载体 | 按文件类型/用户选择决定载体 |
| 7 | AuditTrailPage | "以下为占位数据"穿帮文案 | 接真实穿透链路前改空态文案 |
| 8 | ArchiveCodeConfigPage | 可编辑但保存无效（无反馈差异） | 接通持久化或改为只读+「即将开放」标记 |
| 9 | DirectoryConfigPanel | 不回读已保存勾选 | 初始化读 config.selectedArchiveItems |
| 10 | 组卷工作台 | 移交时 window.prompt 输入库房位置 | 换正式弹窗（下拉库房结构） |
| 11 | DigitalWarehousePanel | 仓位格子与真实盒号/位置无关 | 按 archiveBoxStore.location 渲染真实占用 |
| 12 | FanzongManager | 加载失败静默降级内存镜像，用户无感知 | 顶部加「后端未连接·演示数据」提示条 |

---

## 九、与集成方案的关系

本文件是现状基线。《Alfresco 集成总体方案-2026-07-18.md》中的：
- 内容模型设计 → 解决 P0-2（模型未部署）
- 业务库表设计 → 解决 P0-3（流水号）、P0-4（四性）、P0-5（打包移交）、借阅/日志后端化
- API 分层与 CRUD → 逐模块替代 mock/persist
- 分阶段计划 → 映射第七节全部断点
