# 全链路数据贯通审计报告 · 2026-08-16

> **修复状态：全部断链/孤岛已于当日修复完成**，详见姊妹篇《工作记录-2026-08-16-贯通修复.md》。
> 一处审计误判更正：档号规则配置实际已被服务端赋号引擎消费（VolumeService.assignOnConfirm/buildVolumeCode 读 ams_config），非孤岛——审计时只查了前端消费方。

> 目的：核对系统各环节（全宗/组织/元数据/收集/展示/接口/流程/统计/移交/销毁）数据是否都有真实落库、链路是否端到端贯通；找出断链点并给出修复方案。
> 方法：三路并行静态审计（后端 ams-server 数据落地 / 前端 22 个 store 数据来源 / 业务链路衔接），关键结论全部经过二次人工验证（file:line 证据见文内）。
> 运行时状态：审计时 Docker Desktop 未启动（Alfresco:8080 / PG:15432 / ams-server:8081 / 前端:5000 全部停止），本次为**代码级审计**；运行时冒烟可用 `seed/test-*.mjs` 系列脚本补做。

---

## 一、系统总体思路（架构逻辑）

### 1.1 混合存储架构（两大权威数据源）

```
┌─────────────────────────────────────────────────────────────┐
│  financeWeb (React+TS, vite :5000)                           │
│   22 个 zustand store + 19 个 service                        │
└───────┬──────────────────────────────┬──────────────────────┘
        │ /api/ams → :8081              │ /api/proxy/alfresco → :8080
        ▼                               ▼
┌───────────────────┐          ┌────────────────────────────────┐
│ ams-server (SB)   │          │ Alfresco Content Services      │
│ context-path /api │─────────▶│ 内容仓库 + 认证(ticket)         │
│ JdbcClient 原生SQL │  调REST   │ finance-model v2.2 自定义模型   │
└───────┬───────────┘          └───────────────┬────────────────┘
        │ JDBC :15432                          │ 节点属性/内容落 PG
        ▼                                      ▼
┌──────────────────────────────────────────────────────────────┐
│ PostgreSQL 16.5（同一实例，docker，宿主机 15432）               │
│  ├─ schema alfresco：Alfresco 自身表（节点/属性/权限=内容侧）   │
│  └─ schema ams：21 张业务表（Flyway V1–V5 管理）= 业务过程侧    │
└──────────────────────────────────────────────────────────────┘
```

**分工原则**（Alfresco集成总体方案-2026-07-18 定的决策）：
- **Alfresco 管"内容"**：文件二进制、全宗(finance:fonds)、目录树、件(finance:record)、卷(finance:volume)、盒(finance:box)、原始凭证子件——全部以节点+自定义属性存储；组织/人员/角色 = Alfresco Groups/People。
- **ams schema 管"过程"**：档号流水、借阅4表、审批、履约、操作日志(哈希链防篡改)、四性报告、封装包、移交批次、鉴定、库房树、配置中心KV、用友同步、开放推送、收集台账。
- **配置中心**：`ams_config` 一张 KV 表（jsonb）承载全部前端配置域，前端 `configStorage.ts` 适配器统一读写、API 不可达时降级 localStorage。
- **档号**：`ams_code_serial` 后端乐观锁原子取号（BOX/VOLUME/ITEM 三作用域），彻底解决重号。

### 1.2 ams schema 21 表全清单（Flyway V1–V5）

| 迁移 | 表 | 用途 | 使用状态 |
|---|---|---|---|
| V1 | ams_user_ext | 人员扩展（主键=Alfresco people.id） | ✅ 在用 |
| V1 | ams_code_serial | 档号流水 | ✅ 在用 |
| V1 | ams_borrow_order / borrow_item / approval_step / fulfillment | 借阅全生命周期 4 表 | ✅ 全真在用 |
| V1 | ams_operation_log | 操作日志（append-only+sha256链+触发器禁改删） | ✅ 在用 |
| V1 | ams_inspection_report | 四性检测报告 | ✅ 在用（推送管道写入） |
| V1 | ams_storage_node | 实体库房树 | ⚠️ 后端有端点，前端未接 |
| V1 | ams_config | 配置中心 KV | ✅ 在用（7+ 配置域） |
| V1 | **ams_package** | DA/T 48 封装包 | ❌ **死表**（零 Java 引用） |
| V1 | **ams_transfer_batch** | 对外移交批次 | ❌ **死表** |
| V1 | **ams_appraisal** | 鉴定销毁 | ❌ **死表** |
| V3 | ams_sync_batch / sync_item | 用友同步批次/明细 | ✅ 在用 |
| V4 | ams_open_app / open_push / open_push_item / review_log | 开放推送+审核留痕 | ✅ 在用 |
| V5 | ams_push_log / collect_item | 全链路推送日志/收集台账（四去向） | ✅ 在用（今日新增） |

### 1.3 后端 17 个 Controller（全部真持久化，无假存储）

auth(内存会话12h) / health / records(件,Alfresco) / volumes(卷,Alfresco+档号PG) / boxes(盒,只读) / source-docs(原始凭证) / borrow(借阅,DB全真) / code(取号) / config(配置KV) / datasources(连接配置) / open/v1(推送+台账+字段映射+模拟推送) / review(审核) / inspection(四性) / audit(日志只读) / storage(库房) / stats(统计四域聚合) / yonyou(用友7接口真接+调度)。
种子：DataSeeder（7 演示账号/7 角色组/部门组/全宗目录基础布局，幂等）。

---

## 二、功能域 × 数据落地矩阵（审计主结果）

### ✅ 已贯通（数据有真库、链路闭环）

| 域 | 落点 | 链路证据 |
|---|---|---|
| 全宗 | Alfresco finance:fonds 节点 | FanzongManager 真 CRUD；AppLayout 启动 fetchFondsList 覆盖种子，全局同源 |
| 组织/单位/部门/人员 | Alfresco Groups/People + ams_user_ext | api.ts USE_REAL_API=true 直连 |
| 角色/菜单矩阵 | Alfresco ROLE_* 组 + ams_config(role-menus-v3) | roleStore 持久化，路由守卫+Sidebar 共用 |
| 目录配置 | ams_config(directory) | DirectoryConfigContext → Sidebar/财务/项目视图同源 |
| 元数据显示配置 | ams_config(metadata-display-config-v2) | 26 列注册表一处写多处读 |
| 组卷盒号规则 | ams_config(volume-grouping-config) | perTypeRules 驱动智能组卷推荐 |
| 水印策略 | ams_config(watermark-config-v1) + 服务端 PDFBox 烧录 | 预览/下载/打印三场景 |
| 件（收集池） | Alfresco finance:record | POST /records 真上传、内容下载带水印烧录 |
| 原始凭证（子件） | Alfresco 子节点 | GET /source-docs 真读 |
| 卷 | Alfresco finance:volume + ams_code_serial | 13 端点全真（建/加件/确认赋号/拆卷/移交归盒/退回） |
| 盒（读） | Alfresco finance:box | GET /boxes、盒内卷 |
| 采集-抓取 | 用友 BIP 7 接口真接 + sync_batch/item + collect_item + push_log | 四去向 destination 路由 |
| 采集-推送 | open_app/open_push/open_push_item/push_log/collect_item | 统一四类契约 v2；模拟推送走真实管道 |
| 核对/审核 | collect_item 台账 + review_log + Alfresco 状态属性 | 核对工作台三 Tab；待审核/已处理真 API |
| 归档财务视图 | 盒/卷/件实时装配 | 仅显 transferred 卷，闭环 |
| 门户检索 | usePortalData = 池 ∪ 卷内件合并 | 六种检索模式数据完整 |
| 借阅 | borrow 4 表全真（7态主单/审批/履约10态/黑名单/逾期巡检Job） | 检索加车→提交→审批→出入库→归还→台账→统计 域内闭环 |
| 操作日志 | ams_operation_log 哈希链 | archive-query 的 AuditTrailPage 真查 /audit/logs |
| OCR | tesseract docker 容器真跑 | 上传/扫描双通道 |
| 认证 | Alfresco ticket 真校验 | ams 内存会话 12h（重启丢、可凭 ticket 重建） |

### ⚠️ 半链路（有真成分但存在缺口）

| 域 | 现状 | 缺口 |
|---|---|---|
| 盒（写） | 建盒由卷移交自动完成 | 封盒/上架/删盒仅前端乐观更新（archiveBoxStore，标 P3），后端无写端点 |
| 案卷移交（对外） | 卷属性 transfer 真改 + 自动归盒 | **批次台账无**：ams_transfer_batch 死表；TransferManagePage 混 MOCK 假种子；volumeStore.transferLog 仅会话内存刷新即丢 |
| 统计 | statsEngine 前端对真数据实时计算（非 mock） | 件级统计口径错（只算收集池，已组卷件漏算）；后端 /stats 四端点建成但前端零调用（双轨闲置） |
| 项目分类视图 | 页面在 | 读收集池数据（半成品），与归档态无关 |
| 元数据模型 | finance-model v2.2 权威 | **部署脆弱**：容器重建模型即丢（已四次），需热部署恢复（见 finance-model-deployment 规程） |
| 安全审计日志（系统管理） | /audit/logs 真 | AuditLogsPanel 混入 5 条写死假日志与真数据混排 |
| 会话/token | 可用 | 纯内存（会话 12h、open token 进程内），多实例不可扩展 |
| 凭证管理区文件柜 | 功能在 | voucherFilesStore 把文件 base64 存 localStorage（容量/安全问题） |

### ❌ 断链 / 缺失（本次审计核心发现）

| # | 断链点 | 根因（已验证） | 影响 |
|---|---|---|---|
| 1 | **后台档案查询四页（凭证/事项/附件/审计）在真实后端下永远空** | 四页过滤 `status==='已组卷' && volumeId`（VoucherSearchPage.tsx:51），但 archiveStore.records 只拉 `GET /records` = **未组卷池**（recordService.ts:93 注释"收集池列表（未组卷件）"）；已组卷件入卷后出池 | 后台检索功能整体失效 |
| 2 | **检索 → 借阅车主线两端不通** | BorrowCartBar.tsx:28 用车内 recordId 只查池 records，`filter(Boolean)` 静默丢弃已组卷件；叠加断链 1，后台查询页根本无数据可加车 | "找到档案→发起借阅"主线断裂（门户加车的已组卷档案在结算页消失） |
| 3 | **档案打包页永远空** | packageEngine.ts:77 只取 `status==='已组卷'`，而 ArchivePackagePage.tsx:53,79 传入的是池 records | 打包功能不可用；且 packageStore 纯内存、ams_package 死表 |
| 4 | **核对工作台"补传附件"假持久化** | VoucherManagerPage.tsx:240-268 仅前端 setState；前端 sourceDocumentService 缺上传方法——**但后端 POST /source-docs/by-record/{id} 端点已存在（SourceDocController.java:61）**，纯前端漏接 | 补传刷新即丢 |
| 5 | **采集 → 核对/组卷 池刷新不及时** | ApiReceivePage.handleSync 成功后不调 loadRecords；OpenApiReceivePage 同样不刷；archiveStore 仅启动/切全宗时拉取 | 采集完进核对台需手动刷新页面才见数 |
| 6 | **核对闸门可被绕过（业务管控缺口）** | OpenPushService.java:401-415 任何去向都先 `records.create()` 入池，to-check 仅加台账行；组卷待组卷池 = 全池 | 选"送核对"的件在核对通过前就可被组卷 |
| 7 | **鉴定销毁环节整体缺失** | 无菜单无路由；AppraisalManagePage 是带 MOCK 的死代码；ams_appraisal 死表 | 生命周期尾部缺环 |
| 8 | **实体档案库房纯 mock** | DigitalWarehousePanel.tsx:26 写死 17 格位；后端 /storage/tree|occupancy 端点已建成无人接 | 库房与盒/卷数据零关联 |
| 9 | **档案移交（disposal 组）占位** | ArchiveTransferPage 30 行"功能开发中" | 菜单在、功能空 |

### ❌ 配置孤岛（配置了但不影响任何执行/展示）

| 配置页 | 持久化 | 消费方 | 结论 |
|---|---|---|---|
| 档号规则 archiveCodeConfigStore | ✅ ams_config | **无人消费**（赋号已后端化 ams_code_serial；VolumeWorkspacePage import 未使用） | 孤岛 |
| 三合一表 RetentionConfigPage | ❌ 纯 useState | 期限值在 perTypeRules 另维护一份 | 孤岛+双份维护 |
| 四性检测配置 InspectionConfigPage | ❌ 纯 useState | 真实检测在服务端，不读它；后台 useAppHandlers 检测按钮是 setTimeout 假实现 | 孤岛+假执行 |
| 流程配置 workflowConfigStore | ✅ ams_config | 零消费（借阅审批路由服务端写死） | 孤岛 |
| 报告配置 ReportConfigPage | ❌ 写死 mockData | 无 | 孤岛 |

### 🗑 死代码清单（建议清理）

9 个未路由死页面（SmartReceivePage 整目录/FourChecksPage/TimeViewPage/VoucherUploadPage/VolumePrintPage/AppraisalManagePage/ArchiveScopePage/ArchiveLibPage/FanzongPage）；stores 零消费 3 个（voucherFilesStore[功能在用但存储方式待迁]/viewStore/workflowStore）；archiveStore 残留 mock（treeData/fanzongCategories 无读者）；data.ts(2636 行)+simulationData(781 行) 仅死代码与测试引用；api.ts 死 mock 函数；App.tsx.fixed 残留；vite proxy 残留 :3001（旧 Express 已退役）。

---

## 三、修复方案（按优先级，均可独立实施独立验证）

### P0 — 主线断链修复（建议立即做，预计 0.5–1 天）

**根因归一**：断链 1/2/3 是同一个根因——前端缺少"已归档件"的统一取数口径。

1. **后端 RecordService 加 scope 参数**：`GET /records?scope=all|pool`（默认 pool 保持兼容）；scope=all 查全宗下全部 finance:record 节点（不限"未组卷"），带 volumeId/状态属性返回。
2. **前端 archiveStore 增加 archived 视图或 scope 透传**；档案查询四页、档案打包页改用 scope=all；门户 usePortalData 可切换到此口径（消除自维护的合并逻辑）。
3. **借阅车结算**：BorrowCartBar/Checkout 的对象解析改为"池 + scope=all 缓存"查找；提交时后端按 nodeId 校验兜底（防脏数据）。
4. **补传附件接真**（半小时）：sourceDocumentService 加 `uploadSourceDoc(recordId, file, meta)` 调已有 POST /source-docs/by-record/{id}；VoucherManagerPage 改用它并刷新列表。
5. **采集后自动刷新**：ApiReceivePage/OpenApiReceivePage 在同步/推送成功回调里调 `archiveStore.loadRecords()`；核对通过/审核通过后同样刷新（或引入轻量事件总线统一失效）。

### P1 — 尾部环节补全（1–2 天）

6. **移交批次台账**：后端补 TransferController（CRUD + 状态机 pending→prepared→received）写 ams_transfer_batch；TransferManagePage 删 MOCK_TRANSFERS、transferLog 改读 API；ArchiveTransferPage 实现或暂时从菜单隐藏。
7. **鉴定销毁最小闭环**：到期卷清单（按 retention+年度计算）→ 鉴定登记（写 ams_appraisal）→ 销毁执行（Alfresco 删节点 + operation_log）；恢复菜单路由。
8. **实体库房接真**：DigitalWarehousePanel 接 /storage/tree|occupancy；顺带补 BoxController 写端点（封盒/上架/删盒），archiveBoxStore 乐观更新改真调用。

### P2 — 口径与闸门（0.5–1 天）

9. **统计口径修正**：statsEngine 件级统计输入改 scope=all（与 P0-1 共用）；或统计四页切换调后端 /stats/*。二者择一，避免双口径长期并存。
10. **核对闸门强化**（需产品决策）：组卷待组卷池过滤 collect_item.check_status=pending 的件（"进池但不可组卷"），核对通过才可见；或接受现状并在待组卷池加"未核对"警示列。
11. **删除假检测**：useAppHandlers.handleRunFourPropertiesCheck 的 setTimeout 假实现改调 /inspection/run 或移除按钮。

### P3 — 配置孤岛收编（1 天）

12. 三合一表落 ams_config(retention.table)，perTypeRules 期限改从它派生（消除双份维护）。
13. 四性检测配置落 ams_config(inspection.plan)，服务端检测引擎读取执行。
14. 档号规则页：要么接到后端赋号规则（code_serial scope 规则读配置），要么页面标注"赋号已后端化"并隐藏无效项。
15. 流程配置接借阅审批路由（服务端读 workflow.config）或标注只读；报告配置落库或隐藏菜单。

### P4 — 清理与加固（0.5 天）

16. 删死代码（9 页 + 3 store + data.ts 死引用 + App.tsx.fixed + vite :3001 proxy）。
17. AuditLogsPanel 删 5 条假日志。
18. voucherFilesStore 文件体迁 /records 或功能下线。
19. （可选）会话表 ams_session 持久化；JPA 依赖移除（引了 starter 但零实体）。

### 运行时冒烟（环境起来后补做）

Docker Desktop 启动 → PG/Alfresco 起来 → ams-server 启动（V5 自动迁移）→ financeWeb :5000 → 跑 `seed/test-records-api.mjs`、`test-volumes-smoke.mjs`、`test-yonyou-sync.mjs`、`test-yonyou-autogroup.mjs` 四脚本，再人工走一遍 P0 修复后的「检索→借阅」主线。

---

## 四、一句话总结

**主干已通**：采集（用友/推送/上传/OCR）→ 核对审核 → 组卷 → 归档归盒 → 门户检索 → 借阅全生命周期 → 操作日志，这条主链全部真实 API 闭环、数据有库。
**三个断点**：后台档案查询四页 + 档案打包（取数口径错，永远空）、检索→借阅车（对象解析丢已组卷件）——同一根因，P0 可修。
**两个缺环**：对外移交批次台账、鉴定销毁（表已建好、代码没写）。
**五个孤岛**：档号规则/三合一表/四性检测配置/流程配置/报告配置（配了不生效）。
