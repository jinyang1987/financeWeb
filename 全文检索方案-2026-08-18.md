# 全文检索可行性评估与方案（2026-08-18）

## 一、现状摸底（实测）

| 层 | 现状 | 问题 |
|---|---|---|
| 门户前端 | `usePortalData` 拉 `scope=all` 全量件进内存，JS `includes()` 匹配 8 个字段 | 数据量增长即崩；搜不到正文；无排序 |
| ams-server | `GET /records?keyword=` → 全树扫描 ACS 后 Java 内存 `contains()`，**仅 3 字段**（name / voucherNo / recordRemark） | 同上，且字段更少；每次查询全树拉取 |
| ACS 搜索 API | `GET /alfresco/api/.../search/versions/1/search` 实测 **404**（搜索子系统空转） | 活跃 compose（docker-compose-simple.yml）**无 solr 服务**、ACS 无 solr 连接参数；`docker-solr-1` 已 Exited 2 个月（旧 compose 遗留） |

**但全文检索的原料其实都已就位：**
1. 业务元数据全在节点属性上：finance:* 数十个字段（往来单位/单据号/科目/档号/摘要…，见 finance-model-current.xml）；
2. **正文已回写属性**：OcrService「PDF 双通道」——PDFBox 抽文本层（机打/数电票零误差）+ tesseract OCR（扫描件），统一写入 `finance:ocrText`（RecordController 注释本就写着"写入节点 finance:ocrText → Solr 自动索引"——设计初衷就是走 Solr，部署简化时丢了容器）；
3. 旧 compose（docker-compose.yml）里有现成的 ASS（alfresco-search-services）配置可参照；
4. 权限/行级过滤体系（三维授权）已在 ams 侧成熟，搜索结果接进来即可复用。

## 二、方案对比

| 方案 | 代价 | 正文检索 | 结论 |
|---|---|---|---|
| **A 恢复 Solr(ASS) + ams 搜索代理** | 中低（~1-1.5 人天，无新基础设施） | ✅ 元数据全字段 + ocrText 正文 | **推荐** |
| B PG zhparser/tsvector | 高：record 主数据在 ACS 不在 PG，需双写镜像 + 自编 zhparser 的 postgres 镜像 | ❌ 仍无正文 | 否（架构倒退） |
| C 纯前端扩字段 | 零 | ❌ | 仅权宜，不解决性能与正文 |
| D 新引 ES/Meilisearch | 高：多一套基础设施 + 同步管道 | ✅ | 当前规模过度设计 |

## 三、推荐方案 A 的实施计划

**P1 部署（2-3h，最大风险点=ASS 版本兼容）**
- docker-compose-simple.yml 增加 `alfresco-search-services` 服务（旧 compose 用的 2.0.15 对应 ACS 7.x 时代；现 ACS community 26.1 需 **2.4.x/2.5.x**，实施时查兼容矩阵，不行就逐版试）；
- ACS `JAVA_OPTS` 加 `-Dsolr.secureComms=secret -Dsolr.sharedSecret=secret`，ASS 侧 `ALFRESCO_SECURE_COMMS=secret`（照抄旧 compose）；不需要 ActiveMQ（Solr 主动轮询跟踪）；
- `up -d` 后等全量 reindex（库规模千级节点，分钟级）。

**P2 后端搜索端点（4-6h）**
- `AlfrescoNodeClient.search(ticket, query, skip, max)`：调 ACS public search API（POST afts），`TEXT` 跨字段全文（覆盖全部 d:text 属性含 ocrText）+ `+@nodeType:"finance:record"` 限定 + 可选高亮；
- `RecordController GET /records/search?q=&fondsCode=&skip=&max=`：Solr 候选 → RecordView 映射 → **复用 `perm.recordRowFilter` 行级权限过滤**（Solr 不懂业务密级，必须 ams 后过滤）→ 服务端分页返回；
- 保留原 `/records` 列表端点不动（无关键词浏览场景继续用）。

**P3 前端接入（3-4h）**
- 门户六模式 + 后台档案查询：有关键词时改调 `/records/search`（服务端检索），空关键词维持现状；
- `usePortalData` 不再为检索而全量拉取（浏览/统计仍需要时按需）；
- 回归：vitest 137 + 冒烟扩展搜索用例（元数据命中/正文 ocrText 命中/权限过滤不越权）。

## 四、边界与说明

- 行级权限后过滤：搜索 top-N（如 500）候选再过滤分页，当前规模无压力；量大后再考虑 Solr 侧 ACL 扩展。
- 正文覆盖：原生电子 PDF/图片扫描件均已进 ocrText；若将来要 Solr 直抽 PDF，再补 Transform Core（当前不需要）。
- 资源：ASS 容器约 1-2GB 内存。
- 中文分词：ASS 默认标准分字器对中文按字切分，配合 TEXT 查询可做到任意子串级命中（类似 trgm 效果），够用；要词级精度再谈 zhparser-like 自定义分词（成本高，暂不需要）。

**结论：代价不大。增量工作仅三块——compose 加服务、后端搜索代理端点、前端改调接口；预计 1-1.5 人天，且把"全量拉取内存过滤"的性能债一并还清。**

---

# 二轮补充评估（同日）：两大风险的解法 —— ⚠ 取代上文方案 A

## 新事实（实测/官方）

1. **pg_trgm 现成**：postgres:16.5 容器 `pg_available_extensions` 实测 `pg_trgm 1.6` 可用（官方镜像 contrib，`CREATE EXTENSION` 即可，零部署风险）；zhparser 不可用（需编译，排除）。
2. **ASS(Solr) 是废弃轨道**：官方 [26.1 Release Notes](https://connect.hyland.com/t5/alfresco-blog/alfresco-community-edition-26-1-release-notes/ba-p/497107) 起 Enterprise 移除 Solr；[26.2 Release Notes](https://connect.hyland.com/t5/alfresco-blog/alfresco-community-edition-26-2-release-notes/ba-p/499848) 起含 Community 全版本不再支持；官方方向为 Search Enterprise(ES)/OpenSearch。26.1 推荐矩阵只有 Search Enterprise 5.6，**不含任何 ASS 版本** → 上文"试 2.4.x/2.5.x"= 踩无官方背书的停产分支，下次升级 ACS 还得再拆。
3. **单一写入口成立**：前端 record 域读写全走 ams-server（`services/alfresco.ts` 仅人员域 ACS people/groups 在用）→ PG 读模型一致性前提可靠。

## 问题一解法（全量拉内存崩）：PG 读模型 + 服务端分页（根治）

- **V10 迁移**：`CREATE EXTENSION pg_trgm` + 建 `ams.ams_record_index`（node_id 主键 + fonds/type/year/month/voucher_no/archive_code/摘要/科目/部门/制单人/往来单位/单据号/金额/密级/创建人/时间/卷盒归属 + `search_text` 全文拼接列含 ocr_text、截断上限控索引体积）；GIN `gin_trgm_ops`(search_text) + B-tree(fonds_code, year, archive_type)。
- **写维护（可靠三保险）**：① 单写入口主路径——各变更端点成功后 `RecordIndexService.refresh(nodeId)`（读节点视图 upsert 一行）；② 对账兜底——`rebuild()` 全量重建端点复用现有 gather 逻辑，启动空表自建 + 手动/定期对账（覆盖 Share 手改/seed 直灌等带外漂移）；③ 降级兜底——索引空时列表端点回退现有 ACS gather 路径，降级不报错。
- **端点**：`GET /records/search`（q + 结构化筛选 + 服务端真分页）、`GET /records/facets`（SQL distinct 喂下拉）、统计改 SQL count。
- **前端**：门户改页态——只持当前页，PaginationBar 转服务端分页；全量 JS includes 退役。
- **效果**：任意子串全文（trgm 免中文分词）、索引加速、无全量网络传输与内存常驻。

## 问题二解法（ASS 版本兼容）：不解决，消解

- 复活 ASS = 押停产分支（官方判废、26.1 无推荐版本）→ **原方案 A 撤销**；
- 用问题一的 pg_trgm 路线替代（已实测可用）；检索质量匹配档案场景直觉（凭证号/往来单位/摘要子串命中正是 trgm 强项）；
- 官方继任者（Search Enterprise/OpenSearch）待将来真有词级语义/大规模内容检索需求再评估，当前规模不需要。

## 最终实施顺序（待拍板）

P1 V10 迁移 + RecordIndexService（refresh/rebuild）4-6h → P2 search/facets 端点 + `perm.recordRowFilter` 后过滤 3-4h → P3 门户前端页态化 3-4h → 回归（vitest 137 + 冒烟扩搜索用例）。合计仍 ~1.5 人天，**消灭全部版本/基础设施风险**。
