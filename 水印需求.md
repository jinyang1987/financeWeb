1\. 总体设计目标

安全溯源：发生档案泄露时，能够通过水印精准定位到泄露人、时间和IP。

警示作用：通过醒目的视觉呈现，提醒查阅者当前文件的机密性，降低主动泄露意愿。

防篡改：结合前端防破坏机制与后端文件“烙印”技术，确保水印无法被轻易抹除。

2\. 核心场景与触发时机

系统需要在以下三个关键节点强制注入水印：

2.1 在线预览（打开电子文件）

触发时机：用户在系统内点击查看PDF、OFD、图片（凭证扫描件）或Office文档时。

实现逻辑：为了保证预览性能，通常采用前端渲染水印（如通过 Canvas 生成带有透明度的背景图，覆盖在文档图层之上）。

安全防护：必须加入“防前端篡改”机制（如通过 MutationObserver 监听 DOM 树，防止用户通过浏览器 F12 开发者工具删除水印节点。若被删除，强制关闭预览并记录安全日志）。

2.2 下载与导出（“卸载”电子文件）

触发时机：用户点击“下载”、“导出”或“批量下载”按钮时。

实现逻辑：绝对不能依赖前端！ 必须由后端服务将水印作为图层直接“打死（烧录）”进文件本体中（例如通过 PDFBox 或 iText 处理 PDF），然后再将处理后的文件流返回给用户。

性能要求：大文件或批量下载时，需采用异步排队机制，避免服务器内存溢出（OOM）。

2.3 打印文档

触发时机：用户调用系统打印功能时。

实现逻辑：必须调用带水印的预览流进行打印，禁止浏览器原生无水印打印。

3\. 水印内容设计（动态变量）

水印内容不能是静态的，必须与当前操作的上下文动态绑定。建议包含以下维度的数据：

身份信息：当前操作人的姓名 + 工号/账号（如：张三(004521)）。

时间戳：精确到秒（如：2026-07-18 10:30:15）。

设备信息：操作人的终端 IP 地址（如：IP: 192.168.1.100）。

警示文案：会计档案专属说明（如：机密档案，严禁外传 或 仅供内部审计使用）。

文档状态（可选）：如果档案已作废或处于归档中，可增加 【已作废】 字样。

4\. 水印样式与全局配置（后台管理）

系统应提供一个“水印策略配置”菜单，允许管理员动态调整，而无需重新发版：

布局与密度：采用“全屏倾斜平铺”模式（通常倾斜角 30度 到 45度），确保无论截图文档的哪个局部，都能看到完整的溯源信息。

视觉呈现：

颜色：默认浅灰色（如 #CCCCCC 或 #E5E5E5）。

透明度：建议在 15% - 30% 之间（既能看清字迹，又不会遮挡会计凭证上的关键数字和印章）。

字体大小：可配置，默认 14px - 18px。

策略豁免：配置是否对特定角色（如系统超级管理员、外部高级审计员）免除水印（视具体安全规定而定）。

5\. 高阶安全需求

如果是高密级的会计档案系统，常规的明水印可能不够，建议规划以下进阶功能：

盲水印（暗水印）：除了肉眼可见的文字水印，在文件（尤其是图片和PDF）中嵌入基于频域的数字盲水印。即使用户对屏幕拍照、截图甚至对图片进行裁剪、涂抹、压缩，只要拿到残缺的图片，依然能通过系统工具提取出泄露人的工号信息。

防截屏控制：在移动端App或桌面客户端中，通过系统底层 API 禁用截屏键，或在截屏时直接黑屏。

6\. 技术实现边界与异常处理

格式兼容性：需明确支持打水印的文件类型。常规支持 PDF、OFD、JPG、PNG。对于 Excel/Word，如果在浏览器中预览，通常转化为 PDF/HTML 后再加水印；如果是原文件下载，可以通过后端库修改文档属性或页眉页脚，但防篡改难度大，建议会计档案导出时统一转换为 PDF 格式。

失败降级策略：如果后端加水印服务因负载过高崩溃，系统应阻断下载，而不是将无水印的原文件暴露给用户（即“安全失败”原则）。

