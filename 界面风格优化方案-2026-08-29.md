# 界面风格优化方案（字体·比例·密度·信息完整性）

> 2026-08-29 依据用户提供的两张参考图（紫光电子档案管理系统：档案库配置表单页 + 整理库列表页）提炼。
> 用户明确：**不动配色体系**，优化目标是——字体清晰、比例得当、信息显示全。
> 本方案为**纯计划文档**，列入《系统缺陷审计与修复计划-2026-08-29.md》第六批（T17-T20），批准后再实施。

---

## 1. 参考图提炼的设计语言（紫光系统）

### 1.1 字阶与字体（"字很清晰"的来源）
| 层级 | 参考值 | 用途 |
|---|---|---|
| 系统名 | 20px / 700 | 顶栏左侧 |
| 页面标题/Tab | 15-16px / 600 | 页签、卡片标题 |
| 正文/表格/表单 | **14px / 400（表格内容）、600（标签与表头）** | 主体字号，全站基线 |
| 辅助说明 | 12px / 400 | 次要信息、时间戳 |
| 等宽 | 档号/编号用 monospace | 编号类列 |

字重对比是其清晰感核心：**标签与表头恒 600、正文 400**，层级靠字重+灰度而非缩字号。

### 1.2 密度与比例（"显示得全"的来源）
- 表格：行高 **46-48px**；表头**灰底加粗**；隔行斑马纹；横向浅线、无竖线；**分页条底置固定**（共N条 ‹ 1 › 200条/页）；操作列用**文字链**（"编辑"）而非图标；题名等主列**弹性最大宽度、不截断关键信息**；状态用标签（受控=橙底描边）
- 表单：标签 14px/600 顶置；输入框行高 **40px**；只读灰底/可编辑白底；单选横排；区块间距 24px
- 布局：顶栏 56px 通栏；侧栏 200-220px（图标+文字、当前项浅蓝底+右竖条）；树栏 240px 可折叠；主区边距 16-24px
- 按钮：圆角 4px 小圆角；主按钮实心、次按钮白底描边；工具栏高度 h-9；危险操作弱化为图标置右
- 导航/树：⊞ 展开符、层级缩进对齐、选中整条浅蓝；Tab 为文字+蓝色下划线

### 1.3 一句话总结
**字号全站抬到 14px 基线 + 行高/间距加大 + 表头标签加重 + 关键列给足宽度 + 分页底置**，清晰度与完整感全部来自这四件事，与颜色无关。

---

## 2. 我们现状的差距（代码实证）

- 字号基线偏小：`text-xs`（12px）在表格/表单大量使用，表格 13px 为主；参考系为 14px
- 字重层级弱：表头/标签未恒加粗，层级靠缩小字号（导致"看不清"）
- 列表规整化规范（表头灰带+列竖分隔、13/14px 字阶、48px 行高、h-9 工具栏）2026-08-18 仅在门户综合检索实验，**未全站推广**
- 分页底置仅部分页面；行高/间距各页面自定，无统一 token
- 操作列图标化为主（参考系用文字链，识别成本低）

---

## 3. 设计方案

### 3.1 设计 Token（唯一事实源；落地为 Tailwind theme 扩展 + CSS 变量）

```
字阶 fontSize（全站基线上抬）：
  --fs-display: 20px/700   系统名
  --fs-title:   16px/600   页面标题、卡片标题
  --fs-tab:     15px/600   页签
  --fs-body:    14px/400   正文/表格/表单（基线）
  --fs-label:   14px/600   表单标签、表头
  --fs-aux:     12px/400   辅助说明（仅时间戳/编号等次要信息，不作正文）

行高/尺寸：
  表格行高 48px（表头 44px 灰底）｜表单项行高 40px｜工具栏 h-9(36px)｜按钮 h-8/h-9
  侧栏 220px｜树栏 240px｜顶栏 56px｜主区 padding 16-24px｜卡片圆角 8px｜按钮/输入圆角 4-6px

间距阶梯：4 / 8 / 12 / 16 / 24（禁止 13px 类随机值）

字体栈："PingFang SC","Microsoft YaHei","Noto Sans CJK SC",system-ui,sans-serif
编号等宽：ui-monospace,"SF Mono",Consolas,monospace（档号/凭证号/节点号列）
```

### 3.2 组件级规范（改造点）

| 组件 | 规范 | 现状差距 |
|---|---|---|
| DataTable/列表 | 表头灰底（slate-100）+600+14px；行高 48px；斑马纹 slate-50/60；横线无竖线；操作列文字链（编辑/查看）蓝色；**分页条固定底置**（共N条+页码+N条/页）；主列弹性宽不截断 | 行高/字号/表头不一；分页部分底置 |
| 表单/弹窗 | 标签 14px/600；输入行高 40px；只读灰底（slate-50）可编辑白底；区块间距 24px；必填红星 | 标签偏小、行高 32-36 不一 |
| 按钮 | 主实心 sky-600、次白底描边 slate-300、危险 red 弱化为文字链；圆角 6px；h-8（行内）/h-9（工具栏） | 尺寸阶梯不统一 |
| 树/导航 | 选中整条浅蓝底+左侧 3px 竖条；层级缩进 16px/级；展开符对齐 | 已有基础，统一缩进与选中态 |
| Tab | 文字 15px/600，当前蓝色+2px 下划线 | 部分页面是 pill，需统一 |
| 状态标签 | 描边小胶囊（受控=橙、通过=绿、未过=红），不用实心大色块 | 已接近，统一尺寸 |
| 详情面板 | 字段表 标签列 120px/600 + 值列 14px；长文本不截断（展开/换行） | DetailTable 已有，微调字阶 |

### 3.3 落地机制（不动各页面结构的关键）

1. **tokens 层**：`tailwind.config` 扩展 fontSize/spacing + `index.css` 定义 CSS 变量与全局字体栈、`html { font-size }` 不动（仍 16px 根，组件用相对 token）
2. **共享组件先行**：DataTable / DetailTable / ConfirmModal / 弹窗壳 / 按钮类（`btn-primary/btn-secondary` 工具类）先改——全站页面自动受益 70%
3. **页面逐批走查**：每页只调 className 到 token，不动逻辑；每页走查清单（字号/行高/分页/主列宽/操作列）

### 3.4 页面改造批次（按使用频率排序）

- 第一批（高频主干）：检索门户四模式、组卷工作台、快速检测、件/卷列表、档案分类视图
- 第二批（流程页）：元数据录入弹窗、借阅全链、移交管理、鉴定销毁、回收站
- 第三批（配置页）：档案管理配置 4Tab、四性检测配置、连接配置、角色/菜单矩阵、库房配置
- 第四批（驾驶舱与低频）：驾驶舱大屏（另有大屏规范，仅字阶对齐）、统计五页、操作日志

### 3.5 验收标准

- 视觉走查清单逐页过（字号基线 14px/表头 600/行高 48/分页底置/操作列文字链）
- 174 前端测试全过 + build 通过；关键页面截图对比（改前/改后）附执行日志
- 无信息丢失：任何列/字段不得因排版被移除（只许加宽/换行/展开）

---

## 4. 与总计划的关系

本方案 = 《系统缺陷审计与修复计划-2026-08-29.md》**第六批（界面风格优化）**：
- T17 tokens + 共享组件改造（DataTable/DetailTable/按钮/弹窗壳/全局字体栈）
- T18 高频主干页面走查（门户/组卷/快检/列表/分类视图）
- T19 流程页+配置页走查
- T20 驾驶舱与低频页收尾 + 全站视觉走查验收

实施顺序建议：插在第四批之后、或与第三/四批并行（纯前端样式，与后端改动无冲突）。
