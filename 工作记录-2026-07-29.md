# 工作记录 · 2026-07-29（全天）

> 主线：完成 P1 档案主数据收官 → P2-P4 后端化 → 4 项增强 → 流程配置恢复 → 全站体检整改 → 业务逻辑贯通检查
> 验证基线：tsc 零错误 · vitest 108/108 · vite build 成功（每个里程碑均验证）

---

## 一、P1 档案主数据收官（P1-② ~ P1-⑥）

### P1-② 组卷写路径 ✅
- 后端 VolumeService/Controller 已完整（建卷/加件/拆件/排序/确认赋号/撤销/拆卷/移交归盒/退回，13 端点）；BoxService/Controller 盒列表+盒内卷
- 前端新建 `boxService.ts`（fetchBoxes/fetchBoxVolumes/dtoToBox）
- `archiveBoxStore` 切 API（清除仿真种子 initialBoxes/simulatedBoxes，新增 loadBoxes）
- `AppLayout` 全宗切换联动 loadVolumes + loadBoxes

### P1-③ 读视图切换 ✅
- **核心问题**：财务分类视图显示空盒——useBoxViewData 从 archiveStore.records（仅收集池）查记录，已组卷/移交的记录不在池中
- 后端 RecordService 增 listByParent/listByBox/listByIds；RecordController 增 GET /records/by-volume/{id}、/records/by-box/{id}
- 前端 recordService 增 fetchVolumeRecords/fetchBoxRecords；volumeStore 增 volumeRecords 字段（loadVolumes 并行拉取卷内完整记录）
- useBoxViewData 数据源从 archiveStore.records 切为 volumeStore.volumeRecords
- AppLayout recordCount 聚合回填全宗（收集池+卷内件合计）
- financeViewFilters.test.ts 适配新 assembleBoxViewData 签名

### P1-④ 原始凭证域 ✅
- 后端 SourceDocService/Controller（全宗聚合遍历收集池+案卷库+盒库的 record 子件；GET /source-docs、/source-docs/by-record/{id}、POST 建件）
- 前端 sourceDocumentService.ts（fetchSourceDocsByFonds/dtoToSourceDoc）；sourceDocumentStore 切 API（清种子+loadSourceDocs）

### P1-⑤ 元数据双轨合一 ✅
- volumeColumns.tsx 补真实列定义 V1-V20（accessor 改用 Volume 类型，含卷状态徽章）
- boxColumns.tsx 补真实列定义 B1-B12（accessor 改用 ArchiveBox 类型，含盒状态徽章）
- 修正 VolumeStatus 全 6 值（draft/confirmed/numbered/completed/transferred/destroyed）

### P1-⑥ 配置真生效 ✅
- 组卷引擎 sortField 三模式排序消费（voucherNo/month/amount，原硬编码 voucherNo）
- carrierMode 消费（纯电子模式 maxItems 放宽至 200）
- 档号 buildVolumeCode 配置化（serialDigitsVol/Box、separator、categoryPrefix 从 ams_config 读取）
- archiveCodeConfigStore 扩展 4 个可配字段

---

## 二、P2-P4 后端化 + 4 项增强

### P2-P4 后端服务（ams-server）
- BorrowService/Controller（借阅 4 表 + 11 端点：申请/审批/拆单/履约/巡检/黑名单）
- OperationLogService/Controller（ams_operation_log SHA-256 哈希链）
- InspectionService/Controller（四性检测：真实/完整/可用/安全 + 报告落库 + 节点 aspect 回写）
- SourceDocService、StorageService/Controller、StatsController（inventory/borrow/lifecycle/compliance）
- BorrowService 6 处动作（申请/审批/驳回/出库/归还/中止）写操作日志
- pom.xml 加 PDFBox 3.0.4 + Tess4J 5.13.0

### 4 项增强
1. **水印烧录**：WatermarkService（PDFBox 逐页平铺/居中烧录，读 ams_config('watermark')）；RecordController 下载/打印时拦截烧录
2. **OCR**：OcrService（Tess4J 识别，ams.ocr.enabled 配置）；RecordController POST /records/{id}/ocr 写 finance:ocrText
3. **档号段结构全量可配**：buildVolumeCode 读配置；ArchiveCodeConfigPage ConfigCard 接 ams_config 保存+回显
4. **统计服务端聚合**：StatsController lifecycle/compliance 多维 SQL

---

## 三、流程配置恢复（用户报障：流程配置菜单消失）

### 排查结论
- `config-workflow`（流程配置）在 2026-07-18 借阅重写菜单重组时被漏删，菜单项+ContentArea 路由被移除，组件残留成孤儿
- 旧 `WorkflowConfigPanel` 是卡片式（非可视化），且仅遗留 PageRouter 引用

### 处理
- 重写 `workflowConfigStore`：统一数据模型（业务元数据 类别/触发/核查/责任人 + 可视化节点图 nodes/connections），持久化到 ams_config('workflow.config')；4 条内置流程带真实节点图；全部画布操作（节点增删改/连线/自动布局 BFS 分层/选择态/连线模式）
- 重写 `WorkflowConfigPage` 为三栏可视化设计器（左流程清单按类别分组 / 中 SVG 画布点阵网格+贝塞尔连线+节点差异化造型 / 右上下文属性面板）
- 菜单项加回（水印配置下方）+ ContentArea 路由 + 标题映射
- 删除旧 WorkflowConfigPanel + 清理 PageRouter 死引用

---

## 四、全站样式/结构体检整改（用户：全网站页面检查）

### P0 无效 Tailwind 类名
- 修复活跃文件非法色阶（slate-55/150/205/250/350/450/455/650/705/850、blue-150/605/650/805、emerald-150/250/650、indigo-150/505/605、rose-250/505、amber-850）
- 活跃文件：ArchiveBoxTreeView/AuditLogsPanel/DrawerPanel/DigitalWarehousePanel/InteractivePreview
- 全站零非法色阶

### P1 主色统一
- indigo+blue → sky，77 文件 983 处（441 indigo + 542 blue）
- 新老页面主色一致（sky=主色/信息，green=成功，amber=警告，rose=危险）

### P2 死代码清理
- 删 8 个孤儿/废弃组件：AdvancedWorkbenchSidebar、ArchiveOfflineReceive、ArchiveTree、CategoryConfigPanel、UserRolePanel、SmartDataPage、WorkflowConfigPanel、StatsDashboard
- 清理 PageRouter 中 WorkflowConfigPanel/StatsDashboard 死 import

### P3 接真后端
- boxColumns/volumeColumns 真实列定义（见 P1-⑤）
- 档号配置 ConfigCard 接 ams_config（见 P1-⑥/增强3）
- AuditTrailPage 接操作日志 API（fetchAuditLogs，哈希链展示，空状态，清除 MOCK 假数据）
- 后端 BorrowService 动作写日志

### P4 细节
- 收敛 2 处无效渐变（RetentionConfigPage/VolumeGroupingConfigPage 的 from-sky-50 to-sky-50）
- emoji 换 lucide：载体对 🖥️📄（5 文件）、状态符 ✓✗✕、装饰 📦📐👁👆📝📎⚠ 等
- 保留：DigitalWarehousePanel 印鉴星形★（语义内容）、WorkflowConfigPage SVG 画布 ▶■◇≡👤（SVG 内无法用 lucide）、代码注释里的 ★ 标记
- ApiReceivePage mock 数据图标转 lucide（SOURCE_ICONS 映射）
- 用 Node 脚本做权威 emoji 扫描（PowerShell 的 emoji/UTF-8 处理不可靠，多次误报，最终用 UTF-32 码点+Node 确认）

---

## 五、业务逻辑贯通检查（用户：贯通一遍找硬伤）

### 检查方法
按真实业务流逐条追踪代码：登录→全宗→数据加载、收集→组卷→赋号→移交→视图、借阅全链路、统计、配置。核对各 store 的 API 调用真伪。

### 🔴 硬伤 #1：借阅模块前后端脱节（已修）
- **现象**：borrowStore 头部注释谎称"P2-2 已切真后端"，实测 borrowApi 调用 0 次、零持久化（persist 是死导入）、orders:[] 纯内存、刷新即丢、单号 orders.length+1 本地生成会重号
- **对照**：archiveStore/sourceDocumentStore/volumeStore 都真接 API（2/2/20 处），唯 borrowStore 0 处
- **后果**：借阅数据刷新即失；后端 BorrowService（4表+11端点+日志）完全闲置；借阅统计永远 0
- **修复**：① 更正假注释 ② 包 persist 接 configStorage→ams_config('borrow-lifecycle')，orders/logs/cart 持久化，刷新不丢；保持同步状态机不破坏 8 个借阅页（runDaily 同步返回计数等）；108 测试通过
- **彻底方案**：另写《借阅模块前后端贯通迁移方案-2026-07-29.md》（状态下沉关系型后端，需后端 schema 补全，建议可构建环境执行）

### 🟠 硬伤 #2：四性检测页假数据（已修）
- **现象**：FourChecksPage 全 MOCK_SUMMARY/EXCEPTIONS/HISTORY，未调 InspectionService（runInspection 无人调用）
- **修复**：接 fetchInspectionReports 真实聚合（概览/异常/历史 by 日期分组）+ 空状态 + passRate 除零防护 + 运行检测按钮接真刷新（loadReports）

### 遗留 Mock 页面（需补建后端，待立项）
ApiReceivePage（收集接口·框架）· AppraisalManagePage（鉴定销毁）· TransferManagePage（案卷移交）· VoucherUploadModal/Page（上传模拟）· smart-receive 三 Tab（OCR/抽取/转换演示）· triggerOcr 未接线

### 贯通正常的链路 ✔
档案主流（上传→收集池→组卷→赋号→移交→财务视图，volumeStore 20 处 API）· 登录/全宗/配置持久化 · 档案类统计读真实数据

---

## 六、产出文档
1. 《借阅模块前后端贯通迁移方案-2026-07-29.md》—— 数据形状对照/缺口/4 步迁移/风险回滚/工作量
2. 《工作记录-2026-07-29.md》—— 本文
3. 《实施任务分解-2026-07-18.md》—— 追加"全站体检与逻辑贯通"章节

---

## 七、关键经验教训
1. **"已迁移"注释要与实际代码核对**：borrowStore 假注释误导，需以 API 调用实测为准（grep borrowApi. 调用数）
2. **PowerShell 处理 emoji/UTF-8 不可靠**：Get-Content -Raw 与 .Contains 多次误报，最终用 Node（项目原生 UTF-8）+ UTF-32 码点扫描才权威
3. **数据源切换要查消费链**：useBoxViewData 空盒问题根因是记录被 move 出收集池后查不到，需卷内记录单独加载
4. **同步状态机迁移要保接口**：borrowStore 的 runDaily 同步返回计数被页面直接用，迁移时保持同步接口避免破坏调用方
5. **印鉴星形/SVG 符号不是装饰 emoji**：DigitalWarehousePanel 的★是印章内容、SVG 画布符号无法用 lucide，需语义判断而非一刀切

---

## 八、最终状态
- 五阶段路线图 P0-P4 全部完成（29/29 + 1 取消）
- 全站体检整改完成（无效类名/主色统一/死代码/接后端/emoji）
- 2 个核心逻辑硬伤已修（借阅持久化、四性接真）
- 验证：tsc 零错误 · vitest 108/108 · vite build 成功
- 待办：借阅模块彻底贯通（见迁移方案）· 遗留 mock 页面补后端

---

## 九、借阅模块前后端贯通迁移（执行完成）

> 按《借阅模块前后端贯通迁移方案》4 步逐项执行，全部完成。

### 步骤 1：后端数据形状补全 ✅
- **Flyway V2__borrow_enrich.sql**：ams_borrow_item 加 voucher_no/archive_type；ams_fulfillment 加 volume_title；ams_borrow_order 加 applicant_name/applicant_emp_no/applicant_dept（申请人冗余，submit 时写入，免 join）
- **BorrowService.submitOrder**：order INSERT 写申请人 3 字段；item INSERT 写 voucher_no/archive_type
- **BorrowService.splitFulfillments**：电子/实体两处 fulfillment INSERT 写 volume_title（用件题名，与前端 borrowEngine 行为一致）
- enrichOrder 经 SELECT * 自动带出新列，返回嵌套结构（order + items + approvalRoute + fulfillments）
- 已核对 4 处 INSERT 占位符与参数数量一致（沙箱无法编译 Maven，按既有 JdbcClient 模式编写，待可构建环境验证）

### 步骤 2：前端映射层 ✅
- borrowService.ts 增 4 个 DTO 接口（OrderDto/ItemDto/StepDto/FulfillDto）+ toStrArr（兼容 text[] 返回数组或逗号串）
- 增 dtoToBorrowItem/dtoToStep/dtoToFulfill/dtoToBorrowOrder 映射（snake_case→驼峰；roleLabel 用 ROLE_LABELS；assigneeName 用 firstUserWithRole）
- SubmitOrderCmd.items 补 voucherNo/archiveType
- fetchOrders/fetchOrder 返回映射后的 BorrowOrder[]/BorrowOrder
- submitOrder/approveOrder/rejectOrder/terminateOrder 返回映射后的 BorrowOrder；runDailyCheck 类型化为 DailyCheckResult

### 步骤 3：borrowStore 改纯 API 读写（核心）✅
- **完整重写 borrowStore**：移除 persist/buildSeedLogs/本地计数器/前端状态机运算
  - 新增 loadOrders/loadLogs（从后端拉取）
  - 10 个动作全改 API：submitOrder/cancelOrder/approveCurrentStep/rejectCurrentStep/checkoutPhysical/returnPhysical/terminateOrder/runDaily 调 /borrow/** 后 loadOrders 刷新；logAction 触发 loadLogs（后端已自动写哈希链日志）
  - cart 保留本地暂态
  - 8 个选择器函数（myOrders/pendingApprovalsForRoles/activeElectronicGrants/pendingCheckouts/lentOutPhysical/queuedReservations/volumeStockStatus）原样保留
- **接口异步化适配**（2 处用同步返回值）：
  - BorrowManagePage handleRunDaily 改 async + await runDaily()
  - BorrowCartCheckout handleSubmit 改 async + await submitOrder()
- **AppLayout 挂载加载**：useEffect 一次性 loadOrders + loadLogs（借阅数据全局非全宗维度）
- **测试重写**：borrowLifecycle.test.ts 的"全链路集成"块（测已下沉后端的本地状态机）改写为 **mock borrowService 的 API 接线测试**（8 个：loadOrders/submitOrder/approve/reject/checkout/return/terminate/runDaily/cart 本地）；borrowEngine 纯函数测试（审批路由/拆单/状态机/巡检黑名单）保留

### 步骤 4：统计/审计接真实数据源 ✅
- BorrowStatsPage 与 useArchiveStats 读 borrowStore.orders/logs，经步骤 3 已为真实后端数据（挂载加载）
- AuditTrailPage 读 fetchAuditLogs（操作日志哈希链，已于体检阶段接通）
- 后端 SQL 聚合（fetchBorrowStats）列为大数据量下的未来性能优化（当前前端富计算——热力图/月度趋势/红黑榜——后端尚未提供对应聚合，保持读 API 化 store）

### 验证
- **tsc 零错误** · **vitest 111/111 全过**（纯函数 + API 接线）· **vite build 成功**（1848 模块）
- 后端 V2 + BorrowService 改动待可构建环境编译验证（已核对占位符/参数一致）

### 贯通后的借阅架构
```
borrowStore（前端，纯 API 读写）
  ├─ loadOrders/loadLogs ← /borrow/** + /audit/logs
  ├─ 各动作 → 后端运算 → loadOrders 刷新
  └─ cart 本地暂态
BorrowService（后端，唯一事实源）
  ├─ 4 表关系型 + ams_code_serial 原子单号（绝不重号）
  ├─ 审批路由/拆单/履约/巡检/黑名单 全后端
  └─ ams_operation_log 哈希链审计
统计/审计 → 读 API 化 store（真实数据，多端一致）
```

### 遗留
- 后端 V2 迁移 + BorrowService 改动需在可构建环境 `mvn compile` 验证（沙箱 Maven 受限）
- 建议验证后做手工 E2E：申请→审批→拆单→出库→归还→逾期→刷新不丢

---

## 十、上传弹框报「No static resource records」三层故障修复（下午·用户报障）

### 现象
「会计档案文件上传」弹框点「确认上传入库」报 **No static resource records**。

### 排查路径（每步都有实锤）
1. **错误定性**：该消息是 Spring `NoResourceFoundException`——请求落到了静态资源处理器，即**控制器根本没注册**，不是业务异常。
2. **逐端点探测运行中的 ams-server（8081）**：health/config/box 正常，records/volumes/borrow/stats/audit/source-docs/storage/code **9 个控制器全部 404**——典型的部分注册。
3. **时间戳比对**：JVM 启动于 10:19（%TEMP% argfile 时间戳），而 target/classes 全部 .class 编译于 10:36-10:37（晚 18 分钟）→ **启动瞬间 classes 是半成品**（IDE 自动构建与 Maven 编译交错所致），Spring 只扫到部分控制器。
4. **重启前先验编译**（当日工作记录自己标注"待可构建环境验证"）：`mvn compile` **果然失败，7 处错误**。
5. **编译修好后重启，又暴露第三层**：DataSeeder 日志「全宗 seed 失败（不阻断）: finance:code isn't a valid QName」→ **finance 内容模型第三次丢失**（acs 容器当日重建，extension 热部署文件丢失），根目录「会计档案管理」children 静默为空 → 不修则上传必撞「全宗不存在： Z001」。

### 修复清单
**A. 后端编译错误 7 处**（当日盲写代码，沙箱无法跑 Maven 遗留）：
| 文件 | 错误 | 修法 |
|------|------|------|
| BorrowService.java ×4（:170/:296/:402/:413） | `optionalRow()` 方法不存在（幻觉 API；spring-jdbc 6.2.8 javap 实测 ResultQuerySpec 仅有 listOfRows/singleRow/singleColumn/optionalValue） | `listOfRows().stream().findFirst()` |
| OperationLogService.java:34 | 同上 | 同上 |
| StatsController.java:47 | 同上 | 同上 |
| WatermarkService.java:64 | PDFBox 3.x 已删 `PDDocument.load(InputStream)` | `Loader.loadPDF(byte[])` + 换 import |

**B. finance-model 热部署**（按规程 3 步，第三次执行）：docker cp 模块内权威 finance-model.xml + seed/finance-model-context.xml → 重启 acs → `GET /types/finance:volume` 200，Z001/Z002 全宗节点从 PG 自动恢复可见。
**C. 杀掉半成品 JVM（含 Maven 父进程）→ `mvn spring-boot:run` 重启**。

### 验证（全过）
- 端点探测：/records /volumes /borrow /audit /source-docs /storage 等全部 401（已注册需认证），不再 404
- `mvn compile` BUILD SUCCESS
- **端到端冒烟**：登录 → 复刻弹框字段 multipart 上传 → 返回 finance:record 节点（档号 Z001-PEND-×××，中文元数据全对）→ 清理测试节点 204
- 前端代理链 `localhost:5000/api/ams/health` → 200

### 经验教训
1. **「待可构建环境验证」必须当真**：IDE 能编译 ≠ Maven 能编译；后端改动交付前必须跑一次 `mvn compile`
2. **Spring 404 排查套路**：先看是否落静态资源处理器（NoResourceFoundException），再比对运行中 JVM 启动时间 vs classes 时间戳，判半成品启动
3. **finance-model 丢失已是第三次**，每次容器重建都丢；根治（重建 platform jar+镜像）仍在挂账，建议尽快做
4. **Windows Git Bash 调 curl 传中文参数会被 GBK 化**（本次冒烟 retention=30年 被拒的假象），表单字段中文须用 `-F 'field=<file'` 从 UTF-8 文件读

---

## 十一、核对工作台移除「上传凭证」按钮（下午·用户决策）

### 需求
用户：核对工作台不应有上传凭证按钮——**工作台凭证主要来自前端业务系统推送**，在此只做核对，应去掉上传、只保留推送。

### 分析（确认用户判断正确）
- 页头注释本就写明会计实操原则：「凭证号由会计系统生成，本系统不编造凭证号」，顶部「上传凭证」按钮（2026-07-21 P1-① 加的真上传入口）会手工创建新记账凭证记录，与该原则直接冲突
- 数据流向：档案收集组（抓取收集中台/集成接口采集）→ 收集池 → 核对工作台 → 推送组卷工作台，核对环节不产生凭证
- **保留**：行内「上传」按钮 = 补传原始凭证附件（创建 SourceDocument 绑定凭证号），是页面标题三大职能之一（凭证连续性·**补传附件**·推送组卷），属核对补救动作，不动

### 变更
- 删 VoucherManagerPage 顶部「上传凭证」按钮（含注释），「推送至组卷工作台」保留为唯一主操作
- **连带影响**：全局 UploadModal（会计档案文件上传弹框）此前唯一入口就是此按钮，现组件保留但**无入口（休眠）**；AppLayout 挂载与 archiveStore.isUploadOpen 未动。归宿待定：建议挂到档案收集组页面，或后续整体删除

### 验证
- tsc 零错误 · vitest 111/111 全过

---

## 十二、「确认入待组卷池报全宗不存在」排查（下午·用户报障 #2）

### 结论
**不是新故障，是第十章故障的窗口期余波**。finance 模型缺失期间（12:45–12:57），Alfresco 根目录下 finance:fonds 节点「隐形」，RecordService.resolveFonds 扫描为空 → 任何全宗号都报「全宗不存在」。模型恢复 + DataSeeder 自愈后（Z003 为本次新创建），实测 Z001/Z002/Z003 三个全宗号走 /records 全部 200。

### 排查中的两个干扰项（记录备查）
1. **run.log 无「创建全宗 Z003」日志**：一度误判 seeder 未建 Z003，实为日志抓取遗漏，REST 直查确认 Z003 节点已存在
2. **curl 传中文参数的 GBK 假象**（第十章教训4 再次应验）：冒烟时 retention=30年 被误判拒绝，改 `-F 'field=<file'` 从 UTF-8 文件读字段值后正常——浏览器环境无此问题

### 根治挂账（第三次强调）
finance-model 容器重建必丢已发生 3 次（07-20/07-21/07-29），根治=重建 platform jar+AIO 镜像，仍在 TODO。

---

## 十三、OCR 识别全链路接真（下午·用户报障 #3「识别准确率不高」）

### 诊断（用户直觉完全正确）
原「AI 识别与归类」是 `simulateOcr()` 纯演示桩：文件名只匹配 4 个写死样例名，**不命中则类别抛硬币（Math.random×2）、凭证号/金额/日期全编造、置信度也是随机数（55-94）**。对真实文件识别率=50% 随机基线，零 AI 零 OCR。

### 发现的引擎现实（与设想不同，绕过两个坑）
1. **OcrService 原本是本地 Tess4J 且 enabled=false**：Windows 宿主机无原生 tesseract，且 PDF 分支有 bug（PDF 字节当 jpg 喂引擎）
2. **tesseract4re 容器是工具箱**：compose 定义 `command: tail -f /dev/null`，无 REST 进程（端口 8091→8080 映射形同虚设，连接挂死）→ 唯一可用调用方式是 `docker exec`

### 实施方案（三层）
**A. OcrService 重写（后端）**
- 引擎顺序：`docker exec -i <容器> tesseract stdin stdout -l chi_sim+eng`（管道喂字节）→ local Tess4J 兜底（吞 UnsatisfiedLinkError）
- PDF 双通道：PDFBox 文本层直抽（≥50 字符，机打/数电票零成本零误差）→ 扫描件逐页渲染 300dpi → OCR（封顶 3 页）
- 超时 60s destroyForcibly；配置 ams.ocr.*（enabled/engine/container/lang/max-pages/timeout）
- 新端点 `POST /records/ocr-scan`（multipart 无状态预识别，不建节点，返回 ocrText+耗时）

**B. docClassifier.ts 规则分类器（前端纯函数，替换随机桩）**
- 原理：两类单据版式语义稳定互斥，关键词加权打分——记账凭证（标题/凭证字号强特征 + 会计科目/借贷方/制单审核记账佐证）vs 原始凭证（发票家族/报销单/审批单/回单等强特征 + 税率/税额/价税合计佐证，对齐 96 种原始凭证目录）
- 字段抽取：凭证号（记/收/付/转 归一化**保留前导零**）、金额（行标签优先级：价税合计>合计>金额，兜底取全文最大）、日期（中文/连字符归一 YYYY-MM-DD）
- 置信度 = f(最高分, 胜负分差)，命中证据 hits 可解释；**拿不准一律「未识别」宁缺毋滥**
- 紧凑串匹配（去空白）对抗 tesseract CJK 插空格噪音

**C. VoucherUploadModal 接线**
- processFiles 改真：逐文件 ocr-scan → classifyDocument；OCR 服务不可用降级「仅文件名识别」置信度封顶 60 并标注，不再随机编造
- 步骤名「AI 识别与归类」→「OCR 识别与归类」（诚实）
- 入池 archiveType 改读（人工修正后的）识别结果，不再硬编码记账凭证
- 删除 simulateOcr/MOCK_VOUCHERS；孪生孤儿页面 VoucherUploadPage 仍是假桩（无路由引用，未动）

### 验证（全过）
- 后端 mvn compile BUILD SUCCESS；前端 tsc 零错误
- **vitest 123/123**（新增 docClassifier 12 用例：定类×6 + 字段抽取×6；测试还真抓出 2 个 bug：凭证号前导零被吞、发票金额错取合计金额→改取价税合计）
- **真实 OCR E2E 三路全通**：①PowerShell 生成凭证测试图→OCR 1.2s 全文正确（记-004/23,500.00/日期齐）②发票测试图→0.8s 全文正确（00512366/税率/价税合计齐，分类器判原始凭证 97%）③真实工作区 PDF→文本层直抽 0.5s

### 经验
1. **「遗留 mock」挂账要定期清**：随机桩在演示环境无感，接真数据立刻露馅
2. **容器「Up」≠ 服务可用**：tesseract 容器 tail -f /dev/null 空跑，端口映射无进程监听——排查先 `docker exec ps aux`
3. **先写测试再修抽取逻辑**：前导零与价税合计两个 bug 都是单测抓出来的

---

## 十四、「确认入待组卷池报全宗不存在」根因定位与修复（下午·二轮攻坚）

### 真相
用户重测仍报「全宗不存在」。一轮误判为模型缺失窗口期余波后，服务端加诊断埋点（记录请求 fondsCode 原值+长度+库内清单），用户再点一次即捕获真值：
```
请求 fondsCode=[Fonds Z001](len=10)   ← 发的是节点【显示名】，不是全宗号！
```

### 完整根因链（四层叠加，缺一不可）
1. **fetchFondsList 漏参**（fondsService.ts）：查 Alfresco 子节点未带 `include=properties` → 响应无 `finance:code`
2. **toFondsNode 兜底失效**：`code = props['finance:code'] || entry.name.replace('全宗 ','')` —— 现存节点是早年**英文命名**（"Fonds Z001"，非 DataSeeder/createFonds 的"全宗 Z001"），replace 匹配不上 → code 退化为显示名 "Fonds Z001"
3. **AppLayout 自动校正固化污染**：挂载时发现已存 'Z001' 不在列表（列表代号全是 "Fonds Z001"）→ setCurrentFanzongCode('Fonds Z001')
4. **服务端按 code 严格匹配** → 库内 Z001/Z002/Z003 → FONDS_NOT_FOUND

### 排查方法论（值得复用）
- 排除 7 项嫌疑全靠实锤：三全宗节点直查存在、admin/demo 账号三 code 全 200、死 ticket 报的是「会话已过期」、三收集池全空（件确未入池）、run.log 捕获用户 13:46 刷新流量（证明连的就是本后端）
- **决定性一步是服务端埋点**：与其猜前端发什么，不如让日志说——一轮埋点一次点击即锁定真值

### 修复
1. **fondsService.fetchFondsList 补 `include=properties`**（主修复，响应带 finance:code）
2. **toFondsNode 兜底强化**：`replace(/^(全宗|Fonds)\s+/i,'')` 兼容中英两种历史命名（纵深防御）
3. **服务端错误消息增强**：`全宗不存在: X（库内现有全宗: Z001,Z002,Z003）` + WARN 日志（RecordService/RepoLayout 两处），以后这类问题一眼定位
4. 用户浏览器污染值**自愈**：刷新后列表返回正确 code，AppLayout 自动把 "Fonds Z001" 校正回 "Z001"，无需手工清缓存

### 顺带修复：ams_operation_log SQL 全面错位（排查中发现的独立 bug）
- **现象**：/audit/logs 500 BadSqlGrammar（用户每次刷新都触发，borrowStore loadLogs）
- **根因**：当日盲写的 OperationLogService 与 V1 表结构全面脱节——时间列 `ts` 写成 `created_at`、目标列 `target_label` 写成 `target`、id 是 bigserial 却 INSERT UUID 字符串、order_id 是 uuid 却 setString 直插
- **修复**：查询 `ORDER BY ts DESC` + 别名兼容前端契约（`ts AS created_at, target_label AS target`，前端零改动）；INSERT 按真实列名、id 省略自增、orderId 经 toUuidOrNull 转换、ts 用 Timestamp 传值；非 UUID orderId 查询加 `AND 1=0` 短路
- **验证**：psql 事务内试插+别名查询（ROLLBACK）通过；/audit/logs 带认证 200；前端契约字段（created_at/target）保持可用

### 验证
- 复现：vite 代理直查——不带 include=properties 无 properties 键（bug 复现），带则 finance:code 全齐（修复生效）
- tsc 零错误 · vitest 123/123 · mvn compile BUILD SUCCESS · /audit/logs 200

### 经验
1. **「名称兜底当代号」是经典陷阱**： Alfresco 节点 name 是显示名（还可能多语言/历史异构），业务代号永远走 properties
2. **错误消息带上上下文**：本次若不是给异常加「库内现有全宗」对比，定位还要更久——诊断型错误消息是最便宜的排障工具
3. **盲写代码的债集中爆发**：今日三起（optionalRow 幻觉 API、PDFBox.load、oplog 列错位）全是同一根源——**后端改动必须 mvn compile + 最小冒烟才算交付**

---

## 十五、「上传两件丢一件」排查与修复（傍晚·用户报障 #4）

### 现象
用户经资料归档弹窗上传 1 张记账凭证 + 1 张原始凭证，收集池最终只有 1 件（原始凭证），记账凭证「丢了」。

### 排查实锤链
1. **池内实物**：唯一记录 voucherCategory=原始凭证、voucherNo=20240629（文件名兜底）、14:44 创建
2. **服务端日志**：两次 OCR（记账凭证文本层仅 78 字符 / 原始凭证 337 字符），但「建件成功」**只有一条**——服务端从未收到第二件的上传
3. **结论**：件在前端被拦，没发请求

### 根因 A（用户可见的丢件）：入池门槛静默丢件 UX bug
- 入池过滤：`verified 或 (ocr-done 且置信度≥80)`；按钮计数却是 `stats.verified || stats.done`（显示 2 件）
- 记账凭证文本层仅 78 字符（薄文本层 PDF），关键词命中少 → 置信度 ~67 <80 → **被门槛拦下，但按钮仍显示 2 件**，点击后只入池 1 件，toast「成功 1 件」3 秒即逝 → 用户视角=丢数据
- **修复（VoucherUploadModal 四连改）**：
  1. 按钮计数 = `eligible.length`（真实将入池件数），eligible=0 禁点
  2. 琥珀色警示条：「有 X 件置信度不足 80%，本次不会入池，请校验确认」
  3. 上传后**不再清空被拦文件**——保留在列表继续校验， toast 明示「已入池 N 件；还有 X 件置信度不足」
  4. 「批量确认所有」→「批量确认高置信」（低置信度仍须逐条人工校验，防盲信坏数据）

### 根因 B（排查中揪出的暗雷）：IDE ECJ 残类二度爆发（与今早同源）
- 日志发现 14:42 起 `/volumes` `/boxes` `/source-docs` 等读路径全部 500：`java.lang.Error: Unresolved compilation problem: log cannot be resolved`
- **机制**：我给 RepoLayout 加 `log.warn`（该类无日志声明、全库零 Lombok）→ IDE 的 ECJ（无 Lombok 注解处理）编译报错并「带病产出」残类盖进 target/classes；Maven 增量编译按时间戳误判「已是最新」**跳过不编，BUILD SUCCESS 是空心的**；重启加载残类 → 运行期炸
- 与今早「半成品 target/classes」是同一病灶（IDE 构建产物污染 + Maven 增量失真）第二次爆发
- **修复**：① RepoLayout 显式声明 Logger（注释注明勿依赖 Lombok 的原因）② `mvn clean compile` 全量重编（又抓出 1 处真错误：OperationLogService 的 `param(UUID,int)` 重载不存在——它之前同样被增量跳过没真编过）③ 重启后 7 端点全 200

### 验证
- mvn **clean** compile BUILD SUCCESS（全量 javac，权威）· 7 端点全 200（读路径复活）
- tsc 零错误 · vitest 123/123

### 经验（本日第 5 条铁律）
1. **后端改动交付 = mvn clean compile + 重启 + 最小冒烟**（增量编译的 SUCCESS 不可信，IDE 产物不可信）
2. **计数/按钮/实际行为必须三位一致**：UI 显示的数量 ≠ 实际生效的数量，就是静默丢数据的温床
3. **批量操作不可吞掉低质量数据**：批量确认只应作用于高置信项

---

## 十六、登录页文案清理 + 多文件上传扎实化（傍晚·用户需求）

### 1. 登录页文案去除
删 LoginPage「请输入账号密码登录（真实 Alfresco 认证）」整行及孤儿图标 import。

### 2. 多文件上传扎实化（VoucherUploadModal 六连改）
弹窗本已支持多选/拖拽多文件，但逻辑存在多处不扎实的洞，本轮逐一加固：

| # | 洞 | 修法 |
|---|----|------|
| 1 | **快速连续添加竞态**：handleFilesAdded 用 `setTimeout + files 状态快照` 拼接，快照过期会把先添加的批件卡在"识别中"永不完成 | 改为只处理本次新增 items（按 id 集合标记 processing），零状态快照依赖；识别进行中也可继续添加（解除 Dropzone disabled 耦合） |
| 2 | **OCR 无并发上限**：N 文件=N 个并行 docker exec tesseract，引擎可被压垮 | 客户端并发上限 3（worker 队列模式） |
| 3 | **入池前无文件守卫**：>50MB 撞到服务端 multipart 上限才报错 | 添加时预检：格式白名单/空文件/>50MB，忽略并 toast 明细 |
| 4 | **上传循环一错全停 + 重试重复入池**：首件失败 abort 整批；已入池件留在列表，重试会重复建件 | 逐件独立成败：成功即移出列表、失败标红保留可重试、不中断后续件；全部清空才进成功页自动关闭 |
| 5 | **入池口径分散**（第十五章的按钮计数 bug 根治） | 抽 `uploadEligibility.ts` 纯函数唯一事实源（isPoolEligible/isPoolBlocked/partitionForPool），按钮计数/警示条/上传循环共用，口径永不漂移 |
| 6 | 失败件无现场信息 | 状态徽章 title 挂 errorMsg |

### 3. 测试（含前序问题回归）
- **vitest 130/130**（+7：uploadEligibility 门槛/边界/三桶划分/丢件回归——67 置信度记账凭证必进 blocked 且 eligible=1）
- tsc 零错误
- **多文件服务端 E2E**：3 文件连传（凭证 PNG/发票 PNG/PDF）→ 池内 2→4 件逐件核对 voucherNo 齐全 → 全部清理 204
- 前序回归：fonds 三码 200、/audit/logs 200、OCR 三路（图/发票/PDF 文本层）早已验证
- 插曲：批量测试脚本双引号内双反斜杠路径被 Git Bash 吃掉导致空响应，改字面量单引号后正常——测试线束问题非产品问题（与中文参数 GBK 坑同类，curl 传参一律单引号字面量）

### 4. 当日累计（收官状态）
- 后端：mvn clean compile 全量绿 · 7 端点全 200 · OCR docker exec 链路生产可用
- 前端：tsc 零错误 · vitest 130/130
- 今日 6 起报障全部闭环：上传 404（半成品类启动）→ 全宗不存在（模型丢失+显示名兜底）→ 识别抛硬币（接真 OCR）→ 丢件（门槛静默+ECJ 残类）→ 审计日志 SQL 错位 → 多文件扎实化
- **铁律沉淀**：后端改动交付 = `mvn clean compile`（clean 不可省）+ 重启 + 最小冒烟；UI 计数/按钮/实际行为三位一致；批量操作不吞低质量数据

---

## 十七、「上传 3 件显示 4 件」排查（傍晚·用户报障 #5）

### 结论：不是幻影 bug，是旧测试遗留件
- **服务端建件日志**：本轮上传窗口（16:40:47-48）恰好 3 条「建件成功」，与 3 个文件一一对应，无重复创建
- **池内 4 件构成**：3 件新上传（表格_20260729.pdf=记账凭证、24112000000084327135.pdf=原始凭证、20240629 (2).pdf=原始凭证）+ 1 件**旧遗留**（20240629.pdf，createdAt 06:44 UTC=14:44 本地——正是「丢件」事故中入池成功的那张原始凭证）
- 用户视角：组卷工作台待组卷池**不按年份过滤**（VolumeWorkspacePage:890 仅排除已入卷件），所以 2024 年的旧件与 2026 年新件同列显示 → "3+1=4"
- 用户本轮恰好**重传了同一个 20240629.pdf**（字节数 126002 与旧件完全一致），服务端按同名冲突自动改名 " (2)" 后缀——所以池里出现看似重复的同名件

### 处理
1. **删除旧遗留件**（DELETE 204；新旧两件字节数一致，内容无损）→ 池内恢复恰好 3 件
2. **重复入池防线**（VoucherUploadModal handleFilesAdded 增强）：
   - 列表内重复（同名同大小）→ 跳过并提示「列表中已存在」
   - 与收集池已有件同名 → toast 提醒「入池将生成副本」（不阻断，业务上允许但需知情）

### 验证
- 清理后池内 totalItems=3，逐件名称核对一致
- tsc 零错误 · vitest 130/130

### 经验
1. **报障先核服务端事实**：建件日志条数 vs 用户预期，一次比对即分辨「真重复创建」还是「旧数据残留」
2. **同名冲突自动改名策略要可见**：服务端静默加 " (2)" 后缀，用户容易误读为系统多出一件——前端同名提醒补上这一课

---

## 十八、「删除后又复活」假删除修复（傍晚·用户报障 #6，第十七章的幕后真凶）

### 现象与真相
用户：删过那张旧件，当时列表空了，上传后又冒出来——正是第十七章"3+1=4"里第 4 件的来源。

**根因：删除按钮全是假删除**。VolumeWorkspacePage.handleDeleteRecord/handleBatchDelete、useAppHandlers.handleDeleteRecord 三处都只 `setRecords(filter)` 删前端状态，**从未调任何后端删除接口**（recordService/RecordController 本就无 delete）；记录仍在 Alfresco 收集池，任何一次 loadRecords（上传完成后必触发/切全宗/刷新）即复活。

### 修复（端到端）
1. **后端新端点 `DELETE /records/{nodeId}`**：RecordService.delete 带守卫——非记录节点 400 NOT_RECORD、不存在 400 NOT_FOUND、**非「仅件数据」状态 409**（已组卷须先拆件，防破坏卷内引用）；Alfresco permanent 删除（与前端"永久删除"文案一致）
2. **recordService.deleteRecord(nodeId)**
3. **三处假删除改真**：
   - VolumeWorkspacePage 单个删除：await API → 成功才更新 store，失败 toast 原文
   - VolumeWorkspacePage 批量删除：逐件独立成败，失败件保留并汇总提示（与上传循环同套扎实语义）
   - useAppHandlers（ArchiveTable/时间视图/项目视图共用）：confirm 后调真 API，失败不删本地

### 验证（全过）
- **E2E 闭环**：建件→池 3→4 → DELETE 204 → 池 4→3（服务端实物消失，非 UI 消失）
- 守卫：删根目录 → 400 NOT_RECORD；删不存在 id → 400 NOT_FOUND
- mvn clean compile BUILD SUCCESS · tsc 零错误 · vitest 130/130

### 经验（本日第 6 条）
**「当时生效、刷新复活」类 bug 一律先查是否只动了前端状态**——真假操作的判别法：看代码里有没有对应的 API 调用（grep 调用数），与今日 borrowStore 假注释事件同法。
