# 工作记录 · 2026-08-16 采集链路统一化重构（连接配置收敛 + 核对审核合并 + 集成接口采集 v2 + 元数据页重设计）

## 一、任务来源

用户四项诉求：
1. **抓取收集中台还有连接配置** → 所有抓取相关配置统一到系统管理；中台只做纯抓取 + 去向选择（直接入库 or 档案整理某工作台）。
2. **核对工作台与审核工作台疑似重复** → 重新审核逻辑与链路。
3. **集成接口采集是假页面** → 设计统一推送接口（任何财务系统推 凭证/账簿/报表/其他 四类），页面清晰展示推送数据，支持四性检测、详细日志、去向选择（组卷工作台 vs 自动组卷），并在系统配置下设计字段映射低代码页面。
4. **元数据配置页太乱** → 一屏显示数据太多、逻辑不清晰，用页签或更好方式重设计。

用户拍板的三个决策（AskUserQuestion）：
- 核对/审核 → **合并为一个核对工作台**
- 演示数据 → **模拟推送走真实管道**
- 连接配置 → **合并为一个「连接配置」菜单（三 Tab）**

## 二、核心设计决策

### 2.1 统一去向模型（抓取/推送共用）

| destination | 语义 | 实现 |
|---|---|---|
| `auto-archive` | 直接入库·自动组卷 | 建件→四性(可选)→按类别+期限分组自动建卷→确认取号 |
| `to-volume` | 送组卷工作台 | 建件入收集池（仅件数据），人工组卷 |
| `to-check` | 送核对工作台 | 建件入池 + ams_collect_item 台账 check_status=pending |
| `to-review` | 送审核 | 建件入池 + recordStatus=待审核 + ams_review_log(enter) |

优先级：抓取/推送报文 destination > 接入应用/数据源 default_destination > 默认 to-volume。
自动组卷按「类别+保管期限」分组建卷（不同保管期限严禁混装：年报永久 vs 中期报告10年分开建卷）。

### 2.2 核对/审核合并结论

两页确实重复——都是组卷前准入关口（核对=数据质量视角，审核=准入审批视角），且数据源不一致（本地 store vs 后端 /review），链路理解成本高。
**合并为一个核对工作台**，页内三 Tab：
- 待核对：凭证连续性/补附件（原有）+ 收集池待核对块（ams_collect_item to-check 数据，操作：通过·送组卷 / 通过·送审核）
- 待审核：原审核工作台全部职能（ReviewPanel mode=pending）
- 已处理：审核通过/驳回历史（新增后端 GET /review/processed）
「审核工作台」菜单删除。

### 2.3 统一推送契约 v2（四类）

依据79号令第六条四大类。批次级：period / category / destination / runFourChecks / items[]。
条目公共字段 + 四类特有块：voucher{voucherNo,voucherWord,voucherCategory,entries[],attachedBillCount}、
ledger{ledgerType,subjectCode,subjectName}、report{reportName,reportPeriod}、other{materialType,materialNo}。
保管期限缺省自动带出：凭证/账簿30年、年报永久、中期报告10年、其他10年（三合一表规则）。
files[] 支持 OFD/PDF/A base64；externalId+sourceSystem 幂等不变。

### 2.4 模拟推送（演示策略）

后端 POST /open/v1/simulate：内置 demo-simulator 应用（自动创建），按类别生成仿真样例
（凭证含分录、账簿四种、报表带期间、其他资料），走真实 pushBatch 管道——
批次/四性/日志/去向全部真实可演示，以后接真系统零改动。

## 三、改动清单

### 3.1 后端（ams-server）

| 文件 | 内容 |
|---|---|
| `db/migration/V5__integration_v2.sql` | ams_open_push 加 period/category/destination 列；ams_open_push_item 加 category；ams_open_app 加 default_destination；新表 ams_push_log（全链路日志）、ams_collect_item（收集台账） |
| `openapi/OpenPushService`（重写） | 四类契约解析、去向路由、批量四性检测、批次后续操作（four-checks/to-review/auto-group）、simulate 模拟推送、日志落库 |
| `openapi/OpenApiController`（重写） | 新增 12 个管理端端点（批次操作/日志/模拟/收集台账/字段映射CRUD+试映射） |
| `openapi/PushLogService`（新） | ams_push_log 写入与查询 |
| `openapi/CollectItemService`（新） | 收集台账登记、待核对列表、核对通过流转（送组卷/送审核）、enterReviewLibrary |
| `openapi/FieldMapService`（新） | 字段映射低代码：ams_config 存取（fieldmap.<sourceSystem>）、点路径取值、7种转换规则、试映射 |
| `review/ReviewService/Controller` | 新增 processedList / GET /review/processed（已处理审核记录） |
| `yonyou/YonyouSyncService` | syncNow 新增 destination 重载（auto-archive/to-volume/to-check/to-review）、收集台账登记、push_log 抓取日志；ScheduleConfig 加 destination 字段；scheduleConfig() 优先读连接配置（datasource.config yonyou 源 scheduleEnabled/scheduleCron/defaultDestination），回退旧 yonyou.schedule |
| `yonyou/YonyouController` | /yonyou/sync 接收 destination 参数；PUT /schedule 透传 destination |
| `yonyou/YonyouScheduler` | 定时抓取去向跟随连接配置的 defaultDestination |

### 3.2 前端（financeWeb）

| 文件 | 内容 |
|---|---|
| `services/openPushService.ts`（重写） | v2 全量类型 + API（批次操作/日志/模拟/台账/字段映射），导出 CATEGORY_LABELS/DESTINATION_LABELS |
| `services/yonyouService.ts` | sync 加 destination 参数 |
| `services/reviewService.ts` | 新增 processed API |
| `pages/system/ConnectionConfigPage.tsx`（新） | 系统管理→连接配置：三 Tab（数据源连接/推送接入应用/接口字段映射） |
| `pages/system/FieldMapPanel.tsx`（新） | 字段映射低代码：来源系统列表、规则编辑（类别/标准字段注册表/来源路径/7转换规则/默认值）、样例JSON试映射 |
| `pages/system/DatasourceConfigPage.tsx` | 每源新增抓取计划（启用/cron）+ 默认去向配置与卡片展示 |
| `pages/system/OpenAppManagePage.tsx` | 应用默认去向（签发时选择 + 列表行内切换） |
| `pages/archive-rcv/ApiReceivePage.tsx`（重写） | 抓取收集中台纯执行化：①选数据源（只读）②选期间 ③去向四选一 →立即抓取；删除连接状态卡/调度配置/进审核库开关，配置入口跳连接配置 |
| `pages/archive-rcv/OpenApiReceivePage.tsx`（重写） | 三 Tab：推送监控（统计卡/批次表/四性+送审+自动组卷操作/四类明细）·接口标准（流程/端点/四类契约表/示例报文/错误码/期限速查）·推送日志（级别筛选）+模拟推送弹窗 |
| `pages/archive-arrange/VoucherManagerPage.tsx` | 合并审核：主 Tab 待核对/待审核/已处理；待核对加收集池待核对块（通过·送组卷/送审核） |
| `components/archive-arrange/ReviewPanel.tsx`（新） | 审核面板（pending/processed 双模式），自原审核工作台抽取 |
| `pages/archive-arrange/ReviewWorkbenchPage.tsx` | **删除**（职能并入核对工作台） |
| `pages/archive-config/AccountingMetadataPage.tsx`（重写） | 左右主从：左侧导航（概览+12分组，项数/必选数统计）+全局搜索+必选性筛选；右侧一次一组表格 |
| `pages/archive-config/metadataCatalog.ts`（新） | 104项元数据定义无损抽取（sed 提取，未手抄） |
| `pages/archive-config/RetentionConfigPage.tsx`（重写） | 档案三合一表配置左右主从化：左侧导航（概览+KP/KB/FB/QT 四类+排除范围+期限规则）+全局搜索（编码/名称/说明/期限跨类检索）；单类别视图子项默认展开；数据定义 sed 无损抽取未手抄 |
| `pages/archive-config/VolumeGroupingConfigPage.tsx`（重写） | 组卷盒号配置左右主从化：左侧 8 分区导航（概览/合规基础层/载体模式/按类别独立配置/全局分组维度/盒号规则/装盒校验/管理规则）+右侧单分区；全部 store 交互原样保留；顺带修复原「盒号管理规则」动态 Tailwind 类名（bg-${color}-50 不被 JIT 编译）导致配色失效的问题 |
| 菜单/路由/权限 | menuConfig 删 review-workbench/sys-datasource/sys-open-app 增 sys-connection；appStore MenuId；ContentArea 路由；types/user.ts 角色矩阵（archive_director 增加 voucher-manager/volume-workspace） |

## 四、验证结果

| 检查项 | 结果 |
|---|---|
| ams-server `mvn -DskipTests package`（JDK21） | ✅ 打包成功 |
| 前端 `npx tsc --noEmit` | ✅ 零错误 |
| 前端 `npx vitest run` | ✅ 139/139 通过 |
| 前端 `npm run build` | ✅ 构建成功 |

⚠ 环境注意：本机 PATH 的 java 是 JDK8（D:\jdk-8），编译 ams-server 必须用
`JAVA_HOME=C:\Users\jyimp\.trae-cn\extensions\redhat.java-1.55.0-win32-x64\jre\21.0.11-win32-x86_64`（redhat.java 扩展自带 JDK21；D:\BaiduNetdiskDownload\openjdk-27 是空目录，之前的 Java 27-ea 已不可用）。

## 五、链路现状（改造后）

```
系统管理→连接配置（三Tab）
  ├─ 数据源连接：连接参数 + 抓取计划(cron) + 默认去向
  ├─ 推送接入应用：AppKey签发 + 默认去向 + 批次历史
  └─ 接口字段映射：低代码（来源字段→标准字段+转换规则+试映射）

档案收集
  ├─ 抓取收集中台：选源→选期间→去向四选一→立即抓取 + 批次日志（纯执行）
  └─ 集成接口采集：推送监控（四性/送审/自动组卷）· 接口标准（四类契约）· 推送日志 · 模拟推送

档案整理
  ├─ 核对工作台：待核对（连续性+附件+收集池待核对）→ 待审核（通过/驳回）→ 已处理
  └─ 组卷工作台（不变）

后端统一契约：任何财务系统 POST /open/v1/archives/batch（Bearer）
  { period, category, destination, runFourChecks, items[公共字段+四类特有块+files[]] }
  → 幂等(externalId) → 建件 → 四性(可选) → 去向路由（自动组卷按类别+期限分组建卷）
```

## 六、同日追加：配置页可读性重设计（档案三合一表 + 组卷盒号）

用户第二轮要求：三合一表配置与组卷盒号配置同为"长龙式"，重设计提升可读性/可操作性，确保功能正确。

### 6.1 档案三合一表配置（RetentionConfigPage，纯展示页）

原状：法规横幅+分类架构+4个折叠类别卡（递归明细行）+排除范围+期限计算规则，一屏滚不到头。
重设计为左右主从：
- 左侧导航：概览·法定依据与架构 / KP 会计凭证类 / KB 会计账簿类 / FB 财务会计报告类 / QT 其他会计资料类 / 排除范围 / 保管期限计算规则，各项带数量徽标
- 全局搜索：跨四类递归匹配（编码/名称/说明/期限），结果附"所属类别"标注
- 单类别视图：期限分布统计 + 明细表，**子项默认展开**（原版子项折叠隐藏，可读性差）
- 概览页新增四类别速览卡片入口
- 30 项明细定义（KP-01-01~QT-07）与 DetailRow 组件用 sed 无损抽取，数据零改动

### 6.2 组卷盒号配置（VolumeGroupingConfigPage，真实配置页）

原状：7 个 CollapsibleBlock 折叠块纵向堆叠，其中 4 块含真实配置交互（volumeGroupingStore）。
重设计为左右主从（8 分区）：
- 概览·依据与流程（法定依据 + 组卷→装盒→生成盒号→锁定 流程条）
- 组卷规则组：合规基础层（固化只读）/ 载体模式（carrierMode+itemsPerBox）/ **按档案类别独立配置**（perTypeRules：分组周期/每卷上限/子类型分离，驱动智能组卷）/ 全局分组维度（groupBy* 四维）
- 盒号规则组：盒号规则自定义（管理模式/编码结构/流水位数/预览）
- 流程与规则组：装盒流程与三重校验 / 盒号管理规则
- **所有 store 绑定交互原样保留**（setConfig/setPerTypeRule/resetConfig/保存/恢复默认），仅布局变化

### 6.3 附带修复

- VolumeGroupingConfigPage 原「盒号管理规则」用模板字符串拼 Tailwind 类名（`bg-${item.color}-50` 等），Tailwind JIT 不生成动态类名→配色实际失效；重写为显式类名，配色恢复。

### 6.4 验证

tsc ✅ / vitest 139 ✅ / build ✅（配置页重写后全量复验通过）。

## 七、遗留事项

- 调度配置读取已双轨对齐：scheduleConfig() 优先读连接配置（scheduleEnabled/scheduleCron/defaultDestination），无则回退旧 yonyou.schedule；前端旧的 PUT /yonyou/schedule 接口已无页面调用（保留兼容）。
- 集成接口采集页「四性检测」列展示依赖 RecordDetailPanel/check 属性，批次级四性结果目前落 ams_inspection_report + push_log，未在批次明细表加四性状态列（后续可加）。
- 字段映射目前仅推送 format=raw 时生效；抓取侧（用友）已有固定 YonyouTransformer，未接映射引擎（用友字段稳定，暂无必要）。
- ams_open_app.app_secret 明文落库问题依旧（历史遗留，见 2026-08-09 记录）。
- 模拟推送生成的版式文件是最小空白 PDF（演示用），非真实版式内容。
- V5 迁移需真实数据库执行（Flyway 下次启动自动执行）。

**Why:** 用户要求采集链路逻辑清晰化：配置与执行分离、关口合并、推送接口标准化、页面可读性。
**How to apply:** 后续接新财务系统：连接配置建源→字段映射配规则→推送走统一契约；改动收集链路时先看向去模型（四去向）与收集台账（ams_collect_item）。
