# 工作记录 2026-08-25

## 总览：当天三项完成 + 部署闭环

| # | 工作 | 状态 |
|---|------|------|
| ① | 原始凭证元数据大修（方案A 载体统一到 record） | ✅ 代码 + 部署 + 冒烟全过 |
| ② | 快速检测页 / 元数据录入弹窗 / 操作日志页（菜单+角色矩阵联动） | ✅ 完成 |
| ③ | 全站 UI 清理（删除 SectionTitle desc 中的「需求第X节」引用，~30 页） | ✅ 完成 |

---

## ① 原始凭证元数据大修

**背景结论**：富元数据原挂遗留实体 `finance:sourceDocument`（无写入入口、死代码），主通道把原始凭证存成 `finance:record`（`voucherCategory='原始凭证'`）。规范缺口：原始凭证级未引用 DA/T 95-2022（附录A自制/附录B外来）。硬伤：数电票 checkCode 原被设为必填——按税务总局 2024 年第 11 号，数电票无代码/校验码、仅 20 位号码。

**实施内容（Phase0-4 全做，方案A）**：

1. **定义层** `financeWeb/src/types/sourceDocument.ts`：
   - `FIELD_SETS`：12 个可复用字段集（发票/银行/财政票据/税收缴款/海关/客运/存货/薪酬/资产/费用报销/结算往来/协议特殊），每族带法规依据。
   - 类型树 60 → **113 叶子**（全部既有 code 不改名，存量节点安全）；节点新增 `fieldSetRefs/excludeFields/fieldOverrides/standardBasis`。
   - 数电票 `excludeFields:['invoiceCode','checkCode','machineNo']`；`getExtFieldDefs()` 内部解析=字段集展开→追加→剔除→overrides。
   - 「96 种」文案改 `countLeafTypes()` 动态；公共字段口径统一为「9 类展开 16 字段」。
2. **模型 v2.7** `my-aio-project/.../finance-model.xml`（:ro 挂载权威文件）：`finance:record` 新增 9 属性，**srcDoc 前缀**（TypeCode/TypeName/No/CounterpartyName/CounterpartyTaxId/Summary/AmountUpper/BusinessCategory/ExtFields）。原因：全模型属性名唯一约束，`docTypeCode` 等已被遗留 `finance:sourceDocument` 占用。公共项复用件级属性（amount/voucherDate/preparer/auditor），单据号可落 voucherNo。
3. **后端** `RecordService`：`SourceDocMeta` record、`CreateCmd` 加分量（2 兼容构造器）、`create()` 写 srcDoc*、`toView()` 读并让 counterpartyName/documentNo/description 回落 srcDoc* 值。`RecordController.upload()` 解析 multipart。
4. **前端**：`ArchiveRecord/RecordDto/dtoToRecord` 增 srcDoc 字段；`RecordDetailPanel` isSourceDoc 分支解析 `srcDocExtFields`；`docClassifier.classifySourceDocType()`（有序规则→113 类 code）+ 分类关键词补强；`VoucherUploadModal` 上传携带 docTypeCode/docTypeName/documentNo。
5. **检索页改接 record 口径**：新增 `utils/recordToSourceDoc.ts`，`sourceDocumentStore.loadSourceDocs` 改为 `fetchAllRecords(scope=all)` → `recordsToSourceDocs`（parentVoucherNo 反查、attachmentSequence 排序、extFields 解析），不再读遗留 /source-docs；`RecordDetailPanel` 移除 linkedSourceRecords；`volumeStore` 池内原始凭证件防重复计数。

### 部署与验证（本次完成）

- Alfresco（docker-alfresco-1）重启加载模型 v2.7：容器内 `grep srcDoc finance-model.xml`=10、`srcDocBusinessCategory` 约束生效（用非法值 422 反向证实）。
- ams-server：旧进程停 → `JAVA_HOME=<trae JDK21> mvn package`（22:47 jar 已含全部新增代码，SourceDocMeta.class 在 BOOT-INF）→ `AMS_DB_URL=jdbc:postgresql://localhost:5432/alfresco?currentSchema=ams` 重启（PID 20804，7.2s 启动，DataSeeder 幂等完成，模型对账 upserted=16，0 错误）。
- **冒烟**（`smoke-2026-08-25.py`，11 断言全过）：
  1. 登录 admin；
  2. 上传原始凭证（vat-e-special-invoice + documentNo + extFields JSON）→ 200 回显全部 srcDoc*；
  3. scope=all 检索回验字段完整；
  4. `PUT /records/{id}/metadata` 元数据补录生效；
  5. `/inspection/reports` 列表 200（7 rows）；
  6. `/audit/logs` 操作留痕（target=voucherNo 口径）；
  7. `run-volume` 真跑 Z001 第一卷 → 四性全 pass、reportId 生成、itemCount=2；
  8. 清理：DELETE → 回收站 204。

### ⚠ 踩坑记录

- `finance:srcDocBusinessCategory` 带 **LIS 约束 `finance:businessCategoryList`**：allowed = **采购/销售/费用/资产/薪酬/存货/资金/结算/特殊（9 值）**。传「采购支出」→ Alfresco 422「Invalid property value」。前端 BusinessCategory union 同 9 值 ✓。
- Windows curl 命令行中文 -F 参数被转 GBK → 后端校验失败；**涉中文字段的 multipart 请求改用 Python（UTF-8 文件 + urllib 手工 multipart）**。
- `/inspection/reports` 返回**数组**；`/audit/logs` 返回 `{items,total,skip,limit}`。

---

## ② 三个新功能页面

### 快速检测页（档案整理 → 快速检测，menu=quick-check，ICON=Zap）
`financeWeb/src/pages/archive-arrange/QuickCheckPage.tsx`（新）：
- 四性检测报告明细展示：移交自动检测 + 手动检测 + 历史环节报告；不合格项逐条列出，可展开明细、跳转查看；
- 手动卷级四性检测（选案卷立即执行）；
- 后端 `PermissionService` 新增功能码 **quick-check**（inspection run-volume/review 鉴权含它）；`ROLE_MENU_MATRIX`：manager/archive_director 已加。

### 元数据录入弹窗（组卷工作台）
`financeWeb/src/pages/archive-arrange/MetadataEntryModal.tsx`（新，VolumeWorkspacePage:3084 集成）：
- 件级元数据补录 → `PUT /records/{nodeId}/metadata`（后端白名单字段；仅收集池件/草稿卷内件可编辑）。

### 操作日志页（系统管理 → 操作日志，menu=sys-oplog，ICON=History）
`financeWeb/src/pages/system/OperationLogPage.tsx`（新）：
- 数据源 `/audit/logs`（ams_operation_log 哈希链防篡改，仅追加）；筛选操作人账号/操作类型/日期区间；分页；IP 展示；
- 权限：管理员 / 安全审计员（后端校验）；`ROLE_MENU_MATRIX` security_auditor 加 sys-oplog（管理员 '*' 自动含）。

### 菜单结构调整（同批）
- 移除「目录配置」（directory-config）、「档案三合一表配置」（retention-config）→ 并入档案管理配置；`archive-manage-config` ICON 改 ClipboardList；
- 删除 `DirectoryConfigPage.tsx`、`manage/PrincipleTab.tsx`；ContentArea/PageRouter 联动移除旧 case。

---

## ③ 全局 UI 清理

全站 ~30 页 `SectionTitle` desc 删除「需求第X节」客户需求文档引用（水印/借阅/检测/接收/报告/流程/仓库/仓储等配置与业务页）——内部需求文档引用不应出现在产品 UI 文案。

---

## 质量闸门

- `npx tsc --noEmit`：**0 错误**
- `npx vitest run`：**174/174 通过**（14 文件）
- `npm run build`（vite）：**成功**（12.9s；仅存量 chunk>500kB 警告）
- 后端 `mvn package`（trae JDK21）：成功；`SourceDocMeta.class` 在 jar 内

---

## 当前运行时状态

- docker-alfresco-1（模型 v2.7 已加载）/ docker-postgres-1（5432）/ tesseract：up
- ams-server：PID 20804，`AMS_DB_URL=jdbc:postgresql://localhost:5432/alfresco?currentSchema=ams`，新 jar
- 前端：dev server 未起（由用户启动）；dist 已构建

## 遗留（不影响主流程/下轮候选）

- `finance:sourceDocument` 全链路休眠（无代码创建），`sourceDocumentService.ts`/`SourceDocService.java`/`uploadSourceDoc` 为死代码，后续可清理；
- 存量原始凭证件（改造前上传）无 docTypeCode → 检索页显示「未分类」；新上传经 docClassifier 自动带类型；
- 全站工作树未提交（约 72 文件 / +2110 -2218），部署内容已上线，建议近期提交；
- `四性检测/` 目录为参考文档（DA/T 70 标准、官方方案 md+pdf，2026-08-24 引入）。

## 补续：用户 9 项需求核对表（21:52 提出；22:55 上一会话因 API 配额中断，本次接续验证闭环）

| # | 需求 | 实现位置 | 验证 |
|---|------|---------|------|
| 1 | 组卷工作台元数据录入界面 | `MetadataEntryModal`（卷级 6 字段+件级 17 字段）·卷行工具栏入口·后端 `PUT /records/{id}/metadata`+`PUT /volumes/{id}` | ✅ API 级（件级补录+卷级改题名均落库生效） |
| 2 | 档案盒厚度→一盒装多少件 | GroupingConfigTab「纸质盒容量约束」：boxCapacityMode auto/manual、盒厚预设、单件平均厚度、calcItemsPerBox；装盒消费 `min(baseMax, itemsPerBox)`，满盒自动封盒另开 | ✅ 移交归盒走通 |
| 3 | 档案整理→快速检测菜单 | QuickCheckPage（报告明细/跳转/手动检测）+ 功能码 quick-check + 角色矩阵 | ✅ reports/run-volume/yj 报告均可见 |
| 4 | 财务视图列名显示不全 | DataTable `minWidth=各列size之和`→父级横向滚动，表头不截断 | ✅ 代码级（tsc 过） |
| 5 | 打开盒后所属案卷为空 | 后端 by-volume 响应填充 volumeId/volumeCode；前「所属案卷」列=题名·卷号 | ✅ 盒内卷数据正确 |
| 6 | 集成接口采集 tab 竖排 | 横向滚动+min-width+whitespace-nowrap | ✅ 代码级 |
| 7 | 不能有模拟按钮 | 模拟推送入口/弹窗全删；本次清除 STEP_LABELS `simulate` 残留键（无引用，tsc 复验 0 错） | ✅ 模拟 0 残留 |
| 8 | 四性检测在推送至保管库（移交）时 | `VolumeService.transfer` 先 `runVolume(...,"yj")`，未通过 409 `INSPECTION_FAILED` 阻断；yj 口径=移交项∪归档项 | ✅ **双向实测**：.txt 件 3 issues（YJ-3-01 格式合规）阻断→换合法 PDF 件 33 项全过→200 归盒 |
| 9 | 档案管理配置换图标 | menuConfig ICON 改 ClipboardList | ✅ 代码级 |

**完整链路冒烟结论**（smoke-grouping-transfer-2026-08-25.py）：上传 3 件→建卷→卷元数据录入→加件→确认→移交。同卷两次移交：首次 409（3 项格式问题，卷保持 confirmed 未移动）→ 撤确认换 PDF 件后 200（volumeCode=Z001-KU·01·2026-D10-B009-0009，归入 BOX-2026-KP-001）。
**API 知识点**：卷内条目 key=`nodeId`（非 recordId）；已确认卷移件需先 POST /volumes/{id}/unconfirm；yj 报告 detail_json 为 `{type:jsonb, value:"{...}"}` 双层包裹。

## 工具留存

- `smoke-2026-08-25.sh` — 登录/上传/查询快速回显版（curl；仅 ASCII 字段可靠）
- `smoke-2026-08-25.py` — 完整断言冒烟（11 项，UTF-8 multipart；重跑需先改 voucherNo 避免重号）
- `smoke-grouping-transfer-2026-08-25.py` — 组卷→移交全链路冒烟（失败/放行双路径）
