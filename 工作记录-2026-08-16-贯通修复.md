# 工作记录 · 2026-08-16 全链路贯通修复（P0–P4 全量）

> 承接同日《工作记录-2026-08-16-全链路数据贯通审计.md》。用户指令：全部修复、保证质量、做好记录。
> 验证基线：ams-server `mvn -DskipTests package` ✅（17:33 新 jar）；前端 `tsc --noEmit` ✅ / `vitest` 137/137 ✅ / `npm run build` ✅（21s）。
> 运行时冒烟：Docker Desktop 启动中，见末节。

---

## 一、P0 主线断链修复（一个根因修三个断点）

**根因**：`GET /records` 只返回收集池（未组卷件），件一旦组卷即从前端读侧"消失"。

### 后端（RecordService/RecordController）
- `GET /records` 新增 `scope=all`：`listAll()` = 收集池 ∪ 案卷库卷内件 ∪ 盒库卷内件（children 事务读，不依赖 Solr），每条携带 `volumeId/volumeCode/boxId/boxNo` 归属；与 pool 共用同一套 `filterPage` 过滤/排序/分页。默认 `scope=pool` 完全兼容旧行为。
- RecordView 新增 4 个归属字段（默认空串，listAll 路径覆盖真值）。

### 前端
- `recordService`：`RecordDto` + 4 归属字段；新增 `fetchAllRecords()`；`dtoToRecord` 映射归属。
- `archiveStore`：新增 `allRecords/allRecordsLoading/loadAllRecords()`（读侧全量口径），`records`（池口径）保持不变——工作台语义零回归；全宗切换时两者同刷。
- 切换消费方：
  - 凭证检索 VoucherSearchPage（原永远查不到已归档凭证）
  - 事项检索 MatterSearchPage（父件 lookup 修活，原始凭证→所属记账凭证可归档态解析）
  - 关联查询 VolumeItemSearchPage（件↔卷↔盒 定位）
  - 附件检索 SourceDocumentSearchPage（父件信息联动）+ 挂载自给加载
  - 档案打包 ArchivePackagePage（原永远空；改为 allRecords 驱动，数据到达自动重建封装单元）
  - 门户 usePortalData（原「池 ∪ volumeRecords」双源合并 → 统一 scope=all 单源；旧 assemblePortalRecords 保留导出供测试）
  - 借阅车 BorrowCartBar + BorrowCartCheckout（全量件优先解析，池兜底；车内条目解析不出时自愈重拉）
  - 统计 useArchiveStats（件级口径改全量件）

## 二、P0 附带修复

- **补传附件接真**：后端 `POST /source-docs/by-record/{id}` 端点早已存在，前端 service 漏写——补 `uploadSourceDoc()`；核对工作台补传从"伪造 SourceDocument 对象塞 store（刷新即丢）"改为真实 multipart 上传 + 以服务端为准重拉。
- **原始凭证↔父件关联修复（顺手抓到的潜在断链）**：后端 SourceDocService 的 toView 从不出 `parentRecordId`，前端映射恒为 `""`——附件桶/附件检索的父件联动实际是断的。后端在 collectSourceDocs/create 两处补出 parentRecordId，前端 DTO 接入。
- **采集后自动刷新**：抓取成功（ApiReceivePage）、推送批次操作/模拟推送（OpenApiReceivePage）、核对通过（VoucherManagerPage）、审核通过/驳回（ReviewPanel）后统一 `loadRecords() + loadAllRecords()`；组卷工作台 11 处变更点同步刷 allRecords（sed 批量补挂）。

## 三、P1 尾部环节补全（三张 V1 死表启用两张半）

### 3.1 对外移交批次（ams_transfer_batch 启用）
- 后端新增 `transfer` 包：TransferService + TransferController（`/transfers` CRUD + 状态机 pending→prepared→received + 退回/删除仅 pending + 卷明细实时解析 + 每步写操作日志哈希链）。创建校验：案卷须为已入库（transferred）才可对外移交。
- 前端 `transferService.ts`（新）+ **TransferManagePage 整页重写**（删 MOCK_TRANSFERS）：已入库卷勾选 → 发起移交弹窗 → 批次记录（生成清册/签收/退回/删除/清册打印）。
- **ArchiveTransferPage**（原 30 行占位）→ 实现为三栏批次执行看板（待准备/待签收/已移交），与移交管理页同一份台账。
- 语义边界写明：移交归盒（卷→盒库，所内归档）≠ 对外移交（批次台账，卷节点状态不动）。

### 3.2 鉴定销毁（ams_appraisal 启用，环节从无到有）
- 后端新增 `appraisal` 包：
  - `GET /appraisals/due-volumes`：实时扫描盒库已入库卷，按「年度+期限」算期满日（保管期限自会计年度终了后第一年起算：`(year+期限+1)-01-01`；永久不期满）；
  - `POST /appraisals/scan`：到期卷幂等登记为待鉴定任务；
  - `POST /appraisals/{id}/review`：评审（destroy→approved-destroy / retain→retained，意见留痕）；
  - `POST /appraisals/{id}/execute-destroy`：删 Alfresco 卷节点（级联卷内件）+ 盒计数回退 + destroyed_at + 操作日志。
- 前端 **AppraisalManagePage 整页重写接真**（删 MOCK_APPRAISALS）：到期测算表 / 待鉴定评审 / 待销毁执行 / 已办结四区块 + 一键登记扫描；菜单「档案移交」组改名「档案处置」并新增「鉴定销毁」项（MenuId/路由/角色矩阵默认全开）。

### 3.3 实体库房 + 盒写端点
- 后端 BoxService/BoxController 补写端点：`POST /boxes/{id}/seal|unseal|shelve`、`DELETE /boxes/{id}`（空盒守卫）。
- `archiveBoxStore` 重写：sealBox/unsealBox/storeBox/deleteBox 全真 API（原乐观更新假持久化）；顺带删除零消费的 createBox/updateBox/increment/decrementVolumeCount。
- **DigitalWarehousePanel 整页重写**：库房树（/storage/tree）+ 占用统计（/storage/occupancy）+ 新增库房节点 + 盒上架（真）；**移除演示戏剧化模块**：HSM 阶梯迁移 setInterval 假进度、CA 三方会签假销毁、17 个写死仓位——鉴定销毁改为真实入口卡片跳新页。

## 四、P2 口径与闸门

- **统计口径**：useArchiveStats 件级 = allRecords；流程域移交批次数/件数改由 transferred 卷实时推导（原读会话内存 transferLog，刷新即丢）。
- **核对闸门**：组卷工作台待组卷池排除收集台账 check_status=pending 的件 + 琥珀色提示条（「N 件未核对，去核对工作台」跳转）。服务端任何去向先入池的设计不变，闸门在前端准入侧落实。
- **删除假四性检测**：useAppHandlers 的假检测（setTimeout+编造结论）、假自动组卷（编造操作人/IP/日志）、假赋号校验、假修复（编造修复记录）全部删除；ContentArea/AppLayout 未使用 destructure 同步清理。
- **假"一键修复凭证"按钮链拆除**：DrawerPanel 修复按钮 + InteractivePreview「AI 自修复」CTA（1.5s 假进度）移除，换如实提示（联系供应商重导版式文件）。

## 五、P3 配置孤岛收编

| 配置页 | 处置 | 结果 |
|---|---|---|
| 档号规则 | **纠正审计误判**：后端 VolumeService.assignOnConfirm/buildVolumeCode 本就在读 ams_config('archive-code-config')（赋号时机/流水位数/分隔符/类别前缀），写路径早已贯通；页面加「服务端赋号引擎实时消费」徽标明示；清掉组卷工作台未使用 import | 已贯通（核验确认） |
| 四性检测配置 | **真接**：页面保存 → PUT /config/inspection.plan；服务端 InspectionService 重写为方案驱动（必填字段中文标签映射节点属性逐项核查 / 格式白名单 token→mime 映射 / 敏感关键词扫描 / 各性开关）；新增 `POST /inspection/run-batch`（对收集池批量检测）；页面「立即执行检测」死按钮接真并显示真实结果 | 配置→执行闭环 |
| 三合一表 | 核实为法定口径只读展示页（非编辑配置）；服务端推送缺省期限与其口径一致（凭证/账簿30年、年报永久、中期10年、其他10年）；页面加「法定口径·只读标准·已生效」徽标 + 生效口径说明条 | 消除误导 |
| 流程配置 | 持久化本就在（workflow.config）；内置「借阅利用」流程卡与服务端审批路由规则逐字一致；页面加执行口径横幅（当前路由服务端内置执行，设计器为规则登记册+未来引擎底座） | 如实标注 |
| 报告配置 | **整页重写**：5 行写死 mockData + 全死按钮 → 真实 CRUD 登记册落 ams_config(report.config)（新增/启停/删除/检索/弹窗） | 真实落库 |

## 六、P4 死代码与假数据清理

- **删 9 个死页面**：SmartReceivePage + smart-receive 整目录、FourChecksPage、TimeViewPage、VoucherUploadPage、VolumePrintPage、ArchiveScopePage、ArchiveLibPage、FanzongPage（外部引用核实为零，两处仅注释提及）。
- **删 5 个零消费 store/组件**：workflowStore、viewStore、managementModeStore、voucherFilesStore（文件 base64 存 localStorage 的隐患随删除消除）、ModeSwitcher、cleanStore。
- **api.ts 死 mock 清除**：fetchRecords/fetchCategoryTree/fetchFanzongs/fetchFanzongCategories/mockPersonnel/mockUnits/USE_REAL_API 死分支全删；`api.test.ts` 重写为测真实纯函数（personToPersonnel 映射），不再给假数据背书。
- **vite.config 移除 :3001 残留代理**（旧 Express 已退役）；删 App.tsx.bak/App.tsx.fixed。
- **关联查询页假比对表移除**：MOCK_COMPARE_DATA（假扫描识别值+假匹配图标）、假页码、假"签名有效"徽标全部改真实数据/如实标注。
- **审计日志面板去假**：删 5 条编造管理员日志（含假 IP/假哈希/假水印声明）、删 setTimeout 假验链；**后端新增 `GET /audit/verify` 真验链**（逐环重算 SHA-256 哈希链，如实区分 通过/历史不可验/断链）。
- **审计链精度隐患根治**：OperationLogService.append 原用纳秒 ts 计算 hash，PG timestamptz 微秒精度入库四舍五入 → 链永远无法重算验证（假防篡改）。修复：ts 截断到毫秒再算 hash，验链可真实执行；旧历史行如实标注"不可验"。

### 未动/遗留（如实记录）
- archiveStore 的 treeData/selectedNode/fanzongCategories 旧状态：ArchiveTable 仍编译引用，保留为遗留（后续随旧表格下线再清）。
- data.ts / simulationData.ts：仅作测试 fixture 被 __tests__ 引用，保留（测试基建）。
- 会话/开放 token 内存态（12h TTL，重启可凭 Alfresco ticket 重建）：保持现状，多实例化时再入表。
- 全宗/组织/角色的管理面 CRUD 走 Alfresco 直连（架构决策如此），ams 无代理端点——非断链。
- **新菜单可见性**：「鉴定销毁」已入默认角色矩阵；若部署环境 ams_config 已持久化 role-menus-v3，非管理员角色需在「角色管理」手动开启该菜单（admin 恒见全部）。
- 角色矩阵持久化是整存整取（无增量合并），加新菜单的旧部署都遵循同上规则。

## 七、改动文件清单

**后端 ams-server（编译+打包通过）**
| 文件 | 改动 |
|---|---|
| record/RecordService.java | scope=all 全量件（池∪案卷库∪盒库，带归属）、filterPage 共用化、注入 RepoLayout |
| record/RecordController.java | GET /records 加 scope 参数 |
| sourcedoc/SourceDocService.java | toView 补 parentRecordId（两处调用点） |
| transfer/TransferService.java + TransferController.java | **新增**：对外移交批次全真 CRUD+状态机+清册明细+日志 |
| appraisal/AppraisalService.java + AppraisalController.java | **新增**：到期测算/登记/评审/销毁执行 |
| box/BoxService.java + BoxController.java | seal/unseal/shelve/deleteEmpty 写端点 |
| inspection/InspectionService.java | 重写为 inspection.plan 方案驱动 + runBatch 批量检测 |
| inspection/InspectionController.java | +POST /inspection/run-batch |
| oplog/OperationLogService.java | append 毫秒截断（精度修复）+ verifyChain 真验链 |
| oplog/OperationLogController.java | +GET /audit/verify |

**前端 financeWeb（tsc/vitest/build 全过）**
| 文件 | 改动 |
|---|---|
| services/recordService.ts | RecordDto+归属字段、fetchAllRecords、dtoToRecord 映射 |
| services/sourceDocumentService.ts | +uploadSourceDoc、parentRecordId 接入 |
| services/transferService.ts / appraisalService.ts | **新增** |
| services/boxService.ts | +4 写端点封装 |
| services/api.ts | 死 mock 全删（317→约 230 行） |
| stores/archiveStore.ts | +allRecords 口径；全宗切换双刷 |
| stores/archiveBoxStore.ts | 写操作全真化、死方法删除 |
| stores/（删 4 个） | workflowStore/viewStore/managementModeStore/voucherFilesStore 删除 |
| components/（清理） | DigitalWarehousePanel 重写接真；AuditLogsPanel 去假+真验链；InteractivePreview/DrawerPanel 拆假修复链；ModeSwitcher 删 |
| components/layout/ | useAppHandlers 假处理器全删；AppLayout/ContentArea 清理死 destructure + loadAllRecords 基线 |
| pages/查询四页+门户 | 切换 allRecords 口径 |
| pages/archive-disposal/ | ArchiveTransferPage 实现（看板）；ArchivePackagePage 接全量件 |
| pages/archive-utilization/ | TransferManagePage 重写接真；AppraisalManagePage 重写接真 |
| pages/archive-config/ | InspectionConfigPage 落库+真检测；ReportConfigPage 重写落库；RetentionConfigPage/WorkflowConfigPage/ArchiveCodeConfigPage 生效口径明示 |
| pages/archive-arrange/ | VoucherManagerPage 补传接真+核对通过刷新；VolumeWorkspacePage 核对闸门+allRecords 同步刷新 |
| pages/archive-rcv/ | 抓取/推送后自动刷新件域镜像 |
| components/borrow/ + my-borrow/ | 借阅车全量件解析+自愈 |
| hooks/ | usePortalData 单源化；useArchiveStats 口径修正+自给加载 |
| config/menuConfig.ts + stores/appStore.ts + types/user.ts + ContentArea.tsx | 新增「鉴定销毁」菜单/路由/角色矩阵；组改名「档案处置」 |
| __tests__/api.test.ts | 重写为真映射断言（139→137，减少的 2 个是假 mock 断言） |

## 八、验证

### 8.1 编译/静态验证
| 检查 | 结果 |
|---|---|
| ams-server `mvn -DskipTests package`（JDK21） | ✅ 新 jar（77.4MB） |
| 前端 `npx tsc --noEmit` | ✅ 零错误 |
| 前端 `npx vitest run` | ✅ 137/137（12 文件） |
| 前端 `npm run build` | ✅ 20.9s |

### 8.2 运行时冒烟（真实环境全量跑通）
环境：Docker（docker-postgres-1 @5432 + docker-alfresco-1 @8080）→ ams-server @8081（AMS_DB_URL 指向 5432）→ financeWeb @5000。

⚠ 过程中发现并处理：**finance-model 第五次丢失**（容器重建所致，见记忆 finance-model-deployment）——按规程热部署恢复（docker cp 模型+context → restart → finance:volume 200）。另发现 PG 宿主机端口为 **5432** 而非配置默认的 15432（以 AMS_DB_URL 环境变量覆盖启动）。

- `seed/test-records-api.mjs`：✅ 上传/列表/检索/下载字节一致
- `seed/test-volumes-smoke.mjs`：✅ 29/30（唯一失败"卷列表已空"为脚本假定空库，库里已有今日新产生的真实演示卷——环境假设问题，非缺陷）
- **`seed/test-integration-2026-08-16.mjs`（本次新写，34 断言）：✅ 34/34 全过**
  - [A] scope=all：建件→组卷→赋号→归盒全程归属信息正确（volumeId/volumeCode/boxId/boxNo），池口径与全量口径各自正确
  - [B] 移交批次：建批次→清册→签收→重复签收 409 拒绝；非卷节点 400 拒绝
  - [C] 鉴定销毁：到期测算精确（2010+10年命中、2020+30年不误判）、幂等登记、评审→销毁执行→节点级联删除→盒计数回退
  - [D] 盒写端点：封盒/上架/开封/非空盒 409
  - [E] 审计链验真：chainIntact=true、verified>0（精度修复生效）
  - [F] 四性检测：配置驱动单件+批量检测+报告可查
- 冒烟还揪出一个**沉睡真 bug**：InspectionService 的 INSERT 列名与 V1 表结构完全不符（check_*/all_pass/details vs 实际的 real/complete/detail_json），四性落库此前一直在报错——已修（detail_json jsonb + target_node + operator 列对齐）。
- 前端代理链路验证：`GET :5000/api/ams/records?scope=all` 返回已组卷件（档号 Z001-KU·01·2020-D30-B003-0003-0002，服务端赋号）。

### 8.3 当前运行中的服务（已为用户起好）
- Docker 容器：docker-postgres-1、docker-alfresco-1（tesseract 容器不存在，OCR 走降级路径）
- ams-server @8081（后台 java 进程，日志 /tmp/ams-server2.log）
- financeWeb dev @5000（后台，日志 /tmp/financeWeb-dev.log）
- 演示数据：Z001 下有一盒（BOX-2020-KP-001）一卷 2 件已组卷凭证 + 一个已签收移交批次（TJ-*）+ 一条销毁记录，可直接演示主线

## 九、运行时人工走查清单（打开 http://localhost:5000 逐点验证）

1. 档案查询 → 凭证检索：已组卷凭证可见（修复前为空）
2. 凭证检索 → 加入借阅车 → 门户·我的借阅 → 结算提交（条目不丢失）
3. 档案处置 → 档案打包：待打包封装单元非空
4. 档案处置 → 档案移交：三栏看板里有 TJ-* 已签收批次
5. 档案处置 → 鉴定销毁：到期测算表 + 已办结记录
6. 档案保管 → 实体档案库房：真实库房树（可先建节点）+ 盒上架
7. 系统管理 → 安全审计日志：「验链」按钮返回真实验链结果
8. 档案配置 → 四性检测配置：「立即执行检测」对收集池真跑并回报数字

**Why:** 用户要求全链路真贯通、不留假数据。
**How to apply:** 后续任何"已组卷件"读侧需求一律用 scope=all（不要再从池里找）；新配置页先想清楚消费方再建；死表已启用两张（transfer_batch/appraisal），ams_package 仍待档案打包持久化接入（packageStore 目前内存态，属下一轮）。
