# 借阅模块前后端贯通迁移方案

> 版本：v1.1 · 2026-07-29（已执行完成，详见《工作记录-2026-07-29》第九节）
> 背景：全站逻辑贯通检查发现借阅模块前后端脱节（详见《工作记录-2026-07-29》硬伤 #1）。
> 目标：将 borrowStore 从"前端本地状态机 + ams_config 持久化"迁移为"关系型后端 BorrowService 为唯一事实源 + 前端纯 API 读写"，实现真正的前后端贯通。

---

## 一、现状与问题

### 1.1 当前架构（临时态）
```
borrowStore（前端）
  ├─ 状态机：审批路由/智能拆单/履约流转/每日巡检/黑名单 —— 全部在前端 TS 运算（borrowEngine.ts）
  ├─ 持久化：persist → ams_config('borrow-lifecycle')（orders/logs/cart 整体 JSON 存取）
  └─ 单号：前端本地 orders.length+1 生成（刷新后从已持久化数据恢复，但若清缓存会重号）

BorrowService（后端，已建但未用）
  ├─ 4 表：ams_borrow_order / ams_borrow_item / ams_approval_step / ams_fulfillment
  ├─ 端点：/borrow/** 共 11 个（submit/list/get/approve/reject/terminate/checkout/return/availability/blacklist/daily-check）
  ├─ 单号：ams_code_serial（BORROW 作用域，行锁原子取号，绝不重号）
  └─ 日志：6 处动作写 ams_operation_log 哈希链
```

### 1.2 核心问题
| # | 问题 | 影响 |
|---|------|------|
| 1 | 前端状态机与后端逻辑**双份实现**，存在漂移风险 | 改一处忘改另一处 → 行为不一致 |
| 2 | 持久化用 ams_config（KV 存整团 JSON），**非关系型** | 无法按单查询/统计/审计穿透；并发写覆盖风险 |
| 3 | 后端 4 表 + 11 端点 + 操作日志**完全闲置** | 投入浪费；借阅统计/审计无真实数据源 |
| 4 | 单号前端本地生成 | 极端情况（清缓存）重号 |
| 5 | 借阅统计读 borrowStore（前端态） | 换浏览器/多端不一致 |

---

## 二、数据形状对照与缺口

### 2.1 主单 BorrowOrder ↔ ams_borrow_order
| 前端字段 | 后端列 | 缺口 |
|----------|--------|------|
| id | id (uuid) | ✅ 用后端生成 |
| orderNo | order_no | ✅ 用后端 ams_code_serial 生成 |
| applicantId | applicant_id | ✅ |
| **applicantName / applicantEmpNo / applicantDept** | ❌ 表中无 | 🔴 **需 join ams_user_ext 补全** |
| reasonType / reasonDetail | reason_type / reason_detail | ✅ |
| startDate / endDate | start_date / end_date | ✅ |
| status | status | ✅ |
| currentStepIndex | current_step | ✅ |
| createdAt | created_at | ✅ |

### 2.2 明细 BorrowOrderItem ↔ ams_borrow_item
| 前端字段 | 后端列 | 缺口 |
|----------|--------|------|
| id | id | ✅ |
| recordId | record_node_id | ✅ |
| volumeId | volume_node_id | ✅ |
| title | title | ✅ |
| **voucherNo** | ❌ 表中无 | 🔴 **需加列 voucher_no** |
| **archiveType** | ❌ 表中无 | 🔴 **需加列 archive_type** |
| archiveTypeCode | type_code | ✅ |
| mediaType | media_type | ✅ |
| securityLevel | security_level | ✅ |
| stockStatus | stock_status | ✅ |
| electronicPerms[] | perms (text[]) | ✅ 数组直接映射 |
| physicalMode | physical_mode | ✅ |

### 2.3 审批 ApprovalStep ↔ ams_approval_step
| 前端字段 | 后端列 | 缺口 |
|----------|--------|------|
| seq | seq | ✅ |
| role | role | ✅ |
| **roleLabel** | ❌ | 🟡 前端用 ROLE_LABELS[role] 映射（无需后端） |
| **assigneeName** | assignee_id（有 id 无名） | 🟡 前端查用户表映射，或后端 join 补 |
| status / actedBy / actedAt / comment | status / acted_by / acted_at / comment | ✅ |

### 2.4 履约 Fulfillment ↔ ams_fulfillment
| 前端字段 | 后端列 | 缺口 |
|----------|--------|------|
| id / orderId / type / status | id / order_id / type / status | ✅ |
| recordIds[] | record_node_ids (text[]) | ✅ |
| volumeId | volume_node_id | ✅ |
| **volumeTitle** | ❌ 表中无 | 🟡 后端 split 时冗余存入，或前端查 volume 节点 |
| physicalMode | physical_mode | ✅ |
| startDate/endDate/grantedAt/lentAt/returnedAt | 同名 snake_case | ✅ |
| operatorBy | operator_id | ✅ |

### 2.5 缺口汇总（需后端改动）
| 缺口 | 方案 | 改动量 |
|------|------|--------|
| 申请人姓名/工号/部门 | getOrder/listOrders 的 enrichOrder 里 join ams_user_ext | 后端 1 处 |
| 明细 voucherNo/archiveType | Flyway V2 给 ams_borrow_item 加 2 列 + submit 时写入 | 后端 DDL+1 处 |
| 审批 roleLabel/assigneeName | 前端 ROLE_LABELS 映射 + 用户查询（或后端 join） | 前端为主 |
| 履约 volumeTitle | 后端 split 时从 volume 节点取 title 冗余存入（加列）或前端查 | 后端 1 处 |

---

## 三、迁移方案（分 4 步，每步可独立验证）

### 步骤 1：后端补全数据形状（无前端改动，可单独编译验证）
1. **Flyway V2__borrow_enrich.sql**
   ```sql
   ALTER TABLE ams.ams_borrow_item ADD COLUMN voucher_no text;
   ALTER TABLE ams.ams_borrow_item ADD COLUMN archive_type text;
   ALTER TABLE ams.ams_fulfillment ADD COLUMN volume_title text;
   ```
2. **BorrowService.submitOrder**：写入 item 时补 voucher_no / archive_type（从 SubmitOrderCmd.items 传入，前端已有这些字段）。
3. **BorrowService.splitFulfillments**：建履约单时查 volume 节点 title 写入 volume_title。
4. **BorrowService.enrichOrder**：join ams_user_ext 补 applicant_name / applicant_emp_no / applicant_dept；返回结构改为嵌套：
   ```json
   { "id":..., "orderNo":..., "applicantName":..., "items":[...], "approvalRoute":[...], "fulfillments":[...] }
   ```
   （当前 enrichOrder 已返回 items/approvalRoute/fulfillments，仅需补申请人字段与列名驼峰化）

**验收**：`mvn compile` 通过；curl POST /borrow/orders 后 GET /borrow/orders/{id} 返回含申请人姓名/明细凭证号/卷题名的完整嵌套结构。

### 步骤 2：前端数据映射层（borrowService.ts 增强）
1. 在 borrowService.ts 增加 `dtoToBorrowOrder(dto)` / `dtoToBorrowItem` / `dtoToApprovalStep` / `dtoToFulfillment` 映射函数（snake_case → 前端驼峰；roleLabel 用 ROLE_LABELS；electronicPerms/recordIds 数组直传）。
2. `fetchOrders` / `fetchOrder` 返回值经映射转为 `BorrowOrder[]` / `BorrowOrder`。
3. `submitOrder` 的 SubmitOrderCmd 补传 voucherNo/archiveType（前端 items 已有）。

**验收**：tsc 通过；单测映射函数（构造后端 DTO → 断言前端 BorrowOrder 字段齐全）。

### 步骤 3：borrowStore 改纯 API 读写（核心）
**原则**：保持 BorrowState 公开接口签名不变（8 个页面零改动），内部从"本地运算"改为"调 API + 重载"。

| 动作 | 现状 | 迁移后 |
|------|------|--------|
| submitOrder | 本地造单 | `await borrowApi.submitOrder(cmd)` → `loadOrders()` → 返回新单 |
| approveCurrentStep | 本地改状态+拆单 | `await borrowApi.approveOrder(id, comment)` → `loadOrders()` |
| rejectCurrentStep | 本地改状态 | `await borrowApi.rejectOrder(id, comment)` → `loadOrders()` |
| checkoutPhysical | 本地改履约 | `await borrowApi.checkoutFulfillment(id)` → `loadOrders()` |
| returnPhysical | 本地改履约+预约锁定 | `await borrowApi.returnFulfillment(id)` → `loadOrders()` |
| terminateOrder | 本地中止 | `await borrowApi.terminateOrder(id)` → `loadOrders()` |
| cancelOrder | 本地删除 | 调后端（需补 cancel 端点）或 terminate 复用 → `loadOrders()` |
| runDaily | 本地巡检返回计数 | `const r = await borrowApi.runDailyCheck()` → `loadOrders()` → 返回 r 计数 |
| logAction | 本地写 logs | 后端已自动写 ams_operation_log；前端 logs 改从 fetchAuditLogs 拉取 |
| **loadOrders（新增）** | 无 | `fetchOrders()` → 映射 → set orders |

**接口异步化的调用方适配**（仅 2 处用到同步返回值）：
- `BorrowManagePage` L83 `const n = runDaily()` → `const n = await runDaily()`（handler 改 async）
- `BorrowCartCheckout` L168 `submitOrder({...})` → 检查是否用返回值，按需 await

**移除**：persist 中间件、buildSeedLogs、orderSeq/logSeq 本地计数器、borrowEngine 在 store 内的运算调用（borrowEngine.ts 保留供后端单测对照与前端审批链预览）。

**cart 保留本地**：借阅车是纯前端暂态，无需后端。

**验收**：tsc + vitest + build 通过；手工走查"申请→审批→拆单→出库→归还→逾期"全链路，刷新页面数据不丢、单号连续。

### 步骤 4：统计/审计接真实数据源
1. 借阅统计（BorrowStatsPage）改读 `fetchBorrowStats()`（后端 SQL 聚合）而非 borrowStore。
2. 安全审计日志改读 `fetchAuditLogs()`（已在 AuditTrailPage 接通）。
3. useArchiveStats 的 utilization 域改读后端。

**验收**：借阅统计数字与 ams 表一致；多浏览器打开数据一致。

---

## 四、风险与回滚

| 风险 | 缓解 |
|------|------|
| 后端改动无法在沙箱编译验证 | 步骤 1 在可构建环境单独编译验证后再做步骤 2-3；每步小步提交 |
| 异步化破坏页面同步调用 | 仅 2 处用返回值，已定位；其余 fire-and-forget 不受影响 |
| 映射缺字段致 UI 空白 | dtoToBorrowOrder 全字段兜底默认值；单测覆盖映射 |
| 迁移期数据不一致 | ams_config 旧数据保留只读，新单走后端；不删旧持久化直到验证通过 |
| 回滚 | 每步独立 git 提交；步骤 3 出问题可 revert 回 persist 态（功能不退化） |

---

## 五、工作量估计

| 步骤 | 后端 | 前端 | 测试 |
|------|------|------|------|
| 1 数据形状补全 | V2 DDL + 3 处 service | — | curl 验证 |
| 2 映射层 | — | borrowService 4 个映射 | 映射单测 |
| 3 store 改 API | （补 cancel 端点） | borrowStore 重写 10 动作 + 2 页面适配 | 全链路走查 |
| 4 统计/审计 | — | 3 处数据源切换 | 数字核对 |

**总计**：后端约 0.5 天，前端约 1.5 天，测试 0.5 天。建议在有后端构建能力的环境集中完成。

---

## 六、完成后的目标架构
```
borrowStore（前端，纯读写）
  ├─ loadOrders / 各动作 → borrowApi → ams-server
  ├─ 不再含状态机运算（borrowEngine 仅作审批链预览的纯函数）
  └─ cart 本地暂态

BorrowService（后端，唯一事实源）
  ├─ 4 表关系型存储 + ams_code_serial 原子单号
  ├─ 审批路由/拆单/履约/巡检/黑名单 全部后端运算
  └─ ams_operation_log 哈希链审计

借阅统计/审计 → 后端 SQL 聚合（多端一致）
```

