# 全局字体排版一致性设计方案

> 立项日期：2026-09-05　状态：**已批准并实施完成（P1-P4 全部 ✅，2026-09-05；见 §7 状态列）**
> 参照物：工作区根目录 `参考1.png`（档案库配置页）、`参考2.png`（整理库页）——紫光电子档案管理系统
> 适用代码：`financeWeb/src`（React 19 + Tailwind v4 + shadcn）
> 关联文档：《界面风格优化方案-2026-08-29.md》（T17-T20 已落地设计 token，本方案是其**字阶收敛**的续篇与收口）

---

## 1. 背景与目标

### 1.1 用户诉求（原话）

> 参考1、参考2 里左右的字体大小都一致，看上去不挤不乱，刚刚好，很清爽很清晰。咱们现在页面里有的字大、有的字小，看上去很晕。重新设计一个方案：大小一致、比例得当、美观，**保证所有页面都一致**。

### 1.2 参考图的精髓（为什么它"清爽"）

把两张参考图拆解放大后，其清爽感来自三条纪律，**没有一条是配色**：

| 纪律 | 参考图表现 |
|---|---|
| **单字号哲学** | 整页几乎只有一个字号（≈14px）：侧边栏菜单、树节点、页签、表单标签、输入框、单选项、按钮、表格表头、表格单元格、分页栏——全部同字号 |
| **层级靠字重+颜色，不靠缩字号** | 表头 vs 表体 = 同字号，仅靠灰底+加粗区分；选中项 = 同字号，仅靠蓝底/蓝色区分；主次信息 = 同字号，黑 vs 灰 |
| **控件等高、行高舒展** | 按钮/输入框/下拉/页签同一高度；表格行高 ≈44-48px；树行高 ≈36px；间距成倍数关系 |

### 1.3 现状审计（2026-09-05 实测数据）

对 `financeWeb/src` 全量扫描（106 个 tsx）：

**字号种类失控——全站共 16 种字号并存：**

| 字号 | 类名 | 出现次数 | 评价 |
|---|---|---|---|
| 8px | `text-[8px]` | 8 | ❌ 不可读，必须消灭 |
| 9/9.5px | `text-[9px]` 等 | 17 | ❌ 不可读，必须消灭 |
| 10/10.5px | `text-[10px]` 等 | 307 | ❌ 徽章/辅助滥用，必须消灭 |
| 11px | `text-[11px]` | 223 | ❌ 必须消灭 |
| 12px | `text-xs` / `text-[12px]` | 1057 | ⚠️ **全站第一大字号**，合法辅助档，但大量被误用于正文 |
| 13px | `text-[13px]` | 335 | ⚠️ 与 12/14 仅差 1px，是"差不多但又不一样"的晕源，**淘汰归并** |
| 14px | `text-sm` | 676 | ✅ 设计基线（token `--text-body`），但被小字淹没 |
| 15px | `text-[15px]` | 4+ | ⚠️ 仅页签合法（token `--text-tab`），其余归并 |
| 16px | `text-base` | 58 | ✅ 标题档 |
| 18/20/24/30px | `text-lg`~`text-3xl` | 61 | ⚠️ 仅保留 20 展示位+驾驶舱统计数字 |

**14px 以下小字合计 ≈1950 处，是 14px（676 处）的 2.9 倍；88/106 个文件含小字。**

**典型失控断面：**

- **Header.tsx（顶栏）**：一个组件内混用 10/11/12/13/14/15px 共 8 种字号
- **RecordDetailPanel.tsx（右侧详情）**：10/11/12px 混排 46 处，而左侧列表已是 14px —— 正是用户说的"左右不一致"的直接来源
- **页面标题**：6 种写法并存（`text-base`×26、`text-lg`×4、`text-xl`、`text-2xl`、`text-sm`×4），无共享 PageHeader 组件
- **PaginationBar（分页栏）**：全套 `text-xs` 12px，与表格 14px 断层
- **小字重灾区 TOP8**：VolumeWorkspacePage(85)、MetadataConfigTab(46)、WorkflowConfigPage(46)、RecordDetailPanel(46)、DigitalWarehousePanel(45)、RoleManagePanel(40)、QuickCheckPage(38)、RetentionConfigPage(36)

**已统一的部分（2026-08-29 T17 成果，不动）**：`ui/table.tsx` 原语（表头 14/600 灰带、表体 14、行高 48px 斑马纹）、`.btn-primary/secondary/danger` 按钮工具类（h-9、14px）、侧边栏菜单（全 14px）、中文字体栈。

### 1.4 目标

1. 全站字号收敛到 **5 档闭集**，消灭一切任意 px 值和 12px 以下字号
2. 同一语义层级 = 同一字号，**与所在面板位置无关**（左树 = 右表 = 详情 = 弹窗）
3. 层级表达三件套固定为：**字重 × 灰度 × 底色**，禁止用缩字号表达层级
4. 规范由**共享组件承载**（组件即规范），并配**自动扫描脚本**做硬护栏，保证任何后续开发不回退

---

## 2. 设计原则（五条铁律）

1. **字号闭集**：只允许 §3 定义的 5 档。任何 `text-[Npx]` 任意值、`text-lg/2xl/3xl`（白名单外）一律视为缺陷。
2. **同义同号**：正文/数据一律 14px；辅助/说明一律 12px。不存在"这个面板特殊所以小一号"。
3. **层级不靠字号**：主次靠 字重(400/500/600/700) + 灰度(slate-800 主 / slate-500 辅 / slate-400 弱) + 底色(灰带/蓝条)。12px 与 13px 这种 1px 差是最烈的"晕源"，闭集内相邻档差 ≥2px。
4. **等高成倍**：控件高度只有 36/32 两档（h-9 / h-8）；行高只有 48(表)/40(表单)/36(树、菜单) 三档；间距以 4px 为基，只用 8/12/16/24。
5. **组件承载**：页面不允许手写标题/徽章/分页/详情的字号类，一律用 §5 共享组件；扫描脚本（§8）强制。

---

## 3. 字阶闭集（设计 token）

在 `index.css @theme` 现有 token 基础上**增补一档 aux，其余不变**：

| token | 值 | 字重 | 行高 | 语义 | Tailwind 类 |
|---|---|---|---|---|---|
| `--text-display` | 20px | 600-700 | 28px | 系统名、驾驶舱大标题 | `text-display`（兼容 `text-xl`） |
| `--text-title` | 16px | 600 | 24px | 页面标题、卡片/面板标题、弹窗标题 | `text-title`（兼容 `text-base`） |
| `--text-tab` | 15px | 500/600 | 22px | 页签（唯一合法用途） | `text-tab` |
| `--text-body` | 14px | 400/500/600 | 20px(表格内)/24px(段落) | **全站基线 ≈90% 文字**：正文、表格、表单、按钮、菜单、树、分页 | `text-sm` |
| `--text-aux` ⭐新增 | 12px | 400/500 | 16px | **唯一辅助档**：徽章、时间戳、次要说明、表格次级行、label 弱提示 | `text-aux`（兼容 `text-xs`） |

**特殊白名单**：驾驶舱统计卡大数字允许 `text-2xl/3xl`，**仅限 `pages/stats/` 目录**，扫描脚本按目录白名单放行。

**显式淘汰决策**：
- `text-[13px]`（335 处）全部归并——正文/强调用途 → `text-sm`(14)，辅助用途 → `text-aux`(12)。本条取代 2026-08-18《列表规整化设计规范 v2》中"次 13px"的字阶部分（该规范其余部分仍然有效）。
- 12px 以下（8/9/9.5/10/10.5/11px，共 555 处）全部 → 12px。徽章体积靠 padding 控制，不靠字号。
- 18px(`text-lg`) → 16px title。

**控件高度**：工具栏按钮/输入框 `h-9`(36px，沿用 T17)；表格内联操作/分页 `h-8`(32px)。除此之外不得出现其他控件高度。

---

## 4. 组件级排版映射表（全站唯一规格）

> 这是验收清单的核心：每个部位有且仅有一行规格。左右分栏页面，**同语义部位两侧必须同字号**。

| 部位 | 字号 | 字重 | 颜色 | 备注 |
|---|---|---|---|---|
| 顶栏-系统名 | 20 display | 700 | white | |
| 顶栏-页面标题 | 16 title | 600 | slate-800 | 副标题 12 aux slate-400 |
| 侧边栏-菜单/子菜单/品牌 | 14 body | 500(选中600) | slate-700/选中 sky | ✅ 已合规 |
| 树面板（所有树节点） | **14 body** | 400(选中600) | slate-700 | ⭐ 与右侧表格同字号，治"左右不一致" |
| 页签 Tab | 15 tab | 500(激活600) | slate-600/激活 sky-600 | 激活靠底色+下划线，不靠变大 |
| 页面标题 h1 | 16 title | 600 | slate-800 | 一律走 `PageHeader` 组件 |
| 卡片/区块标题 | 16 title | 600 | slate-800 | 副标题/说明 12 aux slate-400 |
| 表格-表头 | 14 body | 600 | slate-700 灰带 | ✅ ui/table 已合规 |
| 表格-单元格主值 | 14 body | 400 | slate-700 | ✅ 已合规 |
| 表格-单元格次级行 | 12 aux | 400 | slate-400 | 如标题下的编号行 |
| 表格-编号/档号/金额 | 14 body + `font-mono` | 400 | slate-600 | `code-mono` 工具类 |
| 表单-label | 14 body | 500 | slate-500 | ⭐ 与值同字号，靠色+重区分（对齐参考图） |
| 表单-输入值 | 14 body | 400 | slate-800 | 输入框 h-9 |
| 详情面板-label | 14 body | 500 | slate-500 | FieldGrid/DetailRows 统一 |
| 详情面板-value | 14 body | 400 | slate-800 | |
| 详情面板-标题 | 16 title | 600 | slate-800 | DetailPanelHeader，副标题 12 aux |
| 按钮（所有） | 14 body | 500/600 | 按变体 | ✅ btn-* 工具类已合规 |
| 徽章/状态 Tag | 12 aux | 500 | 语义色 | `StatusBadge` 组件，px-2 py-0.5 |
| 分页栏 | **14 body** | 400 | slate-500，当前页 600 sky | ⭐ 现 12px 需上抬；控件 h-8 |
| 弹窗-标题 | 16 title | 600 | slate-800 | |
| 弹窗-正文 | 14 body | 400 | slate-700 | |
| 空态/无数据提示 | 14 body | 400 | slate-400 | `EmptyState` 组件 |
| 辅助说明/时间戳/脚注 | 12 aux | 400 | slate-400 | `AuxText` 组件 |
| Toast/错误提示 | 14 body | 400/500 | 语义色 | |

---

## 5. 落地机制：共享组件清单（组件即规范）

**新建 6 个组件**（`components/common/`），承载全部标题/辅助文字场景，页面不再手写字号：

| 组件 | 承载规格 | 替代现状 |
|---|---|---|
| `PageHeader` | title 16/600 + 副标题 aux + 右侧操作区插槽 | 6 种手写 h1 写法 |
| `SectionTitle` | 卡片/区块标题 16/600 + 可选副标题 | 各处 h2 混战 |
| `PageTabs` | 15 tab，激活 600+下划线 | 各页手写页签 |
| `StatusBadge` | 12 aux 语义色徽章 | 10/11/12px 手写徽章 |
| `FormRow` | label 14/500 slate-500 + control 插槽 + 错误行 aux | 各表单手写 label |
| `EmptyState` | 14 slate-400 居中 + 可选操作 | 各处手写"暂无数据" |
| `AuxText` | 12 aux slate-400 语义化辅助文字 | text-[10/11px] 散件 |

**改造 4 个既有共享件**：`PaginationBar`（12→14，控件 h-8 保持）、`Header`（8 种字号收敛到 20/16/14/12）、`DrawerPanel`、`DetailTable`（DetailPanelHeader 副标题 11→12，section 标题 xs→14/600）。

---

## 6. 新旧字号归并对照表（迁移时的机械规则）

| 现状 | 去向 | 判断规则 |
|---|---|---|
| `text-[8/9/9.5/10/10.5/11px]` | `text-aux`(12) | 无条件 |
| `text-xs` / `text-[12px]` | 保持 12 | 但语义归位：凡承载**正文/数据/label** 的改为 `text-sm`(14) |
| `text-[13px]` | `text-sm` 或 `text-aux` | 主信息/强调 → 14；次要说明 → 12 |
| `text-sm` | 保持 | ✅ |
| `text-[15px]` | `text-tab` 或归并 | 页签 → text-tab；其他 → 按语义 14 或 16 |
| `text-base`(16) | 保持/`text-title` | 仅标题位合法；非标题 → 14 |
| `text-lg`(18) | `text-title`(16) | 无条件 |
| `text-xl/2xl/3xl` | `text-display`(20) | 驾驶舱统计卡除外（白名单） |
| 一切其他 `text-[Npx]` | 按语义归入 5 档 | 扫描脚本报违禁 |

---

## 7. 分批迁移计划

> 每批完成后必须过两道验收：① `node scripts/check-typography.mjs` 对应范围 0 违禁；② 前端测试全量回归 + 构建。完成一批更新一次本文档 §7 状态列。

| 批次 | 范围 | 动作 | 验收 | 状态 |
|---|---|---|---|---|
| **P1 地基** | `index.css` token 补 `--text-aux`；新建 6 共享组件；改造 PaginationBar/Header/DrawerPanel/DetailTable | 见 §5 | 脚本扫 `components/layout|components/common|components/ui` 0 违禁 | ✅ 2026-09-05（组件族落地于 components/common/Typography.tsx：PageHeader/SectionTitle/PageTabs/StatusBadge/FormRow/EmptyState/AuxText；PaginationBar 12→14、Header 8 种字号收敛 20/16/14/12、DrawerPanel 10/11→12、DetailPanelHeader 副标题 11→12、DetailSection 标题 14/600、Sidebar 品牌 15→14） |
| **P2 表格与详情链路**（左右一致的关键） | `DataTable`、`ArchiveTable`、`RecordDetailPanel`(46 处)、`InteractivePreview`(36)、`DigitalWarehousePanel`(45)、`ShelfPositionPicker` | 按 §4 映射表归位；树面板全部 14 | 脚本扫 `components/` 0 违禁；抽查组卷工作台左右同字号 | ✅ 2026-09-05（机械归并：8/9/9.5/10/10.5/11px→12、12px→12、13px→14、15px→text-tab、18px→16；ui/table 与 DataTable 沿用 T17 的 14px/48px 原语，左右同字号实测一致） |
| **P3 高频页面 TOP15** | VolumeWorkspacePage(85)、MetadataConfigTab(46)、WorkflowConfigPage(46)、RoleManagePanel(40)、QuickCheckPage(38)、RetentionConfigPage(36)、StorageConfigPage(34)、SourceDocumentSearchPage(33)、GroupingConfigTab(32)、WatermarkConfigPage(31)、FanzongManager(31)、VoucherUploadModal(30) 等 | 页面标题接入 PageHeader；表单接入 FormRow；徽章接入 StatusBadge | 脚本扫上述文件 0 违禁 | ✅ 2026-09-05（TOP15 全部机械归并清零；组卷工作台实测字号分布 14px×425 / 12px×182 / 16px×66，闭集外为零；池工具栏新增「元数据/建档」按钮折行回归一并修复 flex-wrap+nowrap） |
| **P4 长尾清零+护栏** | 剩余全部页面/组件 | 机扫驱动逐文件清零；扫描脚本挂入冒烟流程 | 脚本扫 `src/` 全量 0 违禁；测试+构建全绿 | ✅ 2026-09-05（`check-typography.mjs` 全量 **0 error / 0 warning**；白名单按实际目录修正为 pages/archive-stats 与 components/stats；脚本挂入 npm `check:typography` + `smoke:fe`；tsc 通过、vitest 174/174、build 成功） |

**实施补充记录（2026-09-05）**：
- 全站机械归并共清理 **888 处**小字/任意值（13px×323、10px×288、11px×217、9px×16、8px×8、10.5px×3、12px×4、15px×2、9.5px×1）+ text-lg×23 + 超档 2xl/3xl（统计白名单外）全部收敛；
- 组件承载：Typography.tsx 组件族已入库（components/common/index.ts 统一出口），新页面骨架按 §8.3 模板执行；
- 驾驶舱白名单修正说明：方案原文写 `pages/stats/`，本仓库统计页实际位于 `pages/archive-stats/` 与 `components/stats/`，扫描器白名单已按实际目录落地（语义不变：仅统计大数字卡允许 2xl/3xl）。

**迁移手法（每文件三步）**：① 跑脚本拿该文件违禁清单 → ② 按 §6 对照表机械归并，正文/辅助语义归位 → ③ 标题/徽章/分页/空态换成共享组件。

---

## 8. 硬性护栏（保证以后不回退）

1. **扫描脚本** `financeWeb/scripts/check-typography.mjs`（附录 A 全文）：递归扫 `src/**/*.tsx`，检出即 `exit 1`：
   - 违禁任意值：一切 `text-[Npx]`（除白名单目录的 2xl/3xl 场景）
   - 违禁档位：`text-lg`、`text-2xl` 及以上（白名单外）
   - 提醒项（warn 不 fail）：`text-[13px]` 迁移期残留
2. **冒烟挂接**：P4 起，`check-typography.mjs` 纳入前端冒烟流程（与 vitest 同列执行）。
3. **新页面模板**：新页面骨架 = `PageHeader` + `PageTabs`? + `DataTable`/`ui/table` + `PaginationBar`，模板内不出现任何 `text-*` 字号类。
4. **评审 checklist**（逐页五项）：
   - [ ] 页面标题 16/600，且由 PageHeader 渲染
   - [ ] 正文/表格/表单/树/按钮全部 14，无 13px、无任意 px
   - [ ] 辅助文字全部 12，无 12px 以下
   - [ ] 左右分栏同语义部位同字号
   - [ ] 分页栏 14，控件仅 h-8/h-9

---

## 9. 与既有规范的关系（防混淆）

| 规范 | 关系 |
|---|---|
| 《列表规整化设计规范 v2》(2026-08-18) | 表头灰带/行高/竖线等**继续有效**；其"次 13px"字阶被本方案取代（13px 淘汰） |
| 《界面风格优化方案-2026-08-29》T17-T20 | 设计 token/字体栈/按钮工具类/表格原语**全部沿用**，本方案仅增补 `--text-aux` 一档并收口字阶 |
| 元数据显示配置/列注册表 | 正交，不受影响 |

---

## 附录 A：扫描脚本 `financeWeb/scripts/check-typography.mjs`

```js
#!/usr/bin/env node
// 全局字体排版一致性扫描器（方案：根目录《全局字体排版一致性设计方案-2026-09-05.md》）
// 用法：node scripts/check-typography.mjs [目录，默认 src]
// 检出违禁 exit 1；warn 项不影响退出码。
import { readdirSync, readFileSync, statSync } from 'node:fs';
import { join, relative } from 'node:path';

const root = process.argv[2] || 'src';
// 白名单：驾驶舱统计卡允许 text-2xl/3xl
const BIG_NUM_WHITELIST = /pages[\\/]stats[\\/]/;

const RULES = [
  { re: /text-\[(?:8|9|9\.5|10|10\.5|11)px\]/g, level: 'error', msg: '12px 以下字号已禁止，归并到 text-aux(12px)' },
  { re: /text-\[(?!13px\])\d+(?:\.\d+)?px\]/g,      level: 'error', msg: '任意 px 字号已禁止，字阶闭集只有 12/14/15/16/20（用 text-aux/sm/tab/title/display）' },
  { re: /text-lg\b/g,                              level: 'error', msg: 'text-lg(18px) 已淘汰，用 text-title(16px)' },
  { re: /text-(2xl|3xl|4xl)\b/g,                   level: 'error', msg: '超档字号仅驾驶舱统计卡可用', skip: f => BIG_NUM_WHITELIST.test(f) },
  { re: /text-\[13px\]/g,                          level: 'warn',  msg: '13px 已淘汰：主信息→text-sm，辅助→text-aux' },
];

let errors = 0, warns = 0;
function walk(dir) {
  for (const name of readdirSync(dir)) {
    const p = join(dir, name);
    if (statSync(p).isDirectory()) { if (name !== 'node_modules') walk(p); continue; }
    if (!/\.tsx?$/.test(name)) continue;
    const src = readFileSync(p, 'utf8');
    src.split('\n').forEach((line, i) => {
      for (const { re, level, msg, skip } of RULES) {
        if (skip?.(p)) continue;
        re.lastIndex = 0;
        if (re.test(line)) {
          const rel = relative(process.cwd(), p);
          console[level === 'error' ? 'error' : 'warn'](`${rel}:${i + 1}  ${line.trim().slice(0, 100)}\n    ↳ ${msg}`);
          level === 'error' ? errors++ : warns++;
        }
      }
    });
  }
}
walk(root);
console.log(`\ntypography-check: ${errors} error(s), ${warns} warning(s)`);
process.exit(errors ? 1 : 0);
```

## 附录 B：参考图规格反推（设计依据存档）

| 部位 | 参考1/参考2 实测观感 | 本方案对应 |
|---|---|---|
| 侧边栏菜单 | ≈14px 白字，子菜单同号缩进 | body 14 ✓ |
| 树节点 | ≈14px，选中蓝底同字号 | body 14 |
| 页签 | ≈15px，激活蓝色加粗下划线 | tab 15 |
| 表单 label/值 | ≈14px 加粗 / 14px 灰 | body 14 + 500/slate-500 |
| 按钮/输入框 | 同高 ≈34-36px，14px | h-9 + body 14 |
| 表格表头/表体 | 同字号 ≈14px，表头靠灰带加粗 | body 14/600 ✓ |
| 分页栏 | ≈14px | body 14 |
| 唯一更小的字 | 几乎找不到；个别辅助说明 ≈12px | aux 12（唯一辅助档） |
