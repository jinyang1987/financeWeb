# 工作记录 · 2026-07-18（下午场）

> 任务来源：《bug以及借阅需求.txt》
> 内容：① 财务档案分类视图四大模块 bug 修复 ② 全链路仿真测试数据 ③ 角色权限体系 ④ 借阅全生命周期管理（PRD 全量实现）
> 结果：**tsc 零错误 · vitest 90/90 全过 · vite build 成功 · dev server 冒烟 200 OK**

---

## 〇、用户三大关键决策（实施前已确认）

| # | 问题 | 决策 |
|---|------|------|
| 1 | 借阅最小单元 | **混合粒度：电子授权到「件」，实体外借到「卷」** |
| 2 | 旧借阅占位页（发起借阅/在线借阅/我的待办/归还核验/借阅管理/专项借单等假数据页） | **全部删除，菜单按新链路重组** |
| 3 | 角色登录演示方式 | **内置 8 账号，登录页点选即登录 + Header 一键切换身份** |

---

## 一、Part A：财务分类视图四大模块修复

### 1.1 根因分析（探查结论）

| 需求 | 根因 |
|------|------|
| 筛选与列表不联动 | 筛选谓词只传给「全部列表」平铺表格 `<ArchiveTable>`，默认视图「按盒查看」`<ArchiveBoxTreeView>` 只接收 year/type 两个 prop，季度/月份/子类型/报表期间全部不传 |
| 统计条数对不上 | 页头统计显示平铺表格口径 `displayRecords.length`，画面呈现的是盒视图的盒数，两套口径 |
| 详情返回跳错 | `focusedBoxId` 是 ArchiveBoxTreeView 内部 state，打开详情时组件卸载 → 返回后盒上下文丢失，只能回盒总列表 |
| 筛选是"假"的 | 全部靠 `remarks/voucherNo` 关键词 `includes` 匹配，数据不含关键词时筛选恒为 0 |
| KP 首屏只剩 1 月 | viewStore 默认 `selectedQuarter='Q1'` + `selectedMonth=1`，且无「全部」选项 |

### 1.2 修复方案

**新交互模型（四模块统一）**：
```
L1 案卷盒列表（进入分类仅展示盒，删除「全部列表」平铺视图）
  → 点击盒 → L2 盒内卷件列表（面包屑返回盒列表）
    → 点击件 → L3 详情分栏（左栏=当前盒卷件，右栏=RecordDetailPanel）
      → 返回 → 回到 L2 盒内列表（盒上下文保留 ✓）
```

**筛选联动（字段级）**：筛选谓词统一注入 `assembleBoxViewData()`，同时作用于：盒内件、盒「件数」列、页头统计条数；筛选后无匹配件的盒自动隐藏。

| 模块 | 筛选维度 | 实现 |
|------|----------|------|
| KP 会计凭证 | 季度(含全部)/月份(含全季) + 凭证类型 + 关联原始凭证类型 | `voucherCategory` 字段 + SourceDocument 类型树 category 映射 |
| KB 会计账簿 | 账簿类型 Tab（总账/明细账/日记账/辅助账簿） | `subType` 字段 |
| FB 财务报表 | 报表分类 + 报表期间 **合并同一筛选卡** | `reportCategory` / `reportPeriod` 字段；统计卡片联动筛选结果 |
| QT 其他 | 子类型 Tab | `subType` 字段 |

### 1.3 顺手修复的数据流 bug（探查中发现）

| Bug | 位置 | 修法 |
|-----|------|------|
| 移交后记录 boxId 恒写 `''` | VolumeWorkspacePage.handleTransfer | transferVolume 后**重读案卷**（旧引用是移交前快照） |
| 撤销确认不同步记录状态 | volumeStore.unconfirmVolume | 卷内记录回退 `仅件数据` + 清档号 |
| 盒退回组卷不同步 | volumeStore.returnVolumes | 卷内记录回退 `仅件数据` + 清 boxId/storageLocation |
| 关联查询载体标签判断失效 | VolumeItemSearchPage | `'原生电子文件'` vs `'原生电子'` 字符串不匹配，顺手重写该区（见 3.4） |
| 报表统计卡 emoji 图标 | FinanceViewPage | 全部换 lucide 图标 |

### 1.4 涉及文件

| 文件 | 变更 |
|------|------|
| `src/hooks/useBoxViewData.ts` | **新建**：`assembleBoxViewData()` 纯函数 + hook 封装，盒→卷→件装配统一口径 |
| `src/pages/archive-arrange/FinanceViewPage.tsx` | **重写**（L1→L2→L3 导航 + 字段级筛选 + FB 合并筛选卡） |
| `src/components/ArchiveBoxTreeView.tsx` | **重写**（受控组件：entries/focusedBoxId 由页面持有） |
| `src/components/layout/ContentArea.tsx` | FinanceViewPage props 精简 |
| `src/pages/archive-arrange/VolumeWorkspacePage.tsx` | 移交重读案卷修 boxId |
| `src/stores/volumeStore.ts` | unconfirm/return 同步 archiveStore + 初始卷接入仿真数据 |

---

## 二、全链路仿真测试数据（`src/data/simulationData.ts` 新建）

### 2.1 设计要点
- **确定性生成**：FNV 哈希伪随机，无 `Math.random`，测试可重复
- **四层引用闭环**：records ↔ volumes ↔ volumeItems ↔ boxes，由 8 个完整性测试守护
- 既有手写数据 5 条悬空引用记录（vol-001×3/vol-004/vol-005）由生成器**同 ID 重建**；6 月 49 张待组卷凭证保留（组卷工作台演示）；既有 4 盒保留并动态修正计数

### 2.2 数据规模

| 类别 | 2025（纸质/mixed 载体） | 2026（原生电子） |
|------|------|------|
| KP 会计凭证 | 12 个月 × 14~20 件，按月组卷、按季 4 盒 | 1-3 月入 box-001；4 月 + **薪酬敏感卷（密级=秘密）** + vol-001(5月) 入 box-002 |
| KB 会计账簿 | 总账/明细账×3/日记账×2/辅助账 = 7 件 2 卷 1 盒 | vol-004(总账) + 明细/日记/辅助 共 7 件 2 卷入 box-004 |
| FB 财务报表 | 年度 4 + 月度 6 + 季度/内部 4 = 14 件 3 卷入 box-003 | 1-5 月 6 件 1 卷入新 box-fb-2026-01 |
| QT 其他 | 银行类 8 + 税务清册 6 = 14 件 2 卷 1 盒 | 上半年 6 件 1 卷 1 盒 |

合计 **~355 新记录 + 27 卷 + 12 盒**，全部 `transferred` 状态（财务视图/检索/借阅直接可用）。

### 2.3 数据模型新增字段

```ts
// ArchiveRecord（types.ts）
voucherCategory?: string   // 收款/付款/转账/通用记账凭证（KP 筛选）
subType?: string           // 总账/明细账/日记账/辅助账簿；银行对账单/纳税申报表…（KB/QT 筛选）
reportCategory?: string    // 法定对外/内部管理/专项报告（FB）
reportPeriod?: string      // 月度/季度/年度（FB）
preparer?: string          // 制单人（检索维度）
accountSubject?: string    // 会计科目（检索维度）
securityLevel?: string     // 普通/内部/秘密/机密（借阅审批路由）

// Volume（types/volume.ts）
carrierType?: 'paper' | 'electronic' | 'mixed'   // 介质类型（借阅单必填项）
securityLevel?: string
```

---

## 三、角色权限体系

### 3.1 内置 8 账号 7 角色（`src/types/user.ts`）

| 账号 | 姓名 | 部门/岗位 | 角色 | 演示用途 |
|------|------|-----------|------|----------|
| zhangwei | 张伟 | 财务部/会计 | employee 员工 | 借阅发起人（种子单主角） |
| lina | 李娜 | 财务部/出纳 | employee 员工 | 借出中 + **逾期黑名单**演示 |
| wangqiang | 王强 | 财务部经理 | dept_manager 部门经理 | 一级审批 |
| chenjing | 陈静 | 档案部 | archivist 档案管理员 | 终审 + 出库/归还核销 |
| liumin | 刘敏 | 档案部主管 | archive_director 档案主管 | 台账/统计驾驶舱 |
| zhaogang | 赵刚 | 财务总监 | cfo | 高危权限升级审批 |
| sunli | 孙丽 | HR 副总裁 | hrvp | 涉密强制会签 |
| admin | 系统管理员 | 信息中心 | admin | 全部菜单（不受矩阵限制） |

直属主管链（逾期抄送）：张伟/李娜 → 王强 → 赵刚；陈静 → 刘敏。

### 3.2 落地清单

| 文件 | 变更 |
|------|------|
| `src/types/user.ts` | **新建**：RoleKey/UserAccount/MOCK_USERS/ROLE_MENU_MATRIX |
| `src/stores/authStore.ts` | 重写：currentUser 完整账号 + persist `auth-session-v1` + hasRole |
| `src/components/LoginPage.tsx` | 重写：账号卡片点选登录（保留左侧品牌区） |
| `src/components/layout/Header.tsx` | 真实用户头像/姓名/角色 + **身份切换下拉** |
| `src/components/layout/Sidebar.tsx` | 按角色过滤菜单项（组空则整组隐藏） |
| `src/stores/roleStore.ts` | **新建**：角色→菜单矩阵可编辑副本，persist `role-menus-v1` |
| `src/components/RoleManagePanel.tsx` | **重写**：左角色列表（含用户）右菜单矩阵勾选，即时生效，可恢复默认 |
| `src/components/watermark/SecurityWatermark.tsx` | 工号/角色改从 currentUser 读取（豁免链路打通） |

### 3.3 角色→功能分工（已捋顺）

- **employee**：档案查询 4 页 + 我的借阅
- **dept_manager**：+ 审批中心 + 借阅台账
- **cfo**：+ 审批中心 + 台账 + 借阅统计
- **hrvp**：+ 审批中心
- **archivist**：全业务（收集/整理/保管/利用全部/配置/统计）
- **archive_director**：保管 + 利用 + 统计
- **admin**：全部

---

## 四、Part B：借阅全生命周期（PRD 全量实现）

### 4.1 数据模型（`src/types/borrow.ts` 新建）

```
BorrowOrder 主单（JY-YYYY-NNNN）
  ├── items: BorrowOrderItem[]       明细行（件级）
  │     题名/凭证号/档案类型/介质类型(纯电子/纯实体/混合)/密级/库存状态快照
  │     electronicPerms: view/download/print（件级勾选）
  │     physicalMode: none/original/copy（卷级，同卷多行须一致）
  ├── approvalRoute: ApprovalStep[]  动态审批链 + currentStepIndex
  └── fulfillments: Fulfillment[]    履约子单（electronic=件级 / physical=卷级）
        status: pending待出库/granted已授权/lent借出/queued预约等待
                /returned/auto_revoked电子到期收回/overdue逾期/terminated
```

主单状态机：`approving → fulfilling/active/returning → completed`（+ rejected/terminated），由子单状态推导（`deriveOrderStatus`）。
常量：`MAX_BORROW_DAYS=30`、`EXPIRY_WARN_DAYS=3`。

### 4.2 动态审批路由（`src/utils/borrowEngine.ts`）

```
仅在线浏览        → 部门经理 → 档案管理员
含下载/打印/实体  → 部门经理 → 财务总监(CFO) → 档案管理员
涉密(秘密/机密)   → 部门经理 → HRVP → 档案管理员
两者叠加          → 部门经理 → CFO → HRVP → 档案管理员
```
申请页提交前**实时预览审批链**（含各节点审批人姓名）。

### 4.3 智能拆单（末级审批通过瞬间）

| 场景 | 子单 | 履约 |
|------|------|------|
| 电子权限行 | 每件一张 electronic 子单 | **即时 granted**，在线调阅开通（强制水印） |
| 实体卷在库 | 每卷一张 physical 子单 | pending → 推送管理员待出库 |
| 实体卷被借出 | 同上 | **queued 预约等待**；归还扫码瞬间自动锁定给排队最久者并写「预约锁定」日志 |

### 4.4 借阅中 / 到期 / 逾期

- **在线调阅**：倒计时标签（<24h 红色）+ 调阅弹窗（红色倒计时横幅 + `SecurityWatermark scene=preview` 配置驱动水印 + 下载/打印按授权门控，未授权置灰）
- **一键中止**：管理员在借阅管理页终止 → 全部生效子单 → terminated，权限瞬间收回
- **每日巡检**（借阅管理页手动触发，模拟定时任务）：
  - 电子 granted 过期 → `auto_revoked`（无感归还）
  - 实体 lent 过期 → `overdue` + 抄送直属主管日志
  - 到期前 3 天 → 催还预警
- **黑名单熔断**：名下有 overdue 实体 → `submitOrder` 抛错 + 我的借阅页红色熔断横幅 + 借阅管理黑名单页展示

### 4.5 操作留痕（等保）

所有动作写入 `borrowStore.logs`：检索/加入借阅车/发起申请/审批通过/审批驳回/电子授权/实体出库/预约排队/预约锁定/归还核销/到期收回/逾期预警/催还/中止/在线查看/下载/打印。
展示入口：借阅统计页「全链路操作日志」+ 系统管理「安全审计日志」（已并入）。

### 4.6 页面清单（6 新 + 3 重写 + 共享组件 2）

| 页面 | 文件 | 说明 |
|------|------|------|
| 我的借阅 | `pages/archive-utilization/my-borrow/MyBorrowPage.tsx` | 3 Tab：借阅车/我的申请/在线调阅 + 黑名单熔断横幅 |
| 借阅车结算 | `my-borrow/BorrowCartCheckout.tsx` | 逐行权限勾选 + 事由/周期 + 审批链预览 + 校验 |
| 我的申请 | `my-borrow/MyBorrowOrders.tsx` | 审批进度链 + 履约子单状态 + 撤销 |
| 在线调阅 | `my-borrow/BorrowReadingRoom.tsx` | 倒计时 + 水印调阅弹窗 + 权限门控 |
| 审批中心 | `pages/archive-utilization/ApprovalCenterPage.tsx` | 按角色命中待办；同意/驳回（必填原因）；已办记录 |
| 借阅管理 | `pages/archive-utilization/BorrowManagePage.tsx` | 5 Tab：待出库/待归还/预约队列/进行中/黑名单与巡检 |
| 借阅台账 | `pages/archive-utilization/BorrowLedgerPage.tsx` | 重写：真实订单派生，筛选+展开履约明细 |
| 借阅统计 | `pages/archive-utilization/BorrowStatsPage.tsx` | KPI + 借阅热力图 + 月度趋势 + 逾期红黑榜 + 操作日志 |
| 凭证检索 | `pages/archive-query/VoucherSearchPage.tsx` | 重写：真实数据 7 维组合查询 + 状态标签 + 加入借阅 |
| 事项检索 | `pages/archive-query/MatterSearchPage.tsx` | 重写：原始凭证全文/OCR 检索 → 回溯所属凭证加入借阅 |
| 关联查询 | `pages/archive-preserve/VolumeItemSearchPage.tsx` | 加状态标签 + 加入借阅 + 修载体标签判断 bug |
| 状态标签 | `components/borrow/ArchiveStatusTags.tsx` | 【电子版可用】【实体在库】【实体借出】 |
| 借阅车浮条 | `components/borrow/BorrowCartBar.tsx` | 检索页右下角悬浮 + 抽屉预览 + 去结算 |

### 4.7 菜单重组

```
档案利用（新）：
  我的借阅 / 审批中心 / 借阅管理 / 借阅台账 / 借阅统计 / 案卷移交管理 / 工作流设计器
档案查询：去掉「在线借阅」（借阅一律从检索发起）
```

### 4.8 删除的文件（7 页 + 2 死路由）

`BorrowApplyPage.tsx` `BorrowOnlinePage.tsx` `MyTasksPage.tsx` `ReturnVerifyPage.tsx` `ReturnManagePage.tsx` `OrderSpecialPage.tsx` `WorkflowControlPage.tsx` `router/index.tsx` `AppRoutes.tsx`

### 4.9 borrowStore 种子数据（5 张单，覆盖全部演示场景）

| 单号 | 人 | 场景 |
|------|-----|------|
| JY-2026-0001 | 李娜 | 2025年3月凭证卷实体**借出中**（7-25 到期） |
| JY-2026-0002 | 张伟 | 电子授权生效中（7-20 到期，**倒计时**演示） |
| JY-2026-0003 | 李娜 | 实体**逾期未还**（黑名单熔断演示） |
| JY-2026-0004 | 张伟 | **审批中**（含打印→三级链，王强演示审批） |
| JY-2026-0005 | 张伟 | **预约等待**（卷被李娜借走，归还后自动锁定） |

---

## 五、推荐演示路径（全流程走查）

1. **张伟**登录 → 凭证检索（可按科目/主体/金额/制单人组合）→ 加入借阅 2~3 件
2. 右下借阅车 → 去结算 → 勾选权限（试勾「允许打印」触发 CFO 链）→ 提交（看审批链预览）
3. Header 切 **王强** → 审批中心同意 → 切 **赵刚**（如有打印/实体）→ 切 **陈静**终审
4. 终审通过瞬间：电子即时授权 / 实体进陈静的借阅管理「待出库」
5. 陈静扫码出库 → 切回 **张伟** → 我的借阅「在线调阅」→ 倒计时 + 水印调阅
6. 陈静「待归还」扫码归还 → 若有排队预约 → 自动锁定并通知
7. **李娜**登录 → 我的借阅顶部可见**黑名单熔断**横幅（新建按钮锁死）
8. 陈静/刘敏 → 借阅统计：热力图/红黑榜/操作日志；系统管理→角色管理可改菜单权限

---

## 六、测试与验证

| 测试文件 | 用例数 | 覆盖 |
|----------|--------|------|
| `__tests__/simulationData.test.ts` | 8 | 四层引用闭环/类别×年份覆盖/字段填充率/敏感卷/载体分布/无重复ID |
| `__tests__/financeViewFilters.test.ts` | 8 | 盒件装配/月份/凭证类型/账簿子类型/报表分类×期间交叉/QT子类型/空筛选 |
| `__tests__/borrowLifecycle.test.ts` | 20 | 审批路由5种组合/拆单3场景/状态机/每日巡检3类事件/黑名单/全链路集成5流程 |
| 既有 5 个测试文件 | 54 | 无回归 |
| **合计** | **90** | 全过 |

- `tsc --noEmit` 零错误；`vite build` 成功；dev server 冒烟 200 OK
- **flaky 教训**：store 内 ID 生成必须单调——`order-${Date.now()}` 同毫秒撞号导致 `find` 命中错误单（2/3 概率复现），改 `order-${Date.now()}-${seq++}` 后 5/5 稳定

---

## 七、遗留事项（后续可做）

- [ ] PersonnelManagePanel（系统管理→人员管理）仍走 Alfresco REST API，与 8 个内置演示账号是两套体系；角色/登录/审批演示均不依赖它，后续可切 mock 或对接
- [ ] AuditTrailPage（审计追踪）仍是单凭证 mock 链路，未接真实 records
- [ ] 电子借阅「下载烧录」为模拟提示（前端水印策略已就绪，后端 PDFBox/iText 烧录待实现）
- [ ] borrowStore 种子数据若调整，需 bump persist key `borrow-lifecycle-v1`
- [ ] 组卷工作台移交时 `window.prompt` 输入库房位置 → 待改正式弹窗（老遗留）
- [ ] wfDesignerStore 流程定义与借阅审批引擎仍是两套（设计器画的是模板，引擎按规则动态路由），后续可将设计器改为路由规则的可视化编辑器

---

## 八、关键文件总清单

**新建（19）**：
`types/user.ts` `types/borrow.ts` `stores/roleStore.ts` `data/simulationData.ts` `hooks/useBoxViewData.ts` `utils/borrowEngine.ts` `components/borrow/ArchiveStatusTags.tsx` `components/borrow/BorrowCartBar.tsx` `pages/archive-utilization/my-borrow/MyBorrowPage.tsx` `my-borrow/BorrowCartCheckout.tsx` `my-borrow/MyBorrowOrders.tsx` `my-borrow/BorrowReadingRoom.tsx` `pages/archive-utilization/ApprovalCenterPage.tsx` `pages/archive-utilization/BorrowManagePage.tsx` `pages/archive-utilization/BorrowStatsPage.tsx` `__tests__/simulationData.test.ts` `__tests__/financeViewFilters.test.ts` `__tests__/borrowLifecycle.test.ts`

**重写（8）**：
`stores/authStore.ts` `stores/borrowStore.ts` `components/LoginPage.tsx` `components/RoleManagePanel.tsx` `components/ArchiveBoxTreeView.tsx` `pages/archive-arrange/FinanceViewPage.tsx` `pages/archive-query/VoucherSearchPage.tsx` `pages/archive-query/MatterSearchPage.tsx` `pages/archive-utilization/BorrowLedgerPage.tsx`

**修改（12）**：
`types.ts` `types/volume.ts` `stores/archiveStore.ts` `stores/volumeStore.ts` `stores/archiveBoxStore.ts` `stores/appStore.ts` `config/menuConfig.ts` `components/layout/Header.tsx` `components/layout/Sidebar.tsx` `components/layout/ContentArea.tsx` `components/watermark/SecurityWatermark.tsx` `components/AuditLogsPanel.tsx` `pages/archive-arrange/VolumeWorkspacePage.tsx` `pages/archive-preserve/VolumeItemSearchPage.tsx` `pages/system/RoleManagePage.tsx`

**删除（9）**：
7 个旧借阅假数据页 + `router/index.tsx` + `AppRoutes.tsx`

---

# 追加任务：档案统计体系重构（依据《会计档案统计维度与统计内容汇总.md》）

## 九、任务要求

1. 借阅统计**连菜单带功能**从「档案利用」挪到「档案统计」组
2. 按《会计档案统计维度与统计内容汇总.md》（79号令 + DA/T 94-2022 + 用友/久其/金蝶/合思/会博通实践）重新设计档案统计，**替换**原有两个菜单（档案统计仪表盘 dashboard + 查询统计分析 search-stats）
3. 驾驶舱大屏展示，**模块可自行配置**，配置入口在系统管理

## 十、统计体系设计（5 菜单 = 需求文档 5 大维度）

```
档案统计
  ├── 统计驾驶舱   stats-cockpit      深色数据大屏（可配置模块，新首页）
  ├── 库藏统计     stats-inventory    维度1 家底盘点：类型/年度/期限/全宗/部门/载体
  ├── 流程统计     stats-lifecycle    维度2 生命周期：归档/组卷/四性/移交/鉴定处置
  ├── 借阅统计     borrow-stats       维度3 利用价值（从利用组挪入 + 利用行为KPI增强）
  └── 合规统计     stats-compliance   维度4 监管合规：期限/数据质量/安全/审计支撑
系统管理
  └── 驾驶舱配置   sys-cockpit-config 大屏模块 开关/排序/宽度（cockpitStore persist）
```
维度5（运营效率）的量化指标（电子化率/自动化占比）融入驾驶舱模块与合规页，不单独造无数据页面。

## 十一、统计引擎（`src/utils/statsEngine.ts`）

纯函数四域计算，**全部从真实 store 数据派生，零硬编码统计值**：
- `computeInventory` — 总量 KPI + 类型/年度/期限/全宗/部门/载体分布 + 电子化率 + 容量（解析组件 size 字符串）
- `computeLifecycle` — 待归档(仅件数据)/已归档(已组卷+卷已移交)/整理完成率/本月新增/卷状态计数/四性分性通过率/近6月归档趋势/到期档案(retention 推算)
- `computeUtilization` — 检索/调阅/下载/打印/加车行为计数 + 热点档案TOP + 类型热力 + 部门/人员利用
- `computeCompliance` — 元数据完整率(8个必填字段)/格式合规率(OFD/PDF/XML白名单)/期限标注率/超期未销毁/未来5年到期预告/审计支撑(外部审计+税务稽查单)/逾期+黑名单
- 测试 `__tests__/statsEngine.test.ts` **22 用例**：口径与源数据一致性 + 构造脏数据验证缺失识别

## 十二、驾驶舱大屏（`StatsCockpitPage`）

- 深色数据大屏风格（slate-950 + 青色辉光 + 背景网格），实时时钟，全屏按钮（Fullscreen API）
- **12 个可配置模块**（cockpitStore，persist `cockpit-config-v1`）：库藏总览/类型分布/年度趋势/期限结构/电子化进程/流程监控/借阅热力/逾期黑名单预警/部门利用排行/全宗分布/数据质量/实时动态
- 模块三属性：**开关、排序、宽度（半栏/全栏）**，配置页（系统管理→驾驶舱配置）即时生效、可预览、可恢复默认
- 图表全部 CSS/SVG 自绘（conic-gradient 环形、SVG 仪表环、CSS 柱条），无第三方图表库依赖
- 图表共享组件 `components/stats/Charts.tsx`（浅色版，供三大统计页复用）

## 十三、接线与守卫

- 菜单组替换 + MenuId 扩展 + getPageTitle 新增 5 条
- **默认首页** `activeMainMenu: 'dashboard'` → `'stats-cockpit'`
- **角色路由守卫**（ContentArea 新增 effect）：当前菜单对角色不可见时自动回退到该角色第一个可见菜单（员工登录落 凭证检索，管理岗落 统计驾驶舱）
- ROLE_MENU_MATRIX 更新（archivist/archive_director 获得 4 个新统计菜单）；roleStore persist key bump `role-menus-v2`（旧缓存矩阵不含新菜单）
- 删除 `DashboardPage.tsx`（371行）+ `SearchStatsPage.tsx`（135行）
- 借阅统计挪组 + 新增「利用行为统计」KPI 行（检索/调阅/下载/打印/加车次数）

## 十四、验证（最终）

- `tsc --noEmit` 零错误；vitest **112/112 全过**（新增 statsEngine 22）；`vite build` 成功；dev server 冒烟 200 OK
- 无悬挂引用（DashboardPage/SearchStatsPage 全清）

## 十五、新增/修改文件（统计模块）

**新建（9）**：`utils/statsEngine.ts` `hooks/useArchiveStats.ts` `stores/cockpitStore.ts` `components/stats/Charts.tsx` `pages/archive-stats/StatsCockpitPage.tsx` `InventoryStatsPage.tsx` `LifecycleStatsPage.tsx` `ComplianceStatsPage.tsx` `pages/system/CockpitConfigPage.tsx` `__tests__/statsEngine.test.ts`

**修改（6）**：`config/menuConfig.ts` `stores/appStore.ts`(MenuId+默认首页) `types/user.ts`(矩阵) `stores/roleStore.ts`(key bump) `components/layout/ContentArea.tsx`(路由+守卫) `pages/archive-utilization/BorrowStatsPage.tsx`(利用行为KPI)

**删除（2）**：`pages/DashboardPage.tsx` `pages/archive-stats/SearchStatsPage.tsx`
