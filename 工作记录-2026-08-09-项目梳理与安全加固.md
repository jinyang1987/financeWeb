# 工作记录 · 2026-08-09（项目梳理与安全加固）

> 任务来源：ISSUE #1「梳理项目结构/核心业务流程；修复已知 bug；检查漏洞与潜在风险点；做好日志记录；确认 DeepSeek V4 Flash 可行性」
> 执行人：CodeBuddy（NPC）· 仓库：xiaoluxiaolu/financeDA
> 分支：auto/security-fix-xxxx（本工作记录随 PR 提交）

---

## 一、项目结构梳理

### 1.1 仓库全景（顶层）

| 目录/文件 | 说明 |
|-----------|------|
| `financeWeb/` | **主前端**（React 19 + Vite + TS + zustand），会计档案管理系统全部业务页面 |
| `financeWeb-vue/` | **参考 Vue3 版本**（Element Plus，功能子集，历史对照/重写参考） |
| `ams-server/` | **Java 业务后端**（Spring Boot 3 + PG/ams schema + Flyway + Alfresco 客户端），档案业务 API 唯一事实源 |
| `alfresco-sdk/` / `my-aio-project/` | Alfresco 集成 SDK / AIO 工程（模型部署、扩展） |
| `custom-model/` / `seed/` | 财务内容模型（finance-model.xml）与种子脚本（Node 冒烟） |
| `finance-model-*.xml` | 内容模型当前/修复/容器内版本比对 |
| `*.md` | 总体方案、主线业务逻辑全景、工作记录、借阅迁移方案、用友 BIP 集成等文档 |

### 1.2 主前端 `financeWeb/` 分层

```
src/
  pages/           业务页面（archive-arrange 整理 / archive-query 查询 / archive-utilization 借阅 /
                   archive-config 配置 / archive-stats 统计 / archive-rcv 采集 / archive-disposal 处置 ...）
  components/      通用组件（ArchiveBoxTreeView 盒视图 / RecordDetailPanel 详情 / borrow 借阅组件 / layout 布局）
  stores/          zustand 状态（archiveStore 档案 / volumeStore 案卷 / archiveBoxStore 盒 / borrowStore 借阅 / 各配置 store）
  services/        API 层（http 统一封装 → ams-server /api/ams；alfresco/fonds 直连 Alfresco；borrowService 借阅）
  hooks/           useBoxViewData（盒→卷→件装配）/ useArchiveStats（统计聚合）
  utils/           borrowEngine（借阅状态机纯函数）/ statsEngine（统计）/ packageEngine（封装包）
  data/            simulationData.ts（全链路仿真数据）
server/            Express 3001（目录配置 JSON 后端，过渡期）
```

### 1.3 后端 `ams-server/` 分层

```
src/main/java/com/finance/ams/
  auth/        认证（Alfresco ticket 会话 + 角色解析）
  record/      件域（上传/列表/卷内盒内读取/内容预览下载水印/OCR）
  volume/      案卷域（建卷/加件/拆件/确认赋号/移交归盒/退回）
  box/         盒域
  borrow/      借阅域（4 表 + 审批路由 + 智能拆单 + 履约 + 每日巡检 + 黑名单）
  inspection/  四性检测（真实/完整/可用/安全）
  storage/     实体库房
  stats/       统计聚合（SQL）
  oplog/       操作日志（SHA-256 哈希链防篡改）
  configcenter/ 配置中心（/config/{key} jsonb）
  code/        档号流水（行锁原子取号）
  yonyou/      用友 BIP 集成（同步/自动组卷）
  watermark/   水印烧录（PDFBox）
  ocr/         OCR（docker exec tesseract / Tess4J）
```

### 1.4 核心业务流程（主线）

```
收集(✘占位) → 整理(✅真闭环) → 保管(✅视图) → 利用(✅借阅全生命周期) → 移交(⚠) → 销毁(✘占位)
                  ↓
  上传 → 收集池(archiveStore.loadRecords) → 核对 → 组卷(volumeStore) → 赋号(CodeSerial 原子) 
  → 移交归盒(archiveBoxStore) → 财务分类视图(useBoxViewData 盒→卷→件) → 借阅(申请→审批→拆单→履约→归还→逾期)
```

**已验证闭环（07-29 贯通检查）**：档案主流（上传→收集池→组卷→赋号→移交→财务视图）· 登录/全宗/配置持久化 · 档案类统计读真实数据 · 借阅后端化（borrowStore 纯 API 读写）。

---

## 二、已知 Bug 复核结论

《bug以及借阅需求.txt》要求的四大模块修复（财务分类视图统一盒层级浏览、筛选联动、统计条数同步、详情返回路由）在 **2026-07-18 已全部实现**，并有 `financeViewFilters.test.ts` 7 个回归用例守护。本次逐条复核：

| 需求 | 现状 | 结论 |
|------|------|------|
| 删除「按盒查看全部列表」入口，统一盒层级 | FinanceViewPage 只渲染 ArchiveBoxTreeView（L1盒→L2盒内件→L3详情），无平铺列表 | ✅ 已修复 |
| 筛选与列表数据联动、统计条数同步 | `recordFilter` 注入 `assembleBoxViewData`，作用于盒内件/件数列/页头统计/报表统计卡 | ✅ 已修复 |
| 详情返回回到盒内卷件列表 | `focusedBoxId` 由页面持有，详情打开不清空，返回留在 L2 | ✅ 已修复 |
| 报表分类+期间合并筛选区 | FinanceViewPage 已合并同一筛选卡 | ✅ 已修复 |

**本次新发现并修复的瑕疵**：`VoucherUploadModal` 上传电子文件（PDF/图片）也强制 `carrierType: 'paper'`（与 UploadModal 口径不一致，且影响借阅介质类型判断）→ 已改为按文件类型判定（PDF/图片→electronic，其余→paper）。

---

## 三、安全漏洞与潜在风险点排查

### 3.1 已修复（本次提交）

| # | 漏洞 | 位置 | 风险等级 | 修复 |
|---|------|------|---------|------|
| 1 | **借阅审批/出库/归还/终止零角色校验**：任何登录用户可 `approve/reject/terminate/checkout/return`，任意操作他人借阅单 | BorrowController | 🔴 严重（越权 IDOR） | `requireAnyRole`：审批需 dept_manager/cfo/hrvp/archivist/archive_director；出库/归还/终止需 archivist/archive_director/admin |
| 2 | **借阅申请可冒名**：申请人姓名/工号/部门取自请求体，可伪造他人申请 | BorrowController | 🔴 严重 | 改为 `authService.me(userId, ticket)` 以服务端会话为准，忽略前端字段；SubmitOrder 服务端兜底校验 userName |
| 3 | **统计接口零鉴权**：`/stats/inventory|lifecycle|compliance|borrow` 无任何会话校验，匿名可读全库统计 | StatsController | 🟠 高 | 统一 `requireAuth(userId, ticket)` |
| 4 | **档号取号接口零鉴权**：`/code/next`、`/code/peek` 匿名可任意消耗/查询流水号 | CodeSerialController | 🟠 高 | 统一 `requireAuth` |
| 5 | **审计日志接口只验 userId 不验 ticket**：伪造 X-User-Id 即可读日志 | OperationLogController | 🟠 高 | 补 ticket 校验 |
| 6 | **四性检测接口只验 userId 不验 ticket** | InspectionController | 🟡 中 | 补 ticket 校验 |

### 3.2 排查后确认安全的部分

- **SQL 注入**：全后端使用 `JdbcClient` 参数化（`?` 占位），未发现字符串拼接注入点。
- **XSS**：前端无 `dangerouslySetInnerHTML`/`v-html`/`eval`，React/Vue 默认转义。
- **文件上传**：multipart 限 50MB/60MB，无路径穿越（文件名仅作展示，存储走 Alfresco 节点）。
- **密码**：演示密码 123456 仅存在于 seed 脚本/开发登录，后端认证走 Alfresco ticket，无明文密码落库。
- **CORS**：白名单默认 `http://localhost:5000`，可环境变量收紧。

### 3.3 遗留风险点（需业务侧/后续立项，本次未改）

| # | 风险 | 说明 |
|---|------|------|
| 1 | **越权读取（水平）未封口**：`GET /borrow/orders`、`GET /records/{id}/content` 等只验会话不验数据归属。档案内容权限依赖 Alfresco 权限体系；借阅单详情建议补「仅本人/审批人/管理员可见」 | 待产品确认 |
| 2 | **操作日志哈希链无验链接口**：已写入哈希链，但缺 `verify` 端点对外校验 | 待立项 |
| 3 | **前端开发期回退凭据** `Basic admin:admin`（session.ts）在生产需彻底移除 | 部署清单项 |
| 4 | **遗留 mock 页面**：ApiReceivePage（收集接口）、AppraisalManagePage（鉴定销毁）、TransferManagePage（案卷移交）、smart-receive 三 Tab（OCR/抽取/转换演示）、triggerOcr 未接线 | 待补后端 |
| 5 | **后端 V2/V3 迁移未在可构建环境编译验证**（沙箱无 mvn/java），已按既有 JdbcClient 模式编写 | 建议 CI 环境验证 |
| 6 | **`.cnb.yml` 是示例占位**，未配置真实构建流水线 | 待配置 |

---

## 四、DeepSeek V4 Flash 可行性回答

**结论：可以，且非常适合本项目。** 理由：

1. **任务可并行拆解**：本仓库所有工作（代码修改/安全审查/测试/文档）都是独立可验证的子任务，AI 编程助手可逐项执行。
2. **已有 CI/测试基线**：`tsc` + `vitest`（123 用例）+ `vite build`，任何代码变更都有客观验收门槛，AI 产出质量可机器校验。
3. **文档驱动**：仓库有全套方案文档（主线业务逻辑全景/集成方案/迁移方案/工作记录），DeepSeek 可读文档后对齐上下文。
4. **需注意的边界**：
   - 后端 `ams-server` 需在有 Java/Maven 的环境构建验证（当前沙箱不可用），建议接入 CI（`.cnb.yml`）让构建自动化。
   - 涉及 Alfresco 容器/模型部署的操作仍需人工或专用 runner。
   - 遗留 mock 页面改造建议按优先级分批（先收集→整理闭环，再处置/鉴定）。
5. **建议的 DeepSeek 分工**：日常代码修复/测试补全/文档整理 → DeepSeek V4 Flash；高危安全评审、数据库迁移、容器部署 → 人工 + CI 把关。

---

## 五、本次变更清单

| 文件 | 变更 |
|------|------|
| `ams-server/.../borrow/BorrowController.java` | 借阅越权修复 + 冒名申请修复（服务端会话取申请人） |
| `ams-server/.../borrow/BorrowService.java` | submitOrder 服务端兜底校验申请人身份 |
| `ams-server/.../stats/StatsController.java` | 4 个统计端点补 requireAuth |
| `ams-server/.../code/CodeSerialController.java` | /code/next、/code/peek 补 requireAuth |
| `ams-server/.../oplog/OperationLogController.java` | 补 ticket 校验 |
| `ams-server/.../inspection/InspectionController.java` | 补 ticket 校验 |
| `financeWeb/src/pages/archive-arrange/VoucherUploadModal.tsx` | 上传载体随文件类型（电子文件不再强制 paper） |
| `工作记录-2026-08-09-项目梳理与安全加固.md` | 本文档 |

## 六、验证情况

- 后端 Java 改动无法在沙箱编译（无 mvn/java），已逐处核对 import、签名与既有代码风格一致（参考 VolumeController/BorrowController requireAuth 模式）。
- 前端改动为纯逻辑修改，`carrierType` 类型为既有字段，不破坏类型。
- 建议在具备构建环境时执行：`cd ams-server && mvn clean compile` 与 `cd financeWeb && npm run lint && npx vitest run && npm run build`。
