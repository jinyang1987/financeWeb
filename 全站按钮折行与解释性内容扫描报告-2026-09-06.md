# 全站按钮折行与解释性内容扫描报告

- 日期：2026-09-06（扫描）/ 2026-09-07（P2+P3 执行完毕，见文末"五、执行记录"）
- 性质：组卷工作台两项修复（《界面精简与按钮防折行方案-2026-09-06.md》）完成后的**同口径全站扫描**
- 状态：**P2（B1-B5 + D1-D4）与 P3（S1/S2）已于 2026-09-07 执行完毕并验收通过；B6 复核后无需改动**
- 扫描方法：
  1. 全站 `pages/ + components/` 共 **80 处 flex-wrap** 容器逐一分类（脚本判定内容类型 + 人工复核上下文）；
  2. 解释性静态文案关键词扫描（按规定/自动执行/将阻断/说明：/提示： 等），命中 20 处逐一人工定性。

---

## 一、按钮折行同类问题（flex-wrap 分类结果）

### 1.1 总体分布

| 分类 | 数量 | 结论 |
|---|---|---|
| TEXT（文本/徽章行） | 44 | **合理换行，不改**。文本行、徽章组、元数据行换行是正常流式布局 |
| FILTER（筛选器行） | 18 | **合理换行，不改**。下拉/输入框组成的筛选栏（与组卷工作台顶部筛选栏同款），换行不产生"按钮折行"的破碎感 |
| BTN（容器内含按钮） | 18 | 人工逐一复核，分两类 ↓ |

### 1.2 BTN 18 处复核结论

**A 类：芯片/分段选择器（12 处）——换行是设计形态，不改。**
选项数量多（类别/字段/库房preset 等 5-20 个），换行布局正是预期形态，与"工具栏按钮折行"的破碎感不同：

| 位置 | 内容 |
|---|---|
| `components/DigitalWarehousePanel.tsx:384` | 密集架房间切换 chips |
| `components/RoleManagePanel.tsx:318/365` | 数据范围（全宗/门类）授权 chips |
| `components/ShelfPositionPicker.tsx:93/110` | 库房/密集架选择 chips |
| `components/portal/PortalHomePage.tsx:111` | 门户快捷检索 chips |
| `pages/archive-arrange/FinanceViewPage.tsx:341/354` | 凭证/账簿类别筛选 chips |
| `pages/archive-config/InspectionConfigPage.tsx:414/454/526` | 检测字段勾选 chips |
| `pages/archive-config/manage/GroupingConfigTab.tsx:269/271` | 档案盒厚度 preset chips |
| `pages/archive-config/manage/MetadataConfigTab.tsx:177` | 字段显隐 chips |

**B 类：真工具栏按钮行（6 处）——与组卷工作台同类型，建议按同款方案改造（P2 批次候选）：**

| # | 位置 | 现状 | 风险 | 建议方案（同款三件套） |
|---|---|---|---|---|
| B1 | `components/FanzongManager.tsx:473` | 分册管理面板头：`justify-between + flex-wrap`，左侧含新建等操作按钮 | 窄屏时按钮逐个掉行（与截图问题同款） | 容器 `flex-nowrap overflow-x-auto no-scrollbar`；按钮补 `whitespace-nowrap shrink-0` |
| B2 | `pages/archive-config/manage/ArchiveManageConfigPage.tsx:38` | 档案配置页头：页签分段器 + 右侧动作按钮，容器 flex-wrap | 页签组与按钮组整体掉行（组间换行，破碎感中等） | 分段器与按钮组各自 `shrink-0 whitespace-nowrap`；容器保留 flex-wrap（组间换行可接受）或改 nowrap+滚动 |
| B3 | `pages/archive-config/manage/MetadataConfigTab.tsx:603` | 元数据配置工具栏：模式分段器 + 右侧按钮组，flex-wrap | 同 B2，组间换行 | 同 B2 |
| B4 | `components/portal/PortalSearchPage.tsx:47` | 门户检索页顶栏（返回/首页 + 检索域切换按钮），flex-wrap | 公众访客界面，窄屏时按钮换行 | 按钮 `shrink-0 whitespace-nowrap` + 容器 nowrap + no-scrollbar |
| B5 | ~~页签行~~ **审批链节点行**（执行时复核修正定性）：`ApprovalCenterPage.tsx:182` / `MyBorrowOrders.tsx:147` 均为借阅单审批链（节点+连接线），非页签 | 容器 flex-wrap，长链换行；被挤压时**节点内部**文字折行 | 链间换行保留（长审批链换行是正确形态）；**节点级补 `whitespace-nowrap shrink-0`**——节点永不在内部折行，只允许在节点之间换行 |
| B6 | `components/FanzongManager.tsx:473` 之外的低风险组间换行 | RoleManagePanel 头部、StorageConfigPage:378 等筛选+按钮混合行 | 按钮组随筛选器换行 | 低优先级：仅给按钮补 nowrap/shrink-0，容器保留 flex-wrap |

> 通用兜底建议（写入执行方案）：凡按钮容器改 `flex-nowrap`，必须同步给容器 `min-w-0`、给每个按钮 `whitespace-nowrap shrink-0`（本次执行实测：缺 shrink-0 会被压缩内部折行——这是比容器折行更隐蔽的形态）。

---

## 二、解释性内容同类问题（与"删右侧解释"同口径）

处置原则（与用户 09-06 指示一致："都是专业的人员，都懂数据，不用解释"）：
- **删**：常驻的环境性说明（界面每页都占地方解释规则）；
- **留**：①数据本身（"批次说明：xxx"是字段值）；②操作当下的决策说明（确认弹窗内、危险操作二次确认里）；③空态引导（无数据时才出现）；④悬浮 title（不占版面）。

### 2.1 建议删除（P2 批次候选，共 4 处）

| # | 位置 | 现状文案 | 理由 | 去向 |
|---|---|---|---|---|
| D1 | `pages/archive-arrange/QuickCheckPage.tsx:418` | 页头常驻副标题「四性检测结果中心 · 按规定检测在移交（推送至保管库）时自动执行，未通过即阻断移交」 | 与已删的组卷卡提示框同源同款；规则由移交环节强制执行 | 删括注，保留「四性检测结果中心」；语义已在本次并入确认组卷 title |
| D2 | `pages/archive-arrange/MetadataEntryModal.tsx:616` | 弹窗底部「原始凭证录入依据 DA/T 95-2022 与主管部门凭证制式；扩展字段按类型字段集动态展开，落库 finance:srcDocExtFields。」 | 连落库字段名都写出来的开发者视角说明，会计人员无需知悉 | 整行删除 |
| D3 | `pages/archive-arrange/MetadataEntryModal.tsx:680` | 页头灰字「元数据依据 DA/T 94-2022 件级必填项白名单」 | 标准编号对录入无操作价值 | 整行删除 |
| D4 | `pages/archive-utilization/ApprovalCenterPage.tsx:308` | 常驻黄条「动态路由规则：仅在线浏览 → 部门经理+档案管理员；含下载/打印/实体外借 → 升级财务总监；涉密…→ 强制 HRVP 会签」 | 审批人每天看，规则已成习惯 | 删黄条，语义移入页头 Info 图标的 title（hover 可查） |

### 2.2 建议精简（P3，共 2 处）

| # | 位置 | 现状 | 建议 |
|---|---|---|---|
| S1 | `pages/archive-config/InspectionConfigPage.tsx:96` | 配置页顶部两行长说明（96 与 325 两处各一大段） | 各压缩为一行短句（配置页面向系统管理员，保留一行边界说明即可） |
| S2 | `pages/archive-config/manage/CodeConfigTab.tsx:276` | 「配置锁定规则：所有自定义规则需在本单位档案管理制度中书面固化…」 | 制度性提示，压成短句或移入 title |

### 2.3 明确保留（10 处，逐一复核过）

| 位置 | 内容 | 保留理由 |
|---|---|---|
| `VolumeWorkspacePage.tsx:1499/1504` | 移交确认弹窗「以服务端实际归盒为准」 | 操作当下的免责说明 |
| `CodeConfigTab.tsx:345` | 「无需额外编写系统档号。确认后案卷将标记为已确认…」 | 操作当下的行为预告 |
| `TransferManagePage.tsx:174` | 空态「暂无可移交案卷（案卷须先在组卷工作台完成移交归盒入库）」 | 空态引导，无数据时才出现 |
| `OpenApiReceivePage.tsx:492/240` | API 字段表说明列 / 「批次说明：{message}」 | 数据/接口契约文档 |
| `MyBorrowOrders.tsx:140` | 「事由说明：{order.reasonDetail}」 | 字段值 |
| `ApiReceivePage.tsx:50` | 采集方式下拉选项描述 | 操作当下的选项说明 |
| `InteractivePreview.tsx:304` | OFD 预览演示文案 | 组件演示用 |
| `DigitalWarehousePanel.tsx:576`、`OrgManagePanel.tsx:248` | 代码注释 | 不渲染 |
| `VolumeWorkspacePage.tsx:933` | 「待移交时自动执行四性检测」状态行 | 状态展示（本次已删其括注） |

---

## 三、建议的执行批次（待确认）

**P2（建议下批执行，影响面小、口径统一）**：
1. B1-B5 五处工具栏/页签行按同款三件套改造（flex-nowrap + overflow-x-auto no-scrollbar + 按钮 whitespace-nowrap shrink-0）；
2. D1-D4 四处解释性文案删除（D1/D4 语义移 title）。

**P3（可选）**：S1/S2 精简；B6 低风险按钮补 nowrap。

**验收方式**：与本次相同（tsc + check-typography + 相关冒烟 + 浏览器 DOM 单行实测 + 截图）。

**预估范围**：P2 约 6 个文件、纯前端样式与静态文案，无后端改动，单提交可回滚。

---

## 四、扫描脚本存档

- `scan-flexwrap.mjs`（flex-wrap 分类）：C:\Users\jyimp\.zcode\workspace\default\scan-flexwrap.mjs
- `scan-hints.mjs`（解释文案扫描）：C:\Users\jyimp\.zcode\workspace\default\scan-hints.mjs

---

## 五、执行记录（2026-09-07，用户批准"按照你的计划进行执行"）

### 改动明细（P2 + P3，共 10 个文件）

| 项 | 文件 | 实际改动 | 浏览器实测 |
|---|---|---|---|
| B1 | `components/FanzongManager.tsx` | 工具栏容器 flex-wrap→flex-nowrap+overflow-x-auto no-scrollbar；左侧按钮组 shrink-0；4 按钮（新建全宗/导出/排序/刷新）补 whitespace-nowrap shrink-0；搜索框 shrink-0 | ✅ 容器 flex-nowrap 生效、4 按钮齐全单行 |
| B2 | `pages/archive-config/manage/ArchiveManageConfigPage.tsx` | 页签栏容器 flex-wrap→flex-nowrap+no-scrollbar；分段器 shrink-0；页签按钮补 nowrap shrink-0 | ✅ flex-nowrap 条数 2（页签栏+元数据工具栏） |
| B3 | `pages/archive-config/manage/MetadataConfigTab.tsx` | 操作行同款 flex-nowrap+no-scrollbar；模式分段器 shrink-0、按钮 nowrap；右侧组 shrink-0、label nowrap | ✅ 模式按钮全带 nowrap shrink-0 |
| B4 | `components/portal/PortalSearchPage.tsx` | 检索模式行 flex-wrap→flex-nowrap+no-scrollbar；返回/分隔线/模式页签/右侧关键词注记 全部 nowrap shrink-0 | ✅ 门户检索页实测通过 |
| B5（定性修正） | `pages/archive-utilization/ApprovalCenterPage.tsx` / `my-borrow/MyBorrowOrders.tsx` | **审批链节点**（非页签）补 whitespace-nowrap shrink-0；容器保留 flex-wrap（长链在节点间换行是正确形态） | 页面渲染正常（无审批数据，节点行为代码级保证） |
| D1 | `pages/archive-arrange/QuickCheckPage.tsx` | 页头副标题删规则解释，保留「四性检测结果中心」 | ✅ subtitle=四性检测结果中心，规则文案不存在 |
| D2 | `pages/archive-arrange/MetadataEntryModal.tsx:615` | 删「…依据 DA/T 95-2022…落库 finance:srcDocExtFields」整段 | 代码级（弹窗需特定入口） |
| D3 | 同文件 ：680 | 删「元数据依据 DA/T 94-2022 件级必填项白名单」灰字 | 代码级 |
| D4 | `pages/archive-utilization/ApprovalCenterPage.tsx` | 删常驻黄条「动态路由规则…」；语义移入页头 Info 图标 span title（hover 可查）；lucide import AlertTriangle→Info | ✅ 黄条不存在、Info title 在位 |
| S1 | `pages/archive-config/InspectionConfigPage.tsx:96/325` | 两段长说明各压缩为一行短句 | ✅ 长句不存在、短句在位 |
| S2 | `pages/archive-config/manage/CodeConfigTab.tsx:276` | 配置锁定规则压缩为一句（保留后果说明，删制度说教） | ✅ 「书面固化」不存在 |

### B6 复核结论（无代码改动）

- `RoleManagePanel.tsx:146/318/365`：标签与授权 **chips 选择器**，换行即设计形态；
- `StorageConfigPage.tsx:378`：库房头**文本信息行**，合理换行。
- 结论：B6 所列两处均属 1.1 表中 TEXT/FILTER 的合理换行范畴，**维持原样**，不计入改造。

### 验收结果（2026-09-07）

- `npm run smoke:fe` 四联：check-typography **0 error/0 warning**、tsc 0 错、vitest **174/174**、build 成功（31.5s）
- 浏览器实测：B1/B2/B3/B4/D1/D4/S1/S2 共 8 项页面级验证通过（Vite :5000 + 后端 :8081 实环境）；B5/D2/D3 为代码级确认
- 提交：见 git log（P2+P3 单提交，可整体 revert）
