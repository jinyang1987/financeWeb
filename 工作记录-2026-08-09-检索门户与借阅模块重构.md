# 工作记录 - 2026-08-09 - 检索门户（前台）与借阅模块重构

> 本文档记录本次「检索门户 + 借阅模块逻辑修正」的设计与实现，供后续维护与验收参考。

## 一、需求背景

用户反馈：当前系统登录后只有「后台管理」一个入口，借阅模块存在逻辑问题。需求如下：

1. **新增检索门户（前台页面）**：类似百度搜索页，提供多维检索（参考后台已有检索维度），
   检索档案库中的凭证，点击进入可看详情，并按权限查看附件。
2. **前后端互切**：后台右上角可点「进入检索门户」，前台也可点「进入后台管理」，逻辑严谨。
3. **借阅模块修正**：后端的「档案利用 → 借阅」定位为审核/审批（审批中心/借阅管理），
   「我的借阅」负责申请/跟踪/在线调阅，需修正其中的逻辑问题。
4. **推送到 GitHub 仓库** `https://github.com/jinyang1987/financeWeb.git`。

## 二、现状梳理

- 前端 `financeWeb/`（React19 + Vite + TS + Zustand），后端 `ams-server/`（Spring Boot 3 + PG），
  另有 Express 中间服务（目录配置）。
- 前端已有完整的借阅模块：`我的借阅`（借阅车/我的申请/在线调阅）、`审批中心`、`借阅管理`、
  `借阅台账`、`借阅统计`；检索页已有：凭证检索 / 事项检索 / 附件检索 / 关联查询 / 审计追踪。
- 数据源：已全部切到 ams-server 真后端（件域 /records、卷域 /volumes、盒域 /boxes、
  原始凭证 /source-docs、借阅 /borrow、认证 /auth）。

## 三、本次改动清单

### 后端（ams-server，Java）

1. **借阅撤销端点**（`BorrowController` / `BorrowService`）
   - 新增 `POST /borrow/orders/{id}/cancel`：申请人本人撤销「审批中」的借阅单。
   - 服务端按会话（X-User-Id / X-Alfresco-Ticket）校验身份，仅申请人本人可撤销，防止他人代撤（安全）。
   - 撤销后主单状态置为 `terminated`，并写操作日志。
2. **原始凭证附件内容读取端点**（`SourceDocController` / `SourceDocService`）
   - 新增 `GET /source-docs/{docId}/content`：读取原始凭证节点内容（预览 inline / 下载 attachment）。
   - 以登录用户 ticket 走 Alfresco 读取，权限生效；带 `Content-Disposition`（UTF-8 文件名）。

### 前端（financeWeb）

1. **检索门户（前台）** 新增目录 `src/components/portal/`
   - `PortalApp.tsx`：门户骨架，顶栏含「检索」「我的借阅」「进入后台管理」「用户/退出」。
   - `PortalHomePage.tsx`：百度式大搜索框 + 快捷分类（全部/凭证/账簿/报表/其他）+ 数据概览。
   - `PortalSearchPage.tsx`：多维检索结果页（关键词/类别/年度/金额范围），列表展示档案状态标签
     （电子版可用/实体在库/实体借出）。
   - `PortalRecordDetail.tsx`：档案详情页，展示元数据 + 原始凭证附件清单；
     **附件权限门控**：当前用户对档案有生效电子授权（`activeElectronicGrants`）才可预览/下载；
     未授权则引导「加入借阅车 → 去结算」。
   - `PortalMyBorrow.tsx`：门户内嵌后台「我的借阅」（三 Tab 复用）。
2. **入口互切**
   - 后台 `Header.tsx` 右上角新增「检索门户」按钮 → `usePortalStore.switchMode('portal')`。
   - 门户顶栏「进入后台管理」→ `switchMode('admin')`。
   - `App.tsx` 按 `portalStore.mode` 渲染 `PortalApp` 或 `AppLayout`（共用登录态/数据源）。
3. **借阅撤销逻辑修正**
   - `borrowStore.cancelOrder` 由调用管理员 `terminateOrder` 改为调用新增的 `cancelOrderByApplicant`
     （申请人本人撤销，审批中），修正「我的借阅」撤销申请语义。
4. **新增 store / hook**
   - `src/stores/portalStore.ts`：端模式（admin/portal）+ 门户检索关键词/分类状态。
   - `src/hooks/usePortalData.ts`：门户数据装配（卷内件 ∪ 收集池件去重），并预加载卷/盒/附件/借阅。

### 验证

- `npx tsc --noEmit`：零错误。
- `npx vitest run`：130 用例全部通过。
- `npm run build`：构建成功。
- `ams-server`：`mvn -q -DskipTests compile` 编译通过（本环境已装 JDK17 + Maven 验证）。

## 四、权限设计说明

- 检索门户所有用户可用（登录后即可进入）。
- 档案详情附件预览/下载：**仅当借阅申请审批通过且电子授权生效中**（`fulfillment.status === 'granted'`），
  并按申请时勾选的权限（view/download）门控；未授权仅展示附件清单，不能预览/下载。
- 借阅撤销：仅申请人本人（审批中）可撤销，服务端会话校验。

## 五、遗留与后续

- 附件预览目前为「新窗口打开原文件」（inline）；如需统一水印预览可后续复用 `BorrowReadingRoom` 的水印容器。
- 检索门户首页的「我的借阅」与后台完全一致；如需门户精简版借阅流程可后续拆分。
- 门户搜索结果当前为前端内存过滤（与后台检索一致）；数据量大后可切换后端检索接口。
