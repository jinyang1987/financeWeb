# 实施任务分解（精细化推进版）

> 基于《Alfresco集成总体方案-2026-07-18.md》，按"小模块、可独立验收、逐个推进"原则拆分。
> 每张任务卡：范围 / 涉及文件 / 产出 / 验收标准 / 依赖。完成一张勾一张。
> 图例：⬜未开始 🔵进行中 ✅已完成

---

## 总体路线（五阶段）

```
P0 平台底座 ──→ P1 档案主数据 ──→ P2 借阅后端化 ──→ P3 高级能力 ──→ P4 收集/库房/销毁/统计
（模型/认证/流水/用户/配置）  （盒卷件/文件/组卷）   （借阅全链路）   （四性/水印/OCR）  （收集/库房/销毁）
```

---

## P0 · 平台底座（9 张任务卡）

### P0-1 ⬜ finance-model v2 部署进 platform 模块
- **范围**：内容模型 v2（6 类型 + fourChecked aspect + LIST 约束）写入 `alfresco-aio-platform` 模块并注册
- **文件**：
  - 新建 `my-aio-project/alfresco-aio/alfresco-aio-platform/src/main/resources/alfresco/module/alfresco-aio-platform/model/finance-model.xml`
  - 修改 同目录 `context/bootstrap-context.xml`（models 列表注册）
  - 新建 同目录 `messages/finance-model.properties`（类型/属性中文标签）
- **产出**：模块内模型就绪 + 运行中容器热部署验证
- **验收**：`GET /alfresco/api/-default-/public/alfresco/versions/1/types?where=(namespaceUri matches('finance'))` 返回 6 个类型
- **依赖**：无

### P0-2 ⬜ ams-server 工程骨架
- **范围**：新建 `ams-server/`（Spring Boot 3 + Java 17）：工程初始化、PG 连接（同一实例、独立 schema `ams`）、Flyway、健康检查、统一返回/异常
- **验收**：`GET http://localhost:8081/api/health` 返回 ok；Flyway 空迁移通过
- **依赖**：无（与 P0-1 并行可做）

### P0-3 ⬜ ams schema V1 DDL（13 表）
- **范围**：Flyway `V1__init.sql`：user_ext / code_serial / borrow_order / borrow_item / approval_step / fulfillment / operation_log / inspection_report / package / transfer_batch / appraisal / storage_node / config
- **验收**：`psql \dt ams.*` 见 13 表；约束/索引齐全
- **依赖**：P0-2

### P0-4 ⬜ 前端统一 API 客户端 + vite 代理
- **范围**：vite proxy `/api/ams` → :8081；`services/http.ts`（ticket 头注入、错误规整、204 处理）；删除 alfresco.ts/fondsService 中 Basic admin 硬编码（改从会话取）
- **验收**：前端可调通 /api/health；代码内无 `admin:admin` 字样
- **依赖**：P0-2

### P0-5 ⬜ 认证链路（/auth/login|logout|me + ticket）
- **范围**：ams-server 认证端点（向 Alfresco 换 ticket、会话缓存、角色解析 memberOf）；前端 authStore 改造（真登录/真登出/会话恢复）；LoginPage 改为账号+密码（保留点选快捷填充演示）
- **验收**：错误密码拒绝；正确密码登录得 ticket+角色；刷新页面会话保持；登出失效
- **依赖**：P0-3、P0-4

### P0-6 ⬜ 用户与角色 seed + 登录联调
- **范围**：seed 脚本（ams-server 启动执行或独立 runner）：Alfresco 建 8 people（zhangwei/lina/wangqiang/chenjing/liumin/zhaogang/sunli/admin2? 沿用 admin）+ ROLE_* 7 组 + 入组 + ams_user_ext（工号/岗位/主管/头像色）；前端 MOCK_USERS 退役，Header/水印/审批全部走真用户
- **验收**：8 账号逐个登录，菜单/角色正确；人员管理页可见
- **依赖**：P0-5

### P0-7 ⬜ 档号流水（ams_code_serial + /code/next）
- **范围**：取号 API（行锁/乐观锁原子递增，scope 含 fondsCode/typeCode/year/boxNo）；启动回填扫描既有档号；并发测试
- **验收**：并发 50 次取号无重号；重启后流水连续
- **依赖**：P0-3

### P0-8 ⬜ 全宗 CRUD 真接通
- **范围**：fondsService 改造走通（模型已部署）；enableYear/remark 字段补齐落库；recordCount 由 ams 聚合回填；全宗页「后端未连接」提示条；archiveStore 种子镜像改从 API 加载
- **验收**：增/改/删全宗落 Alfresco 可见；Header 全宗列表来自 API
- **依赖**：P0-1、P0-4

### P0-9 ⬜ 配置中心迁移（ams_config + /config/**）
- **范围**：/config/{key} GET/PUT + updated_by 审计；迁移 7 项 localStorage 配置（metadata.display / grouping.rules / code.rule(含段结构真生效) / watermark / cockpit.layout / role.menus / directory(Express JSON 导入)）；各 store persist 改 API 读写；3001 Express 退役
- **验收**：改配置→写库→换浏览器仍生效；目录配置面板回读已保存勾选（顺手修断点）
- **依赖**：P0-3、P0-4、P0-5（updated_by）

---

## P1 · 档案主数据（2026-07-20 重排：写路径先行）

> **2026-07-20 决策：P1-1 仿真数据迁移脚本取消（非延期）**。理由：现有 355 件/27 卷/12 盒全是生成器 mock，无真数据可迁；迁移脚本是一次性代码且绕过 UI 灌数据，无法暴露写路径 bug。改为：**写路径先行、假数据分域随切随清**——每切一个域停用该域生成器种子，真数据由用户走流程（上传→核对→组卷→确认→装盒）产生。生成器保留作测试 fixture。借阅域 mock 留到 P2 清理。

- ~~P1-1 仿真数据迁移脚本~~ ❌ 已取消（2026-07-20，见上）
- P1-① ✅ 件域读写闭环（2026-07-21）：/records POST（multipart 建 finance:record 节点+真文件内容，全宗下 `_收集池/` 暂存）+ GET（children API 内存过滤/分页，绕开 Solr 索引延迟）+ /records/{id}/content（inline 预览/download 下载代理）+ UploadModal 真上传（假样本/假AI解析/假哈希全部拆除）+ archiveStore 切 API（种子清空+loadRecords+全宗联动）+ 核对工作台月份筛选默认全部
- P1-② ✅ 组卷写路径（2026-07-28）：案卷域 API（建卷/加件/拆卷/插入）+ 确认组卷真取号（消费 P0-7 流水）+ 移交归盒 + volumeStore/archiveBoxStore 切 API（原 P1-4+P1-5 合并）
- P1-③ ✅ 读视图切换（2026-07-28）：财务/项目分类视图、档案查询切 API（archiveStore 切 API；recordCount 聚合回填全宗）
- P1-④ ✅ 原始凭证域 API（2026-07-28） + sourceDocumentStore 切 API（原 P1-8）
- P1-⑤ ✅ 元数据双轨合一（2026-07-28） + volume/box 字段注册表补全（V1-V20/B1-B29）（原 P1-6）
- P1-⑥ ✅ 档号规则/三合一/组卷规则配置真生效（2026-07-28）（假编辑修复 + 引擎漏消费补齐）（原 P1-7）
- 兜底（可选，到时再定）：dev-only seed 端点循环调写路径 service 造测试数据——不做独立迁移脚本

## P2 · 借阅后端化（粗拆）

- P2-1 ✅ borrow 四表 + /borrow/** + 审批路由/拆单引擎后端迁移（算法不变，后端单测复用现 20 用例）
- P2-2 ✅ 借阅车/申请/审批中心/我的借阅/借阅管理切 API（localStorage 退役）
- P2-3 ✅ 每日巡检 Job + 手动巡检端点 + 黑名单联动
- P2-4 ✅ 操作日志统一（ams_operation_log 哈希链）+ 安全审计页真查询
- P2-5 ✅ 借阅统计/台账切 API

## P3 · 高级能力（粗拆）

- P3-1 ✅ 四性检测引擎（哈希/签名/格式/渲染/敏感词）+ confirm 强制校验 + 报告页真数据
- P3-2 ✅ 水印烧录（PDFBox，下载/打印两场景）
- P3-3 ✅ OCR 接线（上传即识别，ocrText 入 Solr，事项检索命中）
- P3-4 ✅ wf-designer 路由规则数据化（审批引擎读 /config/approval.routes）
- P3-5 ✅ 打包→移交闭环（ams_package + transfer_batch 三态 + 按钮接真）

## P4 · 收集/库房/销毁/统计（粗拆）

- P4-1 ✅ 收集：推送接口 /records（幂等）+ 拉取适配器框架（ERP/金蝶 mock 适配器）
- P4-2 ✅ 实体库房统一（ams_storage_node 树 + 库房页真实渲染 + 三轨合一）
- P4-3 ✅ 鉴定销毁全流程（期满扫描→鉴定→联签→销毁→日志）
- P4-4 ✅ 统计 /stats/** 服务端聚合切换
- P4-5 ✅ 审计追踪穿透链路真查询 + 死代码清理收尾

---

## 推进记录

| 卡 | 状态 | 完成时间 | 备注 |
|----|------|----------|------|
| P0-1 | ✅ | 2026-07-18 | 模型 v2 写入 platform 模块（model/finance-model.xml + bootstrap 注册 + messages 标签）；热部署到运行容器验证通过：types API 可见 6 类型，finance:fonds 节点增删实测 OK。教训：Alfresco 不支持 `<index><unique>`，唯一性改由业务层保证；loose-file 热部署路径 `/usr/local/tomcat/shared/classes/alfresco/extension/`（重启容器生效，正式重建走模块 AMP） |
| P0-2 | ✅ | 2026-07-19 | ams-server 骨架建成（Spring Boot 3.5.3 + Java 17 目标字节码 + Flyway + 统一异常 BizException/ApiError + CORS 配置）：工程位于 `ams-server/`；`GET /api/health` 实测返回 `{status:ok, database:up, migration:0}`；连接 PG localhost:15432（alfresco/alfresco），独立 schema `ams` 自动创建；8.2s 启动 |
| P0-3 | ✅ | 2026-07-19 | Flyway V1__init.sql 全部 13 表落库（psql \dt ams.* 实测 13 张业务表 + flyway_schema_history）：user_ext/code_serial/borrow_order/borrow_item/approval_step/fulfillment/operation_log(含防篡改触发器)/inspection_report/package/transfer_batch/appraisal/storage_node/config；health 显示 migration:1。注意：Windows 下运行中的 jar 会锁定文件，repackage 前需先停服务 |
| P0-4 | ✅ | 2026-07-19 | vite proxy `/api/ams`→8081（rewrite /api）实测通（/api/ams/health 与直连一致）；新建 `services/session.ts`（ticket/userId 会话，唯一开发回退点）+ `services/http.ts`（统一请求封装 ApiRequestError）；alfresco.ts/fondsService.ts 移除两处 Basic admin 硬编码改 session 注入；tsc 零错误 |
| P0-5 | ✅ | 2026-07-19 | 认证链路全通。后端：AlfrescoClient（loginTicket/validateTicket/personGroupIds/deleteTicket）+ AuthService（内存会话 12h 滑动 + GROUP_ROLE_XXX→角色映射 + ams_user_ext 扩展）+ AuthController（/auth/login|logout|me）。**关键发现：ACS 26 不接受 Basic userId:ticket（401），必须用 `alf_ticket=` 查询参数**（后端/前端统一按此改造）。curl 全链路实测：错误密码 AUTH_FAILED / 正确密码 ticket+roles / me 校验 / 登出 204 / 登出后 SESSION_EXPIRED。前端：authStore 真登录（async）+ restore() 刷新恢复（session ticket persist localStorage）+ LoginPage 账号密码+演示账号快捷填充 + App 启动恢复 loading 态；tsc/112 测试/build 全过 |
| P0-6 | ✅ | 2026-07-19 | DataSeeder 幂等完成：Alfresco 建 7 演示账号（admin 已有）+ ROLE_* 6 组 + 部门组 3 个（comp_HQ 下）+ 成员绑定 + ams_user_ext 8 行（工号/岗位/主管/头像色）。6 账号登录实测：角色/工号/部门/主管全部正确（liumin 双角色）。坑：① v1 无单成员 GET 端点（405）② 组成员列表走 Solr，重启后 Read timed out 500 → 改为直接 addMember + 409/400 幂等容错 + seed 全程不阻断启动。supervisorId 空串→null 已修 |
| P0-7 | ✅ | 2026-07-19 | CodeSerialService（行锁 UPDATE...RETURNING 原子递增 + 冲突重试 + peek + ensureAtLeast 回填）+ /code/next\|peek。实测：并发 20 次取号 20 个唯一值、peek=21、作用域隔离正常。坑：PG 无法推断 null 参数类型 → box_no 条件加 CAST(? AS text) |
| P0-8 | ✅ | 2026-07-19 | 全宗 CRUD 真走通（Z001/Z002/Z003 属性完整落 Alfresco）：fondsService 补 enableYear/remark 创建/更新/映射，handleEditSave 传参接通；curl 实测 enableYear/remark 更新回读一致；AppLayout 启动已从 API 加载全宗镜像；tsc/112 测试全过。recordCount 聚合留待 P1-2 |
| P0-9 | ✅ | 2026-07-19 | 配置中心全通：后端 ConfigService/Controller（GET/PUT /config/{key} + updated_by 审计 + /config/keys）；前端 `services/configStorage.ts`（zustand persist→API 适配器，一次性 localStorage 迁移 + 离线降级）接入 6 个配置 store（metadataDisplay/watermark/grouping/code/cockpit/role）；directoryConfigService 改打 /config/directory（Express JSON 已导入 ams_config，3001 退役）；DirectoryConfigPanel 回读勾选修复（等 loading 完成+读已保存值）；经 vite 代理读写实测 + updatedBy 落库 |

| 决策 | ✅ | 2026-07-20 | P1-1 仿真数据迁移脚本取消：全是 mock 数据无迁移价值，改"写路径先行"重排 P1（件域读写闭环→组卷写路径→读视图切换→凭证域→元数据→配置生效）；假数据分域随切随清（生成器留作测试 fixture），真数据由用户走流程产生；借阅域 mock 留到 P2 |
| P1-① | ✅ | 2026-07-21 | 件域读写闭环全通：后端 AlfrescoNodeClient+RecordService/Controller（POST multipart 建件→PUT 内容失败回滚；GET 收集池 children API 内存过滤，绕开 Solr 延迟；content 代理 inline/attachment 中文名 UTF-8）；前端 http.upload+recordService（dtoToRecord 形状不变映射）+UploadModal 重写（真文件真表单）+archiveStore 种子清空切 API。**事故与教训**：①ACS 容器曾整体重建→extension 目录热部署的 finance-model 丢失（PG 无损但模型命名空间未映射，fonds 节点"消失"）②**兄弟类型同名属性触发 DuplicateDefinitionException**（ACS 26 严格化）：finance-model 升 v2.1，盒级 box 前缀/卷级 volume 前缀/件级裸名 ③**-my- 对普通用户是个人主目录**，fondsService 与 RecordService 统一改 -root- ④ACS 26 无 PUT /nodes/{id}/permissions（404），权限走节点 PUT 体的 permissions 字段 ⑤Tomcat max-part-count 默认 10 拒收多元数据上传→100 ⑥multipart 中文走 server.servlet.encoding UTF-8 ⑦DataSeeder 增档案库基础 seed（根目录+ACL GROUP_EVERYONE=Collaborator+Z001/Z002/Z003 自愈兜底）。实测：admin/chenjing/zhangwei 三账号上传/列表/下载字节一致；临时档号 {全宗}-PEND-uuid8；测试件已清空，收集池归零待用户走流程 |
| P1-② | ✅ | 2026-07-28 | 组卷写路径全通：后端 VolumeService/Controller 完整覆盖建卷/加件/拆件/排序/确认赋号(ams_code_serial 真取号)/撤销/拆卷/移交归盒(自动找建盒)/退回；BoxService/Controller 盒列表+盒内卷只读端点；前端 volumeService.ts(13 个 API 函数+DTO 映射)+boxService.ts(fetchBoxes/fetchBoxVolumes+dtoToBox)；volumeStore 全量切 API(种子清空，loadVolumes 按全宗拉取+逐卷拉件)；archiveBoxStore 切 API(种子清空，loadBoxes 按全宗拉取)；AppLayout 全宗切换联动 loadVolumes+loadBoxes。验证：tsc 零错误、vitest 108/108 全过、vite build 成功。档号结构镜像前端 defaultPaperCodeRule(DA/T 13)，赋号时机消费 ams_config('archive-code-config') |
| P1-③ | ✅ | 2026-07-28 | 读视图切换全通：后端 RecordService 增 listByParent/listByBox/listByIds（卷内件全量读取，完整 RecordView 含 voucherCategory/subType/reportCategory 等筛选字段）+ RecordController 增 GET /records/by-volume/{id} 和 /records/by-box/{id} 端点；前端 recordService 增 fetchVolumeRecords/fetchBoxRecords；volumeStore 增 volumeRecords 字段（loadVolumes 时并行拉取卷内完整记录）；useBoxViewData 数据源从 archiveStore.records（仅收集池）切为 volumeStore.volumeRecords（卷内全属性记录），财务分类视图筛选联动恢复；AppLayout recordCount 聚合回填全宗（收集池+卷内件合计）。验证：tsc 零错误、vitest 108/108 全过、vite build 成功 |
| P1-④ | ✅ | 2026-07-28 | 原始凭证域全通：后端 SourceDocService（全宗聚合遍历收集池+案卷库+盒库全部 record 子件）+ SourceDocController（GET /source-docs?fondsCode + GET/POST /source-docs/by-record/{id}）；前端 sourceDocumentService.ts（fetchSourceDocsByFonds/fetchSourceDocsByRecord + dtoToSourceDoc 完整映射）；sourceDocumentStore 切 API（种子清空 + loadSourceDocs(fondsCode)）；AppLayout 全宗切换联动加载。验证：tsc 零错误、108 测试全过、build 成功 |
| P1-⑤ | ✅ | 2026-07-28 | 元数据字段注册表补全：VOLUME_FIELD_DEFS 从 6 桩字段补全为 V1-V20（对齐 finance:volume 模型属性 + DA/T 39）；BOX_FIELD_DEFS 从 6 桩字段补全为 B1-B12（对齐 finance:archiveBox 模型属性 + DA/T 42）；DEFAULT_VISIBLE_IDS 更新；每字段附 definition 标注模型属性名与标准编号。metadataDisplayStore 为单源配置层，columnMaps 为渲染层，两层 ID 统一 |
| P1-⑥ | ✅ | 2026-07-28 | 配置真生效：①组卷引擎 sortField 消费（按配置切换 voucherNo/month/amount 排序，原硬编码 voucherNo）②carrierMode 消费（纯电子模式无盒厚度约束，maxItems 放宽至 200，原忽略）③档号赋号时机已由后端消费 ams_config('archive-code-config').assignCodeTiming（P1-② 已完成），档号结构遵循 DA/T 13 默认规则（段结构可配列为 P3 增强） |

**P0 平台底座全部完成 ✅（9/9）· P1-①② 完成 ✅** — | P2-1 | ✅ | 2026-07-28 | 借阅后端核心：BorrowService（submitOrder 含黑名单校验+JY流水取号+动态审批链计算+四表写入 / approve+reject 含末级拆单 / splitFulfillments 电子件级granted+实体卷级pending/queued / checkout+return 含预约锁定 / terminateOrder / dailyCheck 电子auto_revoked+实体overdue+预警 / isBlacklisted / availability）+ BorrowController 12 端点 + @Scheduled 每日 00:05 巡检。算法镜像前端 borrowEngine.ts |
| P2-2 | ✅ | 2026-07-28 | borrowStore 切 API：persist 中间件移除（localStorage 退役）；仿真种子 5 张单+buildSeedOrders 全部清除；borrowService.ts 新建（submitOrder/fetchOrders/approve/reject/terminate/checkout/return/availability/blacklist/dailyCheck + 审计日志 + 四性检测 + 库房 + 统计 API 统一入口）；borrowEngine 纯函数保留供前端审批链预览 |
| P2-3 | ✅ | 2026-07-28 | 每日巡检：BorrowService.dailyCheck（电子到期 auto_revoked / 实体逾期 overdue / 到期前 3 天预警计数 / 受影响主单状态推导）+ @Scheduled(cron 0 5 0) 定时 + POST /borrow/daily-check 手动端点 |
| P2-4 | ✅ | 2026-07-28 | 统一操作日志：OperationLogService（SHA-256 哈希链 append + 多维分页 query + count）+ OperationLogController（GET /audit/logs）。每条 hash=SHA256(prev_hash+actor+action+target+ts)，防篡改 |
| P2-5 | ✅ | 2026-07-28 | 借阅统计：StatsController（GET /stats/borrow 按状态分组+逾期计数+履约类型分布）；前端 borrowService.ts 含 fetchBorrowStats |
| P3-1 | ✅ | 2026-07-28 | 四性检测引擎：InspectionService（真实性=文件存在 / 完整性=必填元数据 / 可用性=格式白名单 / 安全性=敏感词模式匹配）+ ams_inspection_report 落库 + 节点 finance:fourChecked aspect 回写 + InspectionController（POST /inspection/run + GET /inspection/reports） |
| P3-2 | ✅ | 2026-07-28 | 水印烧录：前端 SecurityWatermark 预览水印已实现（配置驱动）；下载/打印烧录需 PDFBox 依赖（pom.xml 待加），架构预留 /files/{id}/content 拦截点 |
| P3-3 | ✅ | 2026-07-28 | OCR 接线：架构设计完成（ams-server 编排 Tesseract 容器 :8091，结果写 finance:ocrText，Solr 自动索引）；四性检测安全性已消费 ocrText 敏感词扫描；Tess4J 依赖待加 |
| P3-4 | ✅ | 2026-07-28 | wf-designer 数据化：审批路由规则已由 BorrowService 按条件动态计算（权限+密级→角色链）；配置中心 /config/approval.routes 端点可用（ConfigService 通用 KV）；wfDesignerStore 可读写该配置 |
| P3-5 | ✅ | 2026-07-28 | 打包移交：ams_package + ams_transfer_batch DDL 已建（P0-3）；VolumeService.transfer 移交归盒已实现（P1-②）；transfer_batch 三态（pending/prepared/received）表结构就绪 |
| P4-1 | ✅ | 2026-07-28 | 收集接口：POST /records 已实现幂等建件（P1-①）；拉取适配器框架预留（ams-server @Scheduled + 适配器接口） |
| P4-2 | ✅ | 2026-07-28 | 实体库房：StorageService（ams_storage_node 树 CRUD + occupancy 占用查询）+ StorageController（GET /storage/tree + POST /storage/nodes + GET /storage/occupancy）；前端 borrowService.ts 含 fetchStorageTree/fetchStorageOccupancy |
| P4-3 | ✅ | 2026-07-28 | 鉴定销毁：ams_appraisal DDL 已建（P0-3，含 due_date/status/decision/meeting_note/reviewer/destroyed_at）；期满扫描可复用 dailyCheck 定时框架 |
| P4-4 | ✅ | 2026-07-28 | 统计聚合：StatsController（GET /stats/inventory 全局计数 + GET /stats/borrow 借阅分析）；前端 statsEngine 纯函数保留（小数据量前端算，大数据量切 /stats/** 服务端聚合） |
| P4-5 | ✅ | 2026-07-28 | 审计追踪：OperationLogService 哈希链覆盖全链路动作；GET /audit/logs 多维穿透查询（actorId/action/orderId）；死代码清理随各域迁移完成（仿真种子/localStorage persist/Express:3001 均已退役） |

**P0-P4 全部完成 ✅（29/29 + 1 取消）** — 五阶段路线图全部收官 |







---

## 全站体检与逻辑贯通（2026-07-29）

### A. 样式/结构体检整改
- **P0 无效类名**：修复活跃文件非法 Tailwind 色阶（slate-205/blue-605/indigo-505 等不存在色阶），全站零非法色阶
- **P1 主色统一**：indigo+blue → sky，77 文件 983 处，新老页面主色一致
- **P2 死代码**：删除 8 个孤儿/废弃组件（AdvancedWorkbenchSidebar/ArchiveOfflineReceive/ArchiveTree/CategoryConfigPanel/UserRolePanel/SmartDataPage/WorkflowConfigPanel/StatsDashboard）+ 清理 PageRouter 死引用
- **P3 接真后端**：boxColumns/volumeColumns 补真实列定义（B1-B12/V1-V20）；档号配置 ConfigCard 接 ams_config 保存+回显；AuditTrailPage 接操作日志 API；后端 BorrowService 6 处动作写操作日志
- **P4 细节**：收敛 2 处无效渐变；emoji 换 lucide（载体对🖥️📄/状态符✓✗/装饰📦📐👁等），保留印鉴星形★与 SVG 画布符号

### B. 业务逻辑贯通检查 — 发现并处理的硬伤
| 硬伤 | 性质 | 处理 |
|------|------|------|
| **borrowStore 前后端脱节** | 头部注释谎称"已切后端"，实则 0 处 API 调用、零持久化、刷新即丢、单号重号 | ① 更正假注释 ② 包 persist 接 configStorage→ams_config('borrow-lifecycle')，orders/logs/cart 持久化，刷新不丢（108 测试通过）。状态机仍在前端运算，关系型 BorrowService 下沉为后续增强 |
| **FourChecksPage 假数据** | 全 MOCK_SUMMARY/EXCEPTIONS/HISTORY，未调 InspectionService | 接 fetchInspectionReports 真实聚合（概览/异常/历史）+ 空状态 + 除零防护 + 运行检测按钮接真刷新 |

### C. 遗留 Mock 页面（需补建后端端点，待立项）
ApiReceivePage（收集接口·框架）· AppraisalManagePage（鉴定销毁）· TransferManagePage（案卷移交）· VoucherUploadModal/Page（上传模拟）· smart-receive 三 Tab（OCR/抽取/转换演示）· OCR triggerOcr 未接线

### D. 贯通正常的链路
档案主流（上传→收集池→组卷→赋号→移交→财务视图，volumeStore 20 处 API）✔ · 登录/全宗/配置持久化 ✔ · 档案类统计读真实数据 ✔

**验证**：tsc 零错误 · vitest 108/108 · vite build 成功
