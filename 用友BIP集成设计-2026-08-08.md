# 用友 BIP 集成设计（2026-08-08）

> 依据：《用友BIP_水利部接口标准操作手册.md》（7 接口已实测）+ 沙箱实测（2026-08-08）
> 状态：设计定稿，随实现进度更新

## 一、沙箱真实数据画像（实测，纠正手册）

| 接口 | 手册说法 | 实测结论 |
|---|---|---|
| 凭证列表 | "沙箱无凭证数据" | **有误**：不传期间 → recordCount=0；**传 periodStart/periodEnd=2024-01~2026-12 → 106 条**（2024-02~2024-12，每月 6~19 条；凭证字 转×97、记×9；状态全 01） |
| 凭证详情 | 404 示例 | 换真实 voucherId → 完整 header+bodies（科目 code/name、借贷原币、制单人 makerObj；审核人 auditorObj 多为 null） |
| 凭证附件 | — | 106 张凭证 attachedBill=3（附单据数），但 queryBusinessFiles **全空**（无电子文件） |
| 余额/发生报表 | items 空 | 实测 2024-03~2025-06 **items 全空**；2026-06 报 `code:0 "period not exist!"` |
| 账簿 | 1 个 | 北京同仁堂 code=0001 id=1971500567562289169 |
| 期间 | 576 条 | 2024-01 ~ 2025-12 |

**结论**：可真实归档的是 **106 张记账凭证**（含完整分录，无电子附件）；报表接口通但无数据（同步时如实记录 0，不伪造）。

## 二、总体架构

```
用友BIP沙箱(dbox网关)
   │  ① HmacSHA256签名 → token（缓存+过期重取）
   │  ② 账簿/期间/凭证列表/凭证详情/附件/余额表/发生表（7接口）
   ▼
ams-server yonyou 包（新增）
   ├─ YonyouClient        签名、token缓存、7接口封装、容错
   ├─ YonyouTransformer   转换层：用友JSON → 档案元数据 + 凭证版式PDF（PDFBox，CJK字体）
   ├─ YonyouSyncService   同步编排：批次/明细落库、幂等去重、可选自动组卷
   ├─ YonyouScheduler     月度自动归档（cron 配置驱动，默认每月1日02:30 同步上月）
   └─ YonyouController    /api/yonyou/** REST
   ▼
RecordService.create（复用 P1-① 建件链路）→ Alfresco finance:record（v2.2 新属性）
   ▼ （autoGroup 开启时）
VolumeService.create/addItems/confirm（复用 P1-② 组卷链路，确认时按配置真取号）
```

**边界不变**：前端只跟 ams-server 说话；Alfresco 管内容；ams 库管同步批次/明细/配置。

## 三、转换层映射（用友 → 会计档案）

| 用友字段（header/body） | 档案字段（finance:record v2.2） | 取值示例 |
|---|---|---|
| header.displayname | finance:voucherNo | 转-1 |
| header.vouchertype.voucherstr | finance:voucherWord | 转 |
| header.vouchertype.name | finance:voucherCategory | 转账凭证（与前端取值域一致：收款/付款/转账/通用/汇总/调整） |
| header.period | finance:period + year/month | 2024-02 → 2024/2 |
| header.maketime | finance:voucherDate (d:date) | 2024-02-08 |
| header.maker.name | finance:preparer | 张士航 |
| detail.auditorObj?.name | finance:auditor | 可空 |
| detail.tallyManObj?.name | finance:tallyMan | 可空 |
| header.totaldebit_org | finance:amount | 93835.00 |
| header.attachedbill | finance:attachedBillCount | 3 |
| body[]（recordnumber/description/accsubject.code+name/debit_original/credit_original） | finance:entries（JSON） + finance:accountSubject（科目去重汇总，检索维度） | 见下 |
| header.description | cm:description（Alfresco 标准属性=摘要） | 资本公积转增资本 |
| header.id | finance:externalId（幂等键） | 2602375342880194567 |
| 常量 | finance:archiveType=记账凭证、retention=30年、source=digital-native、carrierType=electronic、sourceSystem=用友BIP、recordStatus=仅件数据 | |

**entries JSON 结构**（检索/展示/重生成版式文件的唯一事实源）：
```json
[{"line":1,"summary":"资本公积转增资本","subjectCode":"400201","subjectName":"资本公积_资本（或股本）溢价","debit":93835.00,"credit":0}]
```

**版式文件**：每张凭证生成 `记账凭证-{凭证字号}-{期间}.pdf`（PDFBox + CJK 字体 msyh/simsun 逐级回退），内容=凭证头（字号/日期/期间/制单/审核/记账/附单据数/来源系统）+ 分录表 + 借贷合计（大写）。**理由**：沙箱无电子附件，件必须有内容文件（RecordService 校验 bytes 非空）；且电子会计凭证归档本就要求版式文件（79 号令/封装包规范）。

**幂等**：`ams_sync_item` 以 (item_type, external_id) 判重——已 success 的 voucherId 重跑即 skipped；本地 AtomicBoolean 锁防并发双跑。

## 四、会计期间月度自动归档

- 配置存 ams_config：`yonyou.connection`（gateway/appKey/appSecret/tenantId/accbookCode/fondsCode，secret 读时脱敏）、`yonyou.schedule`（enabled/cron/autoGroup/retention 默认）
- 调度：Spring `@Scheduled(fixedDelay=60s)` + `CronExpression.next()` 内存态计算下次触发点；默认 cron `0 30 2 1 * *`（每月 1 日 02:30）同步**上一会计期间**
- 手动同步：POST /yonyou/sync {period, autoGroup?}，用**触发用户 ticket** 建件（权限/审计正确）；自动用 seed admin
- autoGroup：同步完成后按"记账凭证+期间"建卷《{年}年{月}月记账凭证卷》→ addItems → confirm（消费赋号时机配置，on-confirm 时真取号）

## 五、报表归档策略（诚实版）

余额表/发生表按期间查询：items 非空 → 生成《科目余额表-期间.pdf》《利润发生表-期间.pdf》入档（archiveType=财务报告，reportCategory=内部管理，reportPeriod=月度）；items 空 → 批次如实记录 reportCount=0 + message"该期间无报表数据"（当前沙箱常态）。**不伪造空表归档。**

## 六、库表（Flyway V3）

- `ams_sync_batch`：batch_no(UK)/period/trigger_type(manual|auto)/status(running|success|partial|failed)/total/success/skip/fail/report_count/message/operator/started_at/finished_at
- `ams_sync_item`：batch_id(FK 级联)/item_type(voucher|report)/external_id/voucher_no/summary/amount/status(success|skipped|failed)/record_node_id/archive_code/error/created_at；部分唯一索引 `(item_type,external_id) WHERE status='success'` 兜底防重

## 七、前端（集成接口采集页重写）

ApiReceivePage 全部接真，删除 mock：
1. 数据源区：用友BIP 实时状态卡（test-connection）+ 其他源"未接入"灰态占位（不伪造已连接）
2. 手动同步卡：期间选择（/periods 拉真实期间 + 各期凭证数预览）+ 立即同步 + 本次自动组卷开关
3. 自动归档卡：开关 + cron 预设（每月1日/每月5日/每日/自定义）+ 下次执行时间 + 目标全宗
4. 连接配置抽屉：脱敏回显 + 测试连接按钮
5. 批次历史表（真表）：批次号/期间/触发/状态/总数/成功/跳过/失败/耗时/操作人/时间；行展开 → 明细表（凭证号/摘要/金额/状态/档号/错误）——异常队列即明细 failed 筛选，不再单独造假

## 八、详情面板表格化（第二件事）

普查结论（全站**无任何共享详情组件**，5 种键值风格并存）：
- 新建 `components/common/DetailTable.tsx`：`<DetailSection>` 分区 + `<FieldGrid fields columns>`（真 table，label 灰底列 + value 列，支持 mono/badge/span）+ `<DetailRows>`（明细行集合表）
- 改造优先级：① RecordDetailPanel（4 页面复用，一处改全生效）② smart-receive 三面板（ExtractDetailPanel 字段卡片→表格，头部抽共用）③ layout/DrawerPanel（4列色块最乱）④ SourceDocDrawer 统一
- 复用 `components/ui/table.tsx` 原语与 metadataDisplayStore 可见字段配置
