# 工作记录-2026-08-19-智能组卷凭证附件单元化修正

## 一、问题背景

用户指出：会计凭证归档时，记账凭证与原始凭证的组合核心遵循「业务依据对应 + 编号顺序排列」双重规则，即：

> **1 张记账凭证 + 其 N 个原始凭证附件 =【一件】；诸多【件】按记账凭证编号顺序排列成【一卷】。**

对照《会计基础工作规范》要求，发现当前智能组卷引擎（`volumeStore.ts` 的 `generateRecommendations`）存在实质偏差：
- 引擎只把「单张记账凭证」当作一个独立的"件"来排，**未把"记账凭证 + 其全部原始凭证"当做一个业务单元来整体核算**；
- 件数/页数/容量估算只按凭证张数，未计入原始凭证附件 → 装盒容量校验、卷内件数/页数统计偏小；
- 独立『原始凭证』被注册为独立档案类型，存在被单独分桶、单独成卷、与其对应记账凭证拆开的隐患；
- 附件从属/归集/拆分四类细化规则缺失。

## 二、修正方案（逻辑严谨，已落地 4 步）

### 第 1 步：让引擎"看见附件"（核心）
在 `generateRecommendations` 生成推荐前，从 `sourceDocumentStore` 按 `parentRecordId` 聚合每张记账凭证的富元数据原始凭证附件，构建 `sourceDocsByParent` 映射；同时识别待组卷池内属主在本池内的独立『原始凭证』记录，构建 `orphanSourceByParent` 映射。

### 第 2 步：件数/页数按"凭证+附件"真实核算
新增 `unitStats(chunk)` 辅助函数，统一按单元口径统计：
- `recordIds` = 凭证 + 池内独立原始凭证（需随卷移动的全部记录 id）；
- `items`（预估件数）= 凭证数 + 池内原始凭证数 + 富元数据附件数；
- `pages`（预估页数）= 全部单件页数之和（凭证缺省 2 页、附件按 `attachmentCount` 计，缺省 1 页）。

凭证类、非凭证类的推荐生成均改用 `unitStats` 口径。

### 第 3 步：原始凭证独立类型加强制合并铁律
在分桶（Step 1）处：凡 `archiveType='原始凭证'` 且其 `parentRecordId` 属主在本池内的记录，**一律跳过独立分桶**，随父件整体归卷，禁止单独成卷。

### 第 4 步：加件入卷保持单元完整性
`unitStats` 在拼装 `recordIds` 时把池内独立原始凭证紧随其父凭证加入同一推荐（同一卷），保证"件"以整体进入卷、不做半件；
富元数据附件为凭证节点子节点，Alfresco 移动节点时随父节点自动移动（后端 `addItems` 走 `moveNode` 保持子树），无需额外列入 `recordIds`。

> 编号顺序排列（按 `voucherNo` 升序 + 连续号段切分）经验证正确，未改动。

## 三、改动文件

| 文件 | 改动 |
|------|------|
| `financeWeb/src/stores/volumeStore.ts` | 引入 `useSourceDocumentStore`；`generateRecommendations` 新增附件聚合、强制合并铁律、`unitStats` 单元化统计，凭证/非凭证分支改按单元口径 |
| `financeWeb/src/__tests__/volumeGroupingFlow.test.ts` | 新增 2 个回归用例：①富元数据附件计入件数/页数且不进入 recordIds；②池内独立原始凭证随父件归卷禁止单独成卷 |

## 四、验证结果

| 检查项 | 结果 |
|--------|------|
| `npx tsc --noEmit` | ✅ 零错误 |
| `npx vitest run` | ✅ 139 用例全部通过（含新增 2 个单元化回归用例） |
| `npm run build` | ✅ 构建成功 |

## 五、遗留事项（后续可迭代）

- 四类附件从属细化规则（汇总归集、一单多证拆分、附件另存可追溯、结账/更正错账不附原始凭证）尚未做成配置项接入引擎，可在 `volumeGroupingStore.ts` 的 `PerTypeRule` 增加 `attachmentRules` 配置后迭代。
