# Alfresco 集成总体方案

> 会计档案管理系统 × Alfresco Content Services
> 版本：v1.0 · 2026-07-18
> 现状基线：《主线业务逻辑全景-2026-07-18.md》

---

## 一、目标与原则

### 1.1 目标
将当前"前端仿真数据驱动"的会计档案系统，改造为 **Alfresco 平台承载（内容/组织/检索/转换）+ 自有业务服务承载（状态机/事务/配置/日志）** 的真实三层架构系统，前端逐模块从 mock/persist 迁移到真实 API。

### 1.2 原则
1. **平台能力复用优先**：内容本体、版本、预览转换、全文检索、组织人员，一律用 Alfresco 现成能力，不自造轮子
2. **状态机不进 Alfresco**：借阅单/审批/履约/预约/流水号/队列/配置/日志放自有业务库（关系表），不扭曲 Alfresco 节点模型
3. **前端单一入口**：前端只调业务服务 `ams-server`，由它代理/编排 Alfresco 调用，不直接跨域连多个后端
4. **渐进可验证**：每阶段产出可独立验收，前端 store 按统一模式逐个迁移（services 层适配，页面零改或微改）
5. **合规可追溯**：档号 DA/T 13、元数据 DA/T 94、四性 DA/T 70、封装 DA/T 48、保管期限 79 号令，全部落模型与表结构

---

## 二、现状对接面盘点

| 能力 | 现状 | 结论 |
|------|------|------|
| Alfresco 平台部署 | ACS Community 26.1.0 + Share + Solr 2.0.15 + PG 16.5 + ActiveMQ + Transform 2.6.0（7 容器在跑） | ✅ 可用 |
| 自定义模型 finance:fonds/category | XML 已写但**未部署**（types API 实测 0 条），fondsService 必失败 | ✘ **最大阻塞项** |
| 组织（单位/部门） | Alfresco Groups `org_root→comp_*→dept_*` 已有真实数据（comp_HQ/comp_south） | ✅ 已通 |
| 人员 | Alfresco People 已通（zhangsan），PeopleService CRUD 封装完成 | ✅ 已通 |
| 全宗 CRUD | fondsService 代码就绪，待模型部署 | ⚠ 模型部署后即通 |
| 目录配置 | 自建 Express:3001 + JSON 文件 | ⚠ 过渡方案，应迁入业务库 |
| 认证 | 两处硬编码 `Basic admin:admin`，所有操作以 admin 身份执行（权限/审计失真） | ✘ 需改造 |
| 文件上传/下载/预览/rendition | 前端零代码 | ✘ 需新建 |
| Solr 检索 | SearchService AFTS 封装已写，无消费方 | ⚠ 待接 |
| OCR（Tesseract） | 容器空转，未注册为转换引擎 | ✘ 待接线 |
| 水印烧录 | 无后端（前端 preview 水印已实现） | ✘ 需新建 |
| 22 个业务 store | 无一引用 services/，全部 mock/localStorage | ✘ 迁移主体 |

---

## 三、总体架构与核心决策

### 3.1 目标架构

```
┌────────────────────────────────────────────────────────────────┐
│  financeWeb（React 前端）                                      │
│  stores 只依赖 services/*.ts，不直连 Alfresco                  │
└──────────────────────┬─────────────────────────────────────────┘
                       │ REST /api/ams/**（vite proxy → :8081）
┌──────────────────────▼─────────────────────────────────────────┐
│  ams-server（自有业务服务，Spring Boot 推荐）                  │
│  · 业务状态机（借阅/审批/履约/预约/组卷/移交/鉴定）            │
│  · 档号流水（事务+行锁）  · 配置 KV  · 操作日志（append-only） │
│  · 四性检测引擎  · 水印烧录（PDFBox） · OCR 编排               │
│  · 每日巡检 Job  · 统计聚合查询                                │
└───────┬──────────────────────────────┬─────────────────────────┘
        │ REST v1（ticket 透传）        │ JDBC
┌───────▼────────────────┐   ┌─────────▼─────────────────────────┐
│  Alfresco ACS 26.1     │   │  PostgreSQL 16.5（同一实例）      │
│  · finance 内容模型     │   │  ├─ alfresco schema（平台自有）   │
│  · 文件本体+版本+预览   │   │  └─ ams schema（业务库，本方案）  │
│  · 组织/人员/组         │   └───────────────────────────────────┘
│  · Solr 检索            │
│  · Transform（OFD/PDF） │
└────────────────────────┘
```

### 3.2 核心决策（三大选型）

| 决策 | 选项 | 结论 |
|------|------|------|
| **D1 业务逻辑放哪** | A. Alfresco repo-tier webscript（ACS 内 Java）<br>B. 独立业务服务（Spring Boot）<br>C. 独立业务服务（Node/Express，沿用 3001 先例） | **推荐 B**：借阅/流水/巡检等事务密集，独立服务不污染 ACS 部署物、可独立扩缩与调试；A 把业务耦合进 ACS 生命周期，C 也可行但水印烧录/与 Alfresco 同栈生态弱于 B。**待用户确认** |
| **D2 档案实体建模** | A. 全部 Alfresco 节点（盒/卷/件/原始凭证）<br>B. 元数据全进业务表，Alfresco 只存文件<br>C. 节点树存结构+档案元数据，业务表存状态机 | **推荐 C**：盒/卷/件/原始凭证用 Alfresco 自定义类型节点（目录树即保管视图，Solr 免费索引元数据）；借阅/审批/流水/日志等业务状态放 ams 表——各取所长 |
| **D3 用户体系** | A. Alfresco People 为主 + ams_user_ext 扩展<br>B. 完全自建用户表 | **推荐 A**：组织/人员已在 Alfresco，演示 8 账号 seed 进 People，工号/主管/角色扩展进业务表；避免双体系 |

### 3.3 为什么不是"全塞 Alfresco"
档号流水要求**原子单调递增**（并发组卷不能重号）——节点模型无此类原语；借阅履约是**多步状态机+队列**——关系表一行一锁最自然；统计是**多维聚合**——SQL GROUP BY 远优于遍历 Solr。强行全节点化会导致平台层大量别扭代码，违背原则 2。

---

## 四、认证与权限设计

### 4.1 认证流程（Alfresco Ticket 模式）

```
登录页(账号+密码)
  → POST /api/ams/auth/login {account, password}
  → ams-server 向 Alfresco POST /tickets {userId, password} 换 ticket
  → 校验成功：查 memberOf → 角色集；查 ams_user_ext → 工号/主管
  → 返回 {ticket, user, roles}
前端后续请求：
  → /api/ams/**  Header: X-Alfresco-Ticket + X-User-Id
  → ams-server 校验 ticket（调 Alfresco /tickets/{id} 验证或本地缓存会话）
  → 需要调 Alfresco 时代理：Authorization: Basic base64(userId:ticket)
```

- 替代 Basic admin:admin 硬编码（alfresco.ts:14、fondsService.ts:17 全部移除）
- 登出：DELETE /tickets/-me-
- 会话超时：Alfresco ticket 默认 1h，ams-server 侧做滑动刷新

### 4.2 组织/角色落位

| 实体 | 落点 | 说明 |
|------|------|------|
| 单位/部门 | Alfresco Groups（沿用 `org_root→comp_*→dept_*`，已有数据） | 不动 |
| 人员 | Alfresco People（seed 8 个演示账号：zhangwei/lina/wangqiang/chenjing/liumin/zhaogang/sunli/admin） | seed 脚本创建并加入部门组 |
| 角色 | Alfresco Groups `GROUP_ROLE_EMPLOYEE / _DEPT_MANAGER / _ARCHIVIST / _ARCHIVE_DIRECTOR / _CFO / _HRVP / _ADMIN` | 登录时 memberOf 映射 |
| 人员扩展 | `ams_user_ext`（工号/岗位/直属主管/头像色） | People 无这些字段 |
| 角色→菜单矩阵 | `ams_config('role.menus')`（现 roleStore 迁移） | 管理页改它 |
| 节点级权限（进阶） | Alfresco 节点 ACL：按部门组授权 fonds 子树 | P4 阶段 |

---

## 五、Alfresco 内容模型设计（finance-model v2）

> 在现有 `finance-model.xml` 基础上扩展，部署方式改为**打进 my-aio-project/alfresco-aio-platform 模块**（不再是 loose-file），随平台 AMP 发布。

```xml
<model name="finance:contentModel" xmlns="http://www.alfresco.org/model/dictionary/1.0">
  <namespaces>
    <namespace uri="http://www.finance.org/model/content/1.0" prefix="finance"/>
  </namespaces>

  <constraints>
    <constraint name="finance:typeCodeList" type="LIST">
      <parameter name="allowedValues"><list><value>KP</value><value>KB</value><value>FB</value><value>QT</value></list></parameter>
    </constraint>
    <constraint name="finance:retentionList" type="LIST">
      <parameter name="allowedValues"><list><value>永久</value><value>30年</value><value>10年</value></list></parameter>
    </constraint>
    <constraint name="finance:securityList" type="LIST">
      <parameter name="allowedValues"><list><value>普通</value><value>内部</value><value>秘密</value><value>机密</value></list></parameter>
    </constraint>
    <constraint name="finance:carrierList" type="LIST">
      <parameter name="allowedValues"><list><value>electronic</value><value>paper</value><value>mixed</value></list></parameter>
    </constraint>
    <constraint name="finance:recordStatusList" type="LIST">
      <parameter name="allowedValues"><list><value>仅件数据</value><value>待审核</value><value>已组卷</value></list></parameter>
    </constraint>
  </constraints>

  <types>
    <!-- 全宗（沿用现有，补两个属性） -->
    <type name="finance:fonds">
      <parent>cm:folder</parent>
      <properties>
        code/fondsName/status/address/syncSource/custodianCode/companyId（现有）
        + finance:enableYear(d:int)   启用年度
        + finance:remark(d:text)      备注（历史沿革）
      </properties>
    </type>

    <!-- 门类（沿用现有） -->
    <type name="finance:category">
      <parent>cm:folder</parent>
      <properties>categoryCode/categoryName/sortOrder/properties(JSON)</properties>
    </type>

    <!-- 档案盒 B1-B29 盒级元数据（DA/T 39+42+94） -->
    <type name="finance:archiveBox">
      <parent>cm:folder</parent>
      <properties>
        finance:boxNo(d:text,mandatory)        盒号
        finance:boxName(d:text)                盒名称
        finance:typeCode(d:text,constraint=finance:typeCodeList)
        finance:location(d:text)               存放位置（柜-架-层）
        finance:retention(d:text,constraint=finance:retentionList)
        finance:year(d:int)
        finance:boxStatus(d:text)              active/sealed/stored/destroyed
        finance:securityLevel(d:text,constraint=finance:securityList)
        finance:volumeCount(d:int)
      </properties>
    </type>

    <!-- 案卷 V1-V20 卷级元数据（DA/T 94） -->
    <type name="finance:volume">
      <parent>cm:folder</parent>
      <properties>
        finance:volumeCode(d:text,mandatory)   卷级档号（DA/T 13）
        finance:title(d:text,mandatory)        案卷题名
        finance:typeCode / retention / retentionCode / year
        finance:volumeStatus(d:text)           draft/confirmed/transferred/destroyed
        finance:totalItems(d:int) / totalPages(d:int)
        finance:carrierType(d:text,constraint=finance:carrierList)
        finance:securityLevel
        finance:cabinetNo(d:text) / shelfNo(d:text)   柜号/架号
        finance:dateFrom(d:date) / dateTo(d:date)
        finance:digitalHash(d:text)            数字化副本整体哈希
        finance:createdBy(d:text) / createdDate(d:date)
      </properties>
      <associations>
        <child-association name="finance:volumeItems">
          <target><class>finance:record</class></target>
        </child-association>
      </associations>
    </type>

    <!-- 件（档案记录，DA/T 94 件级 M 系列 + 业务扩展） -->
    <type name="finance:record">
      <parent>cm:content</parent>   <!-- 节点本体即主件电子文件 -->
      <properties>
        finance:archiveCode(d:text,mandatory)  件级档号（唯一索引）
        finance:voucherNo(d:text)              凭证字号（M31）
        finance:archiveType(d:text)            档案类型（M14）
        finance:department(d:text)             责任部门（M13）
        finance:amount(d:double)               金额合计（M35）
        finance:year(d:int) / month(d:int)     会计年度/月份（M11/M41）
        finance:retention / recordStatus(constraint)
        finance:voucherCategory(d:text)        收款/付款/转账/通用
        finance:subType(d:text)                总账/明细账…
        finance:reportCategory(d:text) / reportPeriod(d:text)
        finance:preparer(d:text)               制单人
        finance:accountSubject(d:text)         会计科目
        finance:securityLevel
        finance:source(d:text)                 digital-native/digitized
        finance:carrierType
        finance:volumeItemNo(d:int)            卷内件号
        finance:numbered(d:boolean) / numberedDate(d:date)
        finance:storageLocation(d:text)        实体位置
        finance:ocrText(d:text)                OCR 全文（Solr 索引）
      </properties>
      <associations>
        <child-association name="finance:recordSourceDocs">
          <target><class>finance:sourceDocument</class></target>
        </child-association>
      </associations>
    </type>

    <!-- 原始凭证（子件级，96 类） -->
    <type name="finance:sourceDocument">
      <parent>cm:content</parent>   <!-- 本体即影像/电子文件 -->
      <properties>
        finance:documentNo(d:text,mandatory)   单据编号
        finance:docTypeCode(d:text) / docTypeName(d:text)
        finance:transactionDate(d:date)
        finance:amountLower(d:double) / amountUpper(d:text)
        finance:counterpartyName(d:text) / counterpartyTaxId(d:text)
        finance:counterpartyAddress(d:text) / counterpartyBankAccount(d:text)
        finance:summary(d:text)
        finance:businessCategory(d:text)       采购/销售/费用/资产/薪酬/存货/资金/结算/特殊
        finance:preparer / reviewer
        finance:attachmentCount(d:int)
        finance:parentVoucherNo(d:text)        所属记账凭证号
        finance:attachmentSequence(d:int)      附件顺序号
        finance:extFields(d:text)              类型扩展字段 JSON
      </properties>
    </type>
  </types>

  <!-- 四性检测结果（可附加到 record/volume） -->
  <aspects>
    <aspect name="finance:fourChecked">
      <properties>
        finance:checkReal(d:boolean) / checkComplete(d:boolean)
        finance:checkUsable(d:boolean) / checkSafe(d:boolean)
        finance:checkedAt(d:datetime)
        finance:checkReportRef(d:text)         ams_inspection_report.id
      </properties>
    </aspect>
  </aspects>
</model>
```

### 5.1 仓库目录结构（节点树 = 保管视图）

```
/Company Home/会计档案管理/                     ← 已存在
  Z001 (finance:fonds)                        ← 全宗
    KP (finance:category)                     ← 门类
      2025 (cm:folder)                        ← 年度
        BOX-2025-KP-001 (finance:archiveBox)  ← 盒
          Z001-KU·01·2025-D30-0001 (finance:volume)  ← 卷
            记-001.ofd (finance:record)       ← 件（本体=主件）
              发票001.pdf (finance:sourceDocument)  ← 原始凭证（本体=影像）
      2026/...
    KB / FB / QT /...
  Z002 (finance:fonds) /...
```

**好处**：财务分类视图 = 直接 children 查询；借阅定位 = 节点路径直达；Solr 自动继承层级与元数据索引。

---

## 六、自有业务库表设计（schema `ams`）

> 与 Alfresco 共用 PG 16.5 实例，独立 schema，**绝不改 Alfresco 自有表**。DDL 由 ams-server Flyway 管理。

### 6.1 组织扩展
```sql
ams_user_ext (
  user_id        text PRIMARY KEY,        -- = Alfresco people.id
  emp_no         text NOT NULL,           -- 工号
  position       text,                    -- 岗位
  dept_path      text,                    -- 部门全路径（冗余展示）
  supervisor_id  text,                    -- 直属主管（逾期抄送）
  avatar_color   text,
  updated_at     timestamptz
)
```

### 6.2 档号流水（核心，解决重号）
```sql
ams_code_serial (
  id             bigserial PRIMARY KEY,
  scope          text NOT NULL,           -- 'BOX' | 'VOLUME' | 'ITEM'
  fonds_code     text NOT NULL,           -- Z001
  type_code      text NOT NULL,           -- KP/KB/FB/QT
  year           int  NOT NULL,
  box_no         text,                    -- ITEM 段的父盒作用域
  next_value     int  NOT NULL DEFAULT 1,
  version        bigint NOT NULL DEFAULT 0, -- 乐观锁
  UNIQUE(scope, fonds_code, type_code, year, box_no)
)
-- 取号：UPDATE ... SET next_value=next_value+1, version=version+1
--      WHERE ... AND version=:v RETURNING next_value（失败重试）
-- 启动时按库内既有档号 max 回填（迁移兼容）
```

### 6.3 借阅全链路（4 表）
```sql
ams_borrow_order (
  id              uuid PRIMARY KEY,
  order_no        text UNIQUE NOT NULL,      -- JY-YYYY-NNNN（由 ams_code_serial 出号）
  applicant_id    text NOT NULL,             -- → ams_user_ext
  reason_type     text NOT NULL,
  reason_detail   text,
  start_date      date NOT NULL,
  end_date        date NOT NULL,
  status          text NOT NULL,             -- approving/rejected/fulfilling/active/returning/completed/terminated
  current_step    int  NOT NULL DEFAULT 0,
  created_at      timestamptz NOT NULL,
  updated_at      timestamptz NOT NULL
)
ams_borrow_item (
  id              uuid PRIMARY KEY,
  order_id        uuid REFERENCES ams_borrow_order ON DELETE CASCADE,
  record_node_id  text NOT NULL,             -- Alfresco finance:record 节点 id
  volume_node_id  text NOT NULL,             -- finance:volume 节点 id
  title           text NOT NULL,
  type_code       text NOT NULL,
  media_type      text NOT NULL,             -- electronic/paper/mixed
  security_level  text NOT NULL,
  stock_status    text NOT NULL,             -- 提交时快照
  perms           text[] NOT NULL DEFAULT '{}',   -- view/download/print
  physical_mode   text NOT NULL DEFAULT 'none'    -- none/original/copy
)
ams_approval_step (
  id              uuid PRIMARY KEY,
  order_id        uuid REFERENCES ams_borrow_order ON DELETE CASCADE,
  seq             int  NOT NULL,
  role            text NOT NULL,             -- dept_manager/cfo/hrvp/archivist
  assignee_id     text,
  status          text NOT NULL DEFAULT 'pending',  -- pending/approved/rejected
  acted_by        text, acted_at timestamptz, comment text,
  UNIQUE(order_id, seq)
)
ams_fulfillment (
  id              uuid PRIMARY KEY,
  order_id        uuid REFERENCES ams_borrow_order ON DELETE CASCADE,
  type            text NOT NULL,             -- electronic/physical
  status          text NOT NULL,             -- pending/granted/lent/queued/returned/auto_revoked/overdue/terminated
  volume_node_id  text NOT NULL,
  record_node_ids text[] NOT NULL DEFAULT '{}',
  physical_mode   text,
  start_date      date NOT NULL, end_date date NOT NULL,
  granted_at      timestamptz, lent_at timestamptz, returned_at timestamptz,
  operator_id     text
)
CREATE INDEX idx_fulfill_volume_status ON ams_fulfillment(volume_node_id, status);
```

### 6.4 操作日志（不可篡改，等保）
```sql
ams_operation_log (
  id          bigserial PRIMARY KEY,
  ts          timestamptz NOT NULL DEFAULT now(),
  actor_id    text NOT NULL, actor_name text, actor_role text,
  action      text NOT NULL,             -- 检索/查看/下载/打印/申请/审批/出库/归还/中止...
  target_type text, target_id  text, target_label text,
  order_id    uuid,
  detail      text,
  ip          inet,
  prev_hash   text,                      -- 哈希链（可选增强）
  hash        text                       -- sha256(prev_hash + 行内容)
)
-- 仅 INSERT，无 UPDATE/DELETE（数据库层 REVOKE）
```

### 6.5 四性检测
```sql
ams_inspection_report (
  id           uuid PRIMARY KEY,
  target_node  text NOT NULL,            -- Alfresco 节点 id
  target_kind  text NOT NULL,            -- record/volume/package
  phase        text NOT NULL,            -- pre-archive/archiving/transfer/long-term
  real         boolean, complete boolean, usable boolean, safe boolean,
  detail_json  jsonb NOT NULL,           -- 逐项检测明细（方法/时间/结论/失败原因）
  operator     text,
  created_at   timestamptz NOT NULL
)
```

### 6.6 封装包与移交
```sql
ams_package (
  id           uuid PRIMARY KEY,
  package_no   text UNIQUE NOT NULL,
  name         text NOT NULL,
  unit_kind    text NOT NULL,            -- volume/book/report-type-year/other
  volume_nodes text[] NOT NULL,
  manifest_xml text NOT NULL,            -- DA/T 48 封装说明
  checksum     text NOT NULL,
  status       text NOT NULL,            -- created/downloaded/transferred/received
  created_at   timestamptz, transferred_at timestamptz, received_at timestamptz
)
ams_transfer_batch (
  id           uuid PRIMARY KEY,
  transfer_no  text UNIQUE NOT NULL,
  from_dept    text, to_dept text, from_person text, to_person text,
  volume_nodes text[] NOT NULL,
  total_items  int,
  status       text NOT NULL,            -- pending/prepared/received（修复"待处理恒空"）
  transfer_date date, received_at timestamptz
)
```

### 6.7 鉴定销毁
```sql
ams_appraisal (
  id           uuid PRIMARY KEY,
  volume_node  text NOT NULL,
  due_date     date NOT NULL,            -- 保管期满日（retention 推算）
  status       text NOT NULL,            -- pending/reviewing/approved-destroy/retained
  decision     text,                     -- destroy/retain
  meeting_note text,
  reviewer     text, reviewed_at timestamptz,
  destroyed_at timestamptz
)
```

### 6.8 实体库房（三轨合一）
```sql
ams_storage_node (
  id          uuid PRIMARY KEY,
  parent_id   uuid REFERENCES ams_storage_node,
  kind        text NOT NULL,             -- room/cabinet/shelf/layer
  code        text NOT NULL,             -- 1号库房 / 柜A / 架3 / 层2
  name        text NOT NULL,
  sort        int DEFAULT 0,
  UNIQUE(parent_id, code)
)
-- 盒节点 finance:location 改为存 ams_storage_node.id（或路径）
-- DigitalWarehousePanel 按此树渲染真实占用（盒=ArchiveBox 节点 location 反查）
```

### 6.9 配置中心（替代全部 localStorage persist + Express JSON）
```sql
ams_config (
  key         text PRIMARY KEY,
  -- directory / metadata.display / grouping.rules / code.rule / retention.table
  -- inspection.plan / watermark / cockpit.layout / role.menus / report.config
  value_json  jsonb NOT NULL,
  updated_at  timestamptz, updated_by text
)
```
理由：配置是文档型、低频写、高频读，KV 避免每配置一表的 schema 膨胀；有审计字段。

---

## 七、业务服务与 API 分层（ams-server）

### 7.1 技术栈（按 D1 推荐 B）
Spring Boot 3 + Spring Data JPA（ams schema）+ Alfresco REST Client + PDFBox（水印烧录）+ Quartz/@Scheduled（巡检）+ Flyway（DDL 版本化）。沿用 `my-aio-project` 同 Maven 体系或独立 `ams-server/` 工程。

### 7.2 API 总表（/api/ams/**）

| 域 | 端点 | 说明 |
|----|------|------|
| 认证 | POST /auth/login · POST /auth/logout · GET /auth/me | ticket 换发/校验，返回 user+roles+ext |
| 组织 | GET /org/tree · GET /org/{id}/members · GET /roles | 代理 Alfresco groups/people + ams_user_ext |
| 全宗 | GET/POST/PUT/DELETE /fonds | finance:fonds CRUD + recordCount 聚合回填 |
| 档案-件 | GET /records（多维查询，走 Solr）· GET /records/{nodeId} · POST /records（上传建件）· PUT /records/{nodeId} | 元数据在 Alfresco，业务视图由 ams 拼装 |
| 档案-原始凭证 | GET /records/{id}/source-docs · POST /records/{id}/source-docs | 子节点 CRUD |
| 案卷 | GET /volumes · POST /volumes（建卷）· POST /volumes/{id}/items（加件）· DELETE /volumes/{id}/items/{recordId} · POST /volumes/{id}/confirm（赋号+确认）· POST /volumes/{id}/unconfirm · POST /volumes/{id}/transfer（归盒+写批次）· POST /volumes/return | 组卷全链路后端化（含四性强制校验） |
| 盒 | GET /boxes · GET /boxes/{id}/volumes · GET /boxes/{id}/items | 保管视图数据源 |
| 档号 | POST /code/next {scope,fonds,type,year,boxNo?} | 事务取号 |
| 借阅 | POST /borrow/orders · GET /borrow/orders?mine=&pendingForRole= · GET /borrow/orders/{id} · POST /borrow/orders/{id}/approve · /reject · /cancel · /terminate · POST /borrow/fulfillments/{id}/checkout · /return · GET /borrow/availability/{volumeNodeId} · GET /borrow/blacklist · POST /borrow/daily-check | 状态机全部在服务端 |
| 四性 | POST /inspection/run {nodeId,phase} · GET /inspection/reports?target= | 真检测引擎 |
| 文件 | POST /files/upload（multipart，建 content 节点+属性）· GET /files/{nodeId}/content（下载，水印烧录）· GET /files/{nodeId}/preview（rendition pdf 代理）· POST /files/{nodeId}/ocr | |
| 配置 | GET/PUT /config/{key} | 全部配置 KV |
| 统计 | GET /stats/inventory · /stats/lifecycle · /stats/utilization · /stats/compliance · /stats/cockpit | 服务端聚合（大数据量准备） |
| 审计 | GET /audit/logs（多维分页） | 只读 |
| 库房 | GET /storage/tree · POST /storage/nodes · GET /storage/occupancy | 三轨合一 |
| 巡检 | POST /admin/daily-check · GET /admin/jobs | 手动/定时 |

### 7.3 前端迁移模式（每个 store 同一套路）
```ts
// 现状：create((set) => ({ records: initialRecords, ... }))
// 目标：services/recordService.ts 包装 /records；store 保留形状与 action 签名，
//        内部改为调 service，成功后 set。页面组件零改动或仅改异步处理。
```
迁移顺序见 §九。

---

## 八、逐模块 CRUD 对接设计（前端 store → API → 落点）

| 模块 | 前端现状 | 迁移方式 | 数据落点 |
|------|----------|----------|----------|
| 登录/身份 | authStore persist + MOCK_USERS | authService.login → /auth/login；seed 8 账号进 Alfresco People | Alfresco People + ams_user_ext |
| 角色矩阵 | roleStore localStorage | GET/PUT /config/role.menus | ams_config |
| 单位/组织/人员 | api.ts→Alfresco Groups/People | 保持，改经 ams-server 代理（去掉前端 Basic admin） | Alfresco Groups/People |
| 全宗 | fondsService（模型未部署） | 部署 finance 模型后接通；recordCount 由 ams 聚合回填 | finance:fonds 节点 |
| 目录配置 | Express:3001+JSON | 迁 /config/directory（启动时导入现 JSON） | ams_config |
| 元数据显示 | metadataDisplayStore | /config/metadata.display | ams_config |
| 档号规则 | archiveCodeConfigStore（仅时机真） | /config/code.rule；**补真**：5 项可配置（段结构/流水位数/类别码/期限码）全部入库生效 | ams_config + ams_code_serial |
| 三合一表 | 静态文档页 | /config/retention.table；组卷/赋号引擎读取（期限推断不再硬编码） | ams_config |
| 组卷规则 | volumeGroupingStore | /config/grouping.rules；引擎补读 sortField/carrierMode/itemsPerBox | ams_config |
| 四性检测 | 全 mock | /inspection/run 真引擎（哈希/签名/格式/渲染/敏感词/病毒）+ confirm 强制校验 + 配置 /config/inspection.plan | ams_inspection_report + 节点 aspect |
| 水印 | preview 真，download/print 空 | 下载/打印经 /files/{id}/content 由 PDFBox 烧录；策略 /config/watermark | ams_config + 文件流 |
| 驾驶舱布局 | cockpitStore | /config/cockpit.layout | ams_config |
| 档案记录 | archiveStore 仿真 | GET /records 分页/筛选；UploadModal/VoucherUploadModal → POST /files/upload | finance:record 节点 |
| 原始凭证 | sourceDocumentStore 仿真 | /records/{id}/source-docs | finance:sourceDocument 节点 |
| 案卷/盒 | volumeStore/archiveBoxStore 仿真 | /volumes/** + /boxes/**；仿真数据一次性迁移脚本导入 Alfresco | finance:volume/archiveBox 节点 |
| 借阅 | borrowStore localStorage | /borrow/** 全链路；borrowEngine 路由/拆单逻辑移后端（算法不变，测试保留为后端单测） | ams_borrow_* |
| 案卷移交 | TransferManagePage 半真 | ams_transfer_batch 状态机 pending→prepared→received（修"待处理恒空"） | ams_transfer_batch |
| 档案打包 | packageStore 内存 | packageEngine 移后端；下载= manifest+内容打包 zip 流；移交→ ams_transfer_batch | ams_package |
| 档案移交 | 占位页 | 接 ams_package/ams_transfer_batch 全流程 | 同上 |
| 鉴定销毁 | 死表单 | ams_appraisal + 期满自动扫描 Job + 销毁联签流 | ams_appraisal |
| 实体库房 | 独立 mock | ams_storage_node 树 + 盒 location 反查渲染 | ams_storage_node |
| 统计 | statsEngine 前端算 | 数据量小可保留前端算（读 API 数据）；后续切 /stats/** 服务端聚合 | SQL 聚合 |
| 操作日志 | borrowStore.logs + 拼接 | 全系统统一写 ams_operation_log（哈希链）；安全审计页改真查询 | ams_operation_log |
| 审计追踪 | 单凭证 mock | 按 record 关联 sourceDocs+审批+借阅+日志穿透查询 | 各表联合 |
| 收集 | 纯展示壳 | 接口采集：POST /records（外部系统推送，带源系统标识+幂等键）；抓取中台：ams-server 定时拉取适配器（ERP/金蝶 mock 适配器先行） | finance:record + 采集批次表 |

---

## 九、专项设计

### 9.1 档号赋号服务（解决 P0-3）
- ams_code_serial 行级原子取号（§6.2），confirmVolume → POST /code/next 按段逐个取号
- 启动回填：扫描库内既有档号取各作用域 max（兼容仿真迁移数据）
- 全宗联动：取号 scope 含 fondsCode，Header 切全宗后组卷/赋号用当前全宗（修 P1-8）
- 段结构可配：/config/code.rule 存 ArchiveCodeRule JSON，引擎按配置 buildPrefix（修"假编辑"）

### 9.2 文件上传/下载/预览
```
上传：前端 multipart → ams-server → Alfresco POST /nodes/{parentId}/children
      （form-data: filedata + name + nodeType=finance:record + properties）
下载：GET /files/{nodeId}/content → ams-server 取流 → 如需水印 PDFBox 烧录 → 响应
预览：Alfresco rendition（pdf）→ ams-server 代理流 → 前端 <iframe>/canvas + WatermarkLayer（已实现）
版本：Alfresco 自动版本（cm:versionable），审计追踪可取 versions 列表
```

### 9.3 水印烧录（download/print 落地）
- 策略来自 /config/watermark（现 watermarkStore 迁移）
- PDFBox：按 layout(tile/center)/rotation/opacity/fontSize/density 平铺文本行（姓名+工号+时间戳+IP+警示语），逐页烧录后流式返回
- 盲水印（频域）与防截屏 SDK 列为后续增强（前端配置项已预留）

### 9.4 OCR 接线
- 推荐 **ams-server 编排**：上传后对 PDF/图片调 Tesseract 容器（:8091）或直接嵌入 Tess4J，结果写 finance:ocrText
- Solr 自动索引 ocrText → 事项检索/全文检索立即具备 OCR 全文能力
- 替代方案（注册为 Alfresco Transform 引擎）工作量大、链路长，暂不选

### 9.5 四性检测引擎（解决 P0-4）
| 性 | 检测项（真实现） |
|----|------------------|
| 真实性 | SHA-256 哈希比对（入库时存 finance:digitalHash）；数字签名验证（OFD 签章解析）；操作日志追溯（ams_operation_log 覆盖度） |
| 完整性 | 主附件数量匹配（recordSourceDocs 计数 vs attachmentCount）；元数据必填校验（按 /config/inspection.plan 勾选字段）；包结构校验（移交包 manifest vs 实际内容） |
| 可用性 | 格式白名单（OFD/PDF/XML/JPG/TIFF）；rendition 渲染验证（调 Transform 转 pdf 成功即通过）；DPI 检测（扫描件≥300） |
| 安全性 | 敏感词扫描（ocrText 命中身份证/银行卡/涉密词表）；病毒扫描（ClamAV 容器可选）；密级匹配与动态水印标记 |
- 归档强制点：POST /volumes/{id}/confirm 前置调用，全过才赋号（79 号令"归档触发检测"落地）
- 报告：ams_inspection_report + 节点回写 finance:fourChecked aspect；FourChecksPage 改真数据

### 9.6 审批引擎与 wf-designer 接线（解决 P1-7）
- 路由规则数据化：/config/approval.routes = [{id, name, trigger: {permLevels, securityLevels}, steps: [roles]}]，默认规则 = 现 computeApprovalRoute 逻辑
- wf-designer 保存 → 写 /config/approval.routes；借阅引擎读它 → 设计器不再孤岛
- 远期可迁 Alfresco Activiti 工作流（ACS 内置），但自研轻量路由已满足条件路由需求，建议保持自研

### 9.7 每日巡检 Job
ams-server @Scheduled(cron "0 5 0 * * *")：电子到期 auto_revoke、实体逾期 overdue+抄送直属主管日志、到期前 3 天催还预警；借阅管理页"手动巡检"= POST /admin/daily-check（前端已有按钮，接真端点）。通知通道预留 ActiveMQ topic（供后续接邮件/企微）。

### 9.8 检索（Solr 代理）
- 凭证检索：AFTS `=finance:voucherNo:"记-001" AND =finance:year:2026 AND =finance:amount:[1000 TO 50000]` + 属性过滤
- 事项检索：ocrText 全文 + finance:counterpartyName / documentNo / businessCategory
- ams-server 代理 Search API，前端保持现有查询 UI

### 9.9 实体库房三轨合一（解决 P1-6）
- ams_storage_node 树：库房→柜→架→层（seed：1号库房/柜A/架1-5/层1-4）
- 盒 location 全部改为树节点路径（`1号库房-柜A-架3-层2`）
- DigitalWarehousePanel 按 storage 树 + 盒节点 location 反查渲染真实占用格
- 借阅实体库存 = finance:volume + ams_fulfillment 状态推导（现有 borrowEngine 逻辑后端化，口径不变）

### 9.10 仿真数据迁移（承上启下）
- 写一次性迁移脚本（ams-server /admin/migrate/simulation）：读取 financeWeb 的 simulationData 生成结果 → 按 §5.1 目录结构批量建节点（盒/卷/件/原始凭证 + 属性 + 虚拟 content 文件）
- 好处：前端切 API 后所见数据与现在完全一致，测试口径不断档；同时验证模型与批量写入性能
- borrow 种子单同步迁入 ams_borrow_* 表

---

## 十、断点修复映射表（现状断点 → 方案落点）

| 断点（全景文档 §七） | 方案落点 | 阶段 |
|----------------------|----------|------|
| P0-1 收集不产生记录 | §八 收集行：POST /records 推送接口 + 拉取适配器 | P4 |
| P0-2 模型未部署 | §五 finance-model v2 打进 platform 模块 | **P0** |
| P0-3 流水号重号 | §9.1 ams_code_serial | **P0** |
| P0-4 四性全假 | §9.5 检测引擎 + confirm 强制 | P3 |
| P0-5 打包→移交断 | §6.6 + §八 打包/移交行（按钮接真端点） | P3 |
| P1-6 库房三轨 | §9.9 ams_storage_node | P4 |
| P1-7 wf 孤岛 | §9.6 路由规则数据化 | P3 |
| P1-8 全宗脱节 | §9.1 取号含 fondsCode + store 联动 | P1 |
| P1-9 移交状态机空转 | §6.6 transfer_batch 三态 | P3 |
| P1-10 销毁两套假 UI | §6.7 appraisal + 统一入口 | P4 |
| P1-11 人员双体系 | §4.2 People seed + ams_user_ext | **P0** |
| P1-12 元数据双轨 | /config/metadata.display 单源 + volume/box 字段补全（V1-V20/B1-B29 全量注册） | P1 |
| P2-13~18 假编辑/空场景 | 各配置迁 ams_config 真生效；水印烧录；审计追踪真查询；目录配置回读修复；全宗字段补全 | P1-P3 |
| 死代码清单 | 随迁移逐个删除 | 全程 |

---

## 十一、分阶段实施计划

### P0 · 平台底座（约 1-2 周）—— 打通认证、模型、流水、用户
| 任务 | 产出 | 验收 |
|------|------|------|
| finance-model v2 打进 alfresco-aio-platform，重新构建部署 | types API 可见 finance:* 全部类型 | POST /types 返回 6 类型 |
| ams-server 骨架（Spring Boot + Flyway + ams schema 全表） | 服务起 :8081，/api/health OK | 健康检查 |
| 认证链路（/auth/login + ticket 透传 + 前端 authStore 改造） | 8 账号可登录，角色正确 | 逐角色登录验证菜单 |
| People seed（8 演示账号 + 部门组 + ROLE_* 组 + ams_user_ext） | 人员管理页可见 8 人 | UI 可见 |
| ams_code_serial + /code/next | 并发取号不重号 | 并发测试 |
| 全宗 CRUD 接通（fondsService 走通 + recordCount 回填） | 全宗页真数据 | 增删改全宗 |
| 配置中心 /config/** + 迁移 localStorage/Express JSON 全量配置 | 各配置页读写真后端 | 刷新不丢 |

### P1 · 档案主数据（约 2-3 周）—— 盒/卷/件/原始凭证 + 组卷链路
| 任务 | 产出 |
|------|------|
| 仿真数据迁移脚本（§9.10） | Alfresco 内可见 355 件/27 卷/12 盒 |
| /records /volumes /boxes /files 域 API | 前端 archiveStore/volumeStore/archiveBoxStore/sourceDocumentStore 切 API |
| 文件上传/下载/预览链路（rendition） | UploadModal 真上传，详情真预览 |
| 组卷链路后端化（建卷/加件/拆卷/确认/移交归盒） | 组卷工作台全流程真操作 |
| 档号规则/三合一/组卷规则配置真生效（修假编辑+漏消费） | 改配置影响引擎行为 |
| 元数据双轨合一 + volume/box 字段注册表补全 | 配置页单源真消费 |

### P2 · 借阅后端化（约 1-2 周）
| 任务 | 产出 |
|------|------|
| ams_borrow_* + /borrow/** + 引擎迁移 | 借阅全链路真后端，页面仅改 service |
| 巡检 Job + 手动巡检端点 | 到期收回/逾期/催还真执行 |
| 操作日志统一（ams_operation_log 哈希链） | 安全审计页真查询 |
| 借阅统计/台账/审批中心/我的借阅切 API | 全链路 persist 去除 |

### P3 · 高级能力（约 2 周）
| 任务 | 产出 |
|------|------|
| 四性检测引擎 + confirm 强制校验 + 报告页 | 检测真结论，归档有门槛 |
| 水印烧录（下载/打印 PDFBox） | 下载文件带烧录水印 |
| OCR 接线（上传即识别，ocrText 可检索） | 事项检索命中发票内容 |
| wf-designer 路由规则数据化接线 | 设计器改流程影响审批 |
| 打包→移交流程闭环（ams_package + transfer_batch 三态） | 封装包可下载可移交可签收 |

### P4 · 收集/库房/销毁/统计 API 化（约 2 周）
| 任务 | 产出 |
|------|------|
| 收集：推送接口 + 拉取适配器框架（ERP/金蝶 mock 适配器） | 收集页真数据流入 |
| 实体库房统一（ams_storage_node + 库房页真实渲染） | 三轨合一 |
| 鉴定销毁全流程（期满扫描→鉴定→联签→销毁→日志） | 销毁页真功能 |
| 统计 /stats/** 服务端聚合切换 | 大数据量下秒开 |
| 审计追踪穿透链路真查询 | 单凭证全链路可穿透 |
| 死代码与遗留清理（App.tsx.bak、FanzongPage、voucherFilesStore 等） | 仓库整洁 |

---

## 十二、风险与待决策点

| # | 事项 | 选项 | 建议 |
|---|------|------|------|
| 1 | **业务服务技术栈（D1）** | Spring Boot（推荐）/ Node Express（沿用3001） | Spring Boot：水印烧录 PDFBox、与 Alfresco 同栈；若团队更熟 Node 也可，方案表结构不变 |
| 2 | 全宗/门类建模 | Alfresco 节点（推荐，已在走）/ 业务表 | 节点：层级+权限+检索免费 |
| 3 | 现有 Express:3001 目录配置服务 | 迁入 ams-server 后退役 | 退役 |
| 4 | 演示 8 账号 | seed 进 Alfresco People（推荐，统一认证） | 是 |
| 5 | 仿真数据 | 迁移脚本导入 Alfresco（推荐，演示不断档） | 是 |
| 6 | 审批引擎 | 自研轻量路由（推荐）/ Alfresco Activiti | 自研够用，远期可迁 |
| 7 | OCR 方式 | ams-server 编排 Tesseract（推荐）/ 注册 Transform 引擎 | 前者简单可控 |
| 8 | 通知通道 | 日志+预留 ActiveMQ（推荐先行）/ 立即接邮件企微 | 先预留 |

---

## 十三、前端改造工作量评估（配合各阶段）

| 层 | 改造量 | 说明 |
|----|--------|------|
| services/ | **新建 12 个 service**（auth/org/fonds/record/volume/box/borrow/inspection/file/config/stats/audit） | 统一 request 封装（ticket 头、错误处理） |
| stores/ | **18 个 store 逐个迁移**（保留形状与 action 签名，内部换 service 调用） | 页面零改或仅异步处理微调 |
| 页面 | 主要是断点修复类改造（打包/移交/销毁/库房/收集按钮接真端点） | 与断点映射表一一对应 |
| 测试 | 引擎/完整性测试保留；新增 service 层契约测试（mock fetch） | vitest 体系沿用 |

---

## 十四、一句话总结

**Alfresco 管"档案是什么"（内容/元数据/组织/检索/预览），ams-server 管"档案在发生什么"（组卷/借阅/审批/履约/检测/流水/日志），前端只跟 ams-server 说话**——本方案所有表结构、模型、API、阶段均按此边界设计，可直接进入 P0 实施。
