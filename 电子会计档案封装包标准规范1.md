# 电子会计档案封装包标准规范

电子会计档案封装包功能严格依据《电子会计档案管理规范》（DA/T 94\-2022）、《基于 XML 的电子文件封装规范》（DA/T 48\-2009）、《电子档案移交接收操作规程》（DA/T 93\-2022）及《电子文件归档与电子档案管理规范》（GB/T 18894\-2016）设计，是电子会计档案归档、移交、离线备份全流程的标准载体，核心目标是保障电子档案的真实性、完整性、可用性与安全性。封装逻辑与纸质档案的分类、组卷、档号规则完全一一对应，实现双套制下档案标识与管理逻辑的统一，所有封装规则均在法定框架内设置，无自定义合规底线内容。

封装单元严格匹配会计档案的法定组卷规则，与实体案卷形成一一映射关系，确保电子与纸质整理逻辑完全同频。会计凭证类以月度内连续记账凭证号对应的一卷凭证为基本封装单元，单月凭证拆分为多卷的对应生成多个封装包，包内包含对应号段内全部记账凭证电子文件及附属原始凭证附件，原始凭证不单独拆分封装，与记账凭证绑定为同一归档单元，贴合会计实操中 “按凭证号装订、附件依附凭证” 的工作习惯；会计账簿类以单种独立账簿为基本封装单元，总账、明细账、日记账、辅助账簿分别单独封装，对应同一年度内同一账簿的完整电子数据；财务会计报告类按报告周期分别封装，年度财务会计报告单独生成封装包，月度、季度、半年度中期报告可按年度合并为一个封装包，永久保管的年度报告与定期保管的中期报告严禁合并封装；其他会计资料类按资料类别结合保管期限封装，同一年度、同一类别、同一保管期限的资料合并封装，不同保管期限的资料不得混入同一封装包。集团型多组织机构单位可按核算主体分别生成独立封装包，与分类体系中的组织机构维度一一对应。

标准归档信息包采用分层结构化设计，内部固定包含四大核心组成部分，所有组件缺一不可。第一部分为电子档案内容文件，是封装包的核心主体，电子会计资料的归档版式优先采用 OFD 格式，暂不具备条件的可使用 PDF/A 等长期保存格式；原生电子文件如需保留原格式的，需同步归档原格式文件与转换后的归档格式文件，确保长期可读。内容文件按档号顺序有序存放，记账凭证类按凭证号升序排列，附件文件随对应记账凭证集中存放，命名与对应凭证号建立关联映射。第二部分为结构化元数据，包含包级元数据、案卷级元数据、文件级元数据三个层级，元数据项严格遵循 DA/T 94\-2022 附录 A 的法定要求，覆盖全宗号、档号、年度、类别、保管期限、文件形成时间、经办审核信息等核心字段，元数据采用 XML 格式存储，与内容文件永久绑定，确保档案背景信息完整可追溯。第三部分为封装说明文件，以 XML 或版式文件格式存放，记录封装包唯一标识、创建主体、创建时间、封装工具、档案范围（起止档号、文件数量）、软硬件运行环境等信息，用于档案接收、迁移、恢复时的基础信息核验。第四部分为防篡改校验信息，系统自动对包内所有文件生成符合国家密码管理规范的数字摘要，支持附加可信时间戳与合法电子签名，用于校验封装包的完整性，防止传输、存储过程中被非法篡改。

封装包的格式与命名遵循统一合规规则，兼顾通用性与可识别性。封装包的容器格式默认采用标准 ZIP 格式，需符合档案行业专项封装要求的场景可扩展支持 DA/T 48\-2009 规定的 XML 封装格式，文件编码统一采用 UTF\-8 字符集，确保跨系统、跨平台可正常读取解析。封装包命名规则与档号体系深度绑定，默认命名规则为「全宗号 \- 门类代码・类别号・年度 \- 案卷号」，与实体案卷档号完全一致，确保通过文件名即可快速识别档案归属与范围；单件整理模式下命名规则对应调整为「全宗号 \- 门类代码・类别号・年度・保管期限 \- 件号」，命名全局唯一，禁止重名。针对批量归档场景，可按批次生成批次级总封装包，内部按分类维度嵌套子封装包，批次包命名增加批次标识与生成时间，便于批量管理与追溯。

系统内置封装全流程合规校验机制，与档案四性检测功能深度联动。封装前自动校验待封装档案的合规性：校验档案分类是否准确、是否存在跨年度跨类别跨期限混装、元数据必填项是否完整、内容文件格式是否符合长期保存要求，校验不通过的档案自动拦截，禁止进入封装环节，并标注异常原因引导修正。封装完成后自动执行完整性校验：比对包内文件清单与待封装档案清单是否一致、数字摘要校验是否通过、元数据与内容文件是否一一对应，校验通过后自动生成封装成功标识与封装日志，记录封装人、封装时间、包内文件明细、校验结果等信息，全程留痕可追溯。接收外部归档封装包时，系统自动执行解包校验，按照 DA/T 70 规定的检测标准完成真实性、完整性、可用性、安全性检测，检测合格后方可入库归档，不合格的自动退回并同步异常说明。

针对不同业务场景，系统提供差异化封装适配能力。日常归档场景生成标准归档信息包，用于业务系统向档案系统移交归档，与系统内的组卷、著录流程自动联动，完成整理后可一键生成封装包并完成归档入库，无需人工重复操作。档案移交进馆场景生成标准移交信息包，严格遵循 DA/T 93\-2022 的结构要求，额外补充电子档案移交清单、移交说明文件，包结构与内容可适配接收档案馆的进馆规范，支持按进馆要求自定义调整封装维度与补充字段。离线备份场景生成备份封装包，额外增加数据恢复说明文档与格式读取工具说明，支持按备份周期批量打包，确保离线存储的电子档案可独立解析、完整恢复，符合电子档案长期保存的管理要求。所有封装包均与对应纸质档案的盒号、案卷号建立双向关联，支持通过实体档号检索对应电子封装包，也可通过电子封装包反查实体存放位置，实现双套档案的一体化管理。

> （注：部分内容可能由 AI 生成）
