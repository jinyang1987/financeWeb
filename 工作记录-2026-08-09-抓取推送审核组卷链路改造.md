# 工作记录 · 2026-08-09 抓取/推送/审核/组卷链路梳理与改造

## 一、任务来源

Issue#1 用户（靳阳）要求：

1. **链路检查**：抓取（用友BIP 已真通）与推送（假页面）都能选择「进审核库」或「直接组卷」，验证两条链路是否通、缺什么按钮/逻辑，从前台到后台全程检查。
2. **Token 配置权限**：抓取的 token 配置页面应收敛到「系统管理」，支持用友/金蝶等不同数据源分别配置，不允许所有人随意设置；抓取人员只负责抓取和看日志。
3. **元数据对应关系**：抓过来的原始数据与档案管理要求的元数据差异大不大、怎么对应。

## 二、调研结论（链路现状）

| 环节 | 现状 | 结论 |
|------|------|------|
| 抓取（用友BIP Pull） | `YonyouSyncService` 真拉真转真入库，`ApiReceivePage` 全接真接口 | ✅ 真通 |
| 推送（业务系统 Push） | `OpenApiReceivePage` 纯规划占位，无后端接口 | ❌ 假页面 |
| 审核环节 | `recordStatus` 仅「仅件数据→待审核→已组卷」，「待审核」只是入卷中间态，**无独立审核动作/页面/接口** | ⚠️ 缺独立审核库 |
| Token 配置 | 用友连接配置放在抓取页抽屉，archivist 可改，无角色控制 | ❌ 权限错位 |
| 元数据映射 | `YonyouTransformer` 已有映射，但缺与 DA/T 94 的严谨对照 | ⚠️ 需文档化 |

## 三、本次改造内容

### 3.1 后端（ams-server）

| 模块 | 内容 |
|------|------|
| `datasource/DatasourceConfigService` | 多数据源配置中心（用友/金蝶/发票/银行/报销等），存 `ams_config(datasource.config)`，兼容迁移旧 `yonyou.connection` |
| `datasource/DatasourceController` | `/datasources/**` CRUD，**仅 admin/档案主管/档案管理员可读写**（403 拦截） |
| `review/ReviewService` | 审核服务：enter（进审核库）/approve（通过）/reject（驳回），`ams_review_log` 留痕 |
| `review/ReviewController` | `/review/**`：pending 列表 / 审核历史 / enter / approve / reject，仅档案管理员/主管/admin |
| `openapi/OpenPushService` | 推送接入：token 签发、单件推送、批量推送、回执查询，幂等去重 |
| `openapi/OpenApiController` | `/open/v1/**`：推送方 Bearer 认证 + 档案侧管理（签发应用/批次历史） |
| `yonyou/YonyouClient` | `conn()` 优先读多数据源配置（yonyou-bip），兼容回退旧配置 |
| `yonyou/YonyouSyncService` | `syncNow` 新增 `review` 参数：进审核库（待审核）则禁止自动组卷，审核通过后再组卷 |
| `yonyou/YonyouController` | `/yonyou/config` GET/PUT 增加**角色权限校验**（仅档案管理员/主管/admin） |
| `db/V4` | 新增 `ams_open_app`、`ams_open_push`、`ams_open_push_item`、`ams_review_log` 四张表 |

### 3.2 前端（financeWeb）

| 页面/文件 | 内容 |
|----------|------|
| `pages/system/DatasourceConfigPage` | 新增「系统管理→数据源配置」：多数据源卡片/新增/编辑/删除，secret 脱敏，非管理员只读提示 |
| `pages/system/OpenAppManagePage` | 新增「系统管理→推送接入应用」：签发 AppKey/AppSecret（仅展示一次）、接入应用列表、推送批次历史+明细 |
| `pages/archive-arrange/ReviewWorkbenchPage` | 新增「档案整理→审核工作台」：待审核列表、审核通过/驳回（驳回必填意见）、审核历史 |
| `pages/archive-rcv/ApiReceivePage` | 移除原连接配置抽屉；「连接配置」按钮改跳系统管理；同步增加「进审核库」开关 |
| `pages/archive-rcv/OpenApiReceivePage` | 从「功能建设中」改为「已上线」，端点改真实 `/api/ams/open/v1/**`，接入应用入口跳系统管理 |
| `services/datasourceService.ts` / `reviewService.ts` / `openPushService.ts` | 新增三个 API 服务 |
| `config/menuConfig.ts` / `stores/appStore.ts` / `types/user.ts` | 新增菜单、路由、角色权限矩阵 |
| `components/layout/ContentArea.tsx` | 注册新页面路由 |
| `__tests__/reviewPipeline.test.ts` | 新增 9 个测试：双链路状态机 + 数据源权限逻辑 |

## 四、验证结果

| 检查项 | 结果 |
|--------|------|
| `ams-server` `mvn -DskipTests package` | ✅ 编译打包成功 |
| 前端 `npx tsc --noEmit` | ✅ 零错误 |
| 前端 `npx vitest run` | ✅ **139 用例全部通过**（12 个测试文件，含新增 9 个） |
| 前端 `npm run build` | ✅ 构建成功 |

## 五、链路结论（对用户问题的回答）

1. **双链路已打通**：抓取/推送 → 收集池 → 可「进审核库」（审核通过后组卷）或「直接组卷」（跳过审核），两条链路后端+前端均已实现。
2. **Token 配置已收敛到系统管理**：多数据源（用友/金蝶/发票/银行/报销）分别配置，仅档案管理员/主管/admin 可改；抓取人员只看到抓取操作和同步日志。
3. **元数据差异已文档化**：《用友抓取数据与档案元数据映射差异分析-2026-08-09.md》，核心要素全部映射，差异集中在可空字段与归档补齐，无结构冲突。

## 六、遗留事项

- Alfresco 模型 `finance:recordStatus` 枚举未新增值（审核流复用现有枚举），如需更细粒度（如「已审核」独立状态）需改模型并部署——建议后续评估。
- 推送接口为同步受理；若量大可改异步队列（后续优化）。
- `ams_open_app.app_secret` 明文落库（与 yonyou.connection 一致），生产建议加密存储或外部密钥管理。
