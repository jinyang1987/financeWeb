# 用友 BIP 抓取数据 ↔ 会计档案元数据 映射与差异分析

> 2026-08-09 · 依据《用友BIP集成设计-2026-08-08.md》《YonyouTransformer.java》实测比对
> 目的：严谨回答「抓过来的原始数据 vs 档案管理要求的元数据 差异大不大、怎么对应」

---

## 一、结论先行

**差异可控，主体元数据已完整映射，剩余差异集中在 3 类：**

| 差异类别 | 严重度 | 说明 |
|---------|--------|------|
| ① 字段级差异（用友有 / 档案缺 或 档案有 / 用友缺） | 🟢 低 | 已用默认值/可空字段兜底，不影响归档合规 |
| ② 语义差异（同一字段两侧含义不同） | 🟡 中 | 转换层已做业务映射（如 凭证字+号 → 凭证字号） |
| ③ 归档要求差异（DA/T 94 / 79 号令要求但用友无） | 🟠 较高 | 用版式 PDF、默认值、四性检测补齐；部分需人工补录 |

**一句话：抓过来的数据 80% 直接可用，20% 通过转换规则/默认值/补录承接，无结构性冲突。**

---

## 二、映射对照表（用友字段 → 档案元数据 finance:record v2.2）

### 2.1 直接映射（一对一，无损耗）

| 用友字段 | 档案字段 | DA/T 94 要素 | 说明 |
|---------|---------|-------------|------|
| `header.displayname` | `finance:voucherNo` | M31 凭证字号 | 转-1 / 记-2 |
| `header.vouchertype.voucherstr` | `finance:voucherWord` | M31 凭证字 | 转 / 记 |
| `header.vouchertype.name` | `finance:voucherCategory` | M14 档案类型 | 收款/付款/转账/通用 |
| `header.period` | `finance:period` | M11 年度 + M41 月份 | 2024-02 → year=2024, month=2 |
| `header.maketime` | `finance:voucherDate` | M32 形成时间 | d:date |
| `header.maker.name` | `finance:preparer` | M36 制单人 | 列表接口 maker；详情接口 makerObj |
| `header.description` | `cm:description` | M44 摘要 | Alfresco 标准属性 |
| `header.attachedbill` | `finance:attachedBillCount` | M34 附件张数 | 3 |
| `header.totaldebit_org` | `finance:amount` | M35 金额合计 | 借方合计（=贷方，借贷平衡） |
| `header.id` | `finance:externalId` | — | 幂等键 |
| 常量 | `finance:archiveType=记账凭证` | M14 | 用友不区分凭证大类，固定记账凭证 |
| 常量 | `finance:retention=30年` | M12 | 记账凭证法定保管期限（会计档案管理办法） |
| 常量 | `finance:source=digital-native` | — | 原生电子 |
| 常量 | `finance:carrierType=electronic` | — | 纯电子 |
| 常量 | `finance:sourceSystem=用友BIP` | — | 来源系统追溯 |

### 2.2 结构化映射（列表 → JSON）

| 用友字段 | 档案字段 | 说明 |
|---------|---------|------|
| `body[]`（recordnumber/description/accsubject.code+name/debit_original/credit_original） | `finance:entries`（JSON） | 分录结构化存储，是版式/检索/展示的唯一事实源 |
| 分录科目去重汇总 | `finance:accountSubject` | 检索维度 |

**entries JSON 结构：**
```json
[{"line":1,"summary":"资本公积转增资本","subjectCode":"400201",
  "subjectName":"资本公积_资本（或股本）溢价","debit":93835.00,"credit":0}]
```

### 2.3 详情接口补拉（审核/记账人只在详情）

| 用友字段（详情） | 档案字段 | 说明 |
|----------------|---------|------|
| `detail.auditorObj?.name` | `finance:auditor` | 可空（多数凭证未审核时 null） |
| `detail.tallyManObj?.name` | `finance:tallyMan` | 可空 |
| `detail.makerObj.name` | `finance:preparer` | 详情优先 |

---

## 三、差异清单（严谨逐类）

### 3.1 🟡 语义差异（转换层已处理，但需知晓）

| # | 差异 | 处理 |
|---|------|------|
| S1 | 用友「凭证字 + 凭证号」是两个字段；档案「凭证字号」是组合值 | 转换层拼接 `{字}-{号}`，同时保留 `voucherWord` 字段 |
| S2 | 用友 `period` 是 `yyyy-MM`；档案拆成 `year` + `month` 两个整数 | 转换层拆分，用于按年/月筛选 |
| S3 | 用友 `voucherstatus=01` 是凭证流转状态 | 不映射（非档案元数据，避免污染归档） |
| S4 | 用友科目是「编码+名称」；档案 entries 用 code 作主键 | 结构化保留 code + name 双字段 |
| S5 | 用友 `displayname` 直接是"转-1"；个别含前缀 | 已兼容，无拆分损失 |

### 3.2 🟠 归档要求差异（档案要求但用友无 → 已用策略承接）

| # | 档案/国标要求 | 用友侧现状 | 承接策略 |
|---|-------------|-----------|---------|
| G1 | 79 号令：电子会计凭证须有版式文件 | 沙箱 `queryBusinessFiles` **全空**（无电子附件） | **生成版式 PDF**（凭证头+分录表+借贷合计+大写金额），件必须有内容文件 |
| G2 | DA/T 94 归档信息包须含四性检测 | 无 | 四性检测在组卷工作台执行（真实引擎接入前不伪造），同步批次如实记录 |
| G3 | 审计人/记账人（DA/T 94 元数据） | 沙箱 `auditorObj` 多为 null | 可空字段，缺失不阻断；人工核对时可补录 |
| G4 | 归档范围分类（DA/T 42） | 用友只有"记账凭证"概念 | 固定 archiveType=记账凭证，报表类另行归档为财务报告 |
| G5 | 密级 | 无 | 默认"普通"，可在核对/审核时调整 |
| G6 | 责任部门 | 用友 header 无直接部门字段 | 可空，从分录科目/制单人部门推断或人工补录 |

### 3.3 🟢 字段级差异（已用默认值/可空兜底）

| 档案字段 | 用友来源 | 兜底 |
|---------|---------|------|
| department（责任部门） | 无 | 空，人工补录 |
| securityLevel（密级） | 无 | 默认"普通" |
| retention（保管期限） | 无 | 固定"30年"（记账凭证法定） |
| storageLocation（实体位置） | 无 | 空（电子件无实体） |
| ocrText | 无（版式 PDF 可 OCR） | 手动触发 OCR 时写入 |

---

## 四、双链路数据流（本次打通后）

```
┌─ 抓取（Pull）─ 用友 BIP ─┐
│  YonyouSyncService       │──┐
│  （转换+版式PDF+幂等）      │  │   ┌─ 进审核库 ─ 审核工作台 ─ 审核通过 ─┐
└──────────────────────────┘  ├──→┤                                   ├──→ 组卷工作台 → 确认赋号 → 移交归盒
┌─ 推送（Push）─ 业务系统 ──┐  │   └─ 直接组卷（跳过审核）────────────┘
│  OpenApiController       │──┘
│  （Bearer认证+幂等）       │
└──────────────────────────┘
        ↓
   目标全宗收集池（仅件数据）
```

**审核状态机（`finance:recordStatus`）：**
```
仅件数据 ──enter──→ 待审核 ──approve──→ 仅件数据（可组卷）
   │                    └──reject──→ 仅件数据（驳回回池）
   └───── 直接组卷（跳过审核）→ 待审核（入卷）→ 已组卷（确认）
```

> 注：`finance:recordStatus` 枚举约束为 `仅件数据|待审核|已组卷`，本次审核流完全复用该枚举，
> 不新增 Alfresco 模型枚举值，避免模型部署风险；审核动作通过 `ams_review_log` 表留痕（谁/何时/什么意见）。

---

## 五、遗留差异与后续建议

| # | 事项 | 建议 |
|---|------|------|
| 1 | 沙箱无电子附件（queryBusinessFiles 全空） | 生产接入时附件走附件同步链路（已实现 best-effort），无附件生成版式 PDF |
| 2 | 凭证审核人/记账人多为 null | 建议用友侧完善审核流后再同步，或人工在核对工作台补录 |
| 3 | 报表接口 items 全空 | 已按"诚实策略"如实记录 0，不伪造空表归档 |
| 4 | 科目体系对应（用友科目 vs 档案三合一分类） | 建议后续做科目映射表配置（系统管理→数据源配置 扩展） |

---

## 六、元数据差异结论（给靳阳的直答）

> **差异大不大？** 不大。用友抓来的数据与档案管理要求的元数据，**核心身份要素（凭证号/年度/期间/金额/制单人/摘要/分录）全部能映射**，无结构冲突。
>
> **怎么对应？** 用 `YonyouTransformer` 转换层做「字段映射 + 语义转换 + 默认值补齐」，版式 PDF 承接 79 号令文件要求，四性检测/人工核对承接 DA/T 94 归档要求。详见上方映射表。
>
> **真正要人工关注的点**：审核人缺失、责任部门缺失、报表无数据、密级默认普通——这些都不阻断归档，但在合规审计时可能需要补录。
