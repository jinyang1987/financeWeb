# 界面精简与按钮防折行方案（组卷工作台）

- 日期：2026-09-06
- 状态：**已执行完毕（2026-09-06 用户批准"动工"，全部验收通过；执行记录见文末）**
- 截图来源：用户 2026-09-06 提供（组卷工作台页面）
- 关联文档：《系统缺陷审计与修复计划-2026-08-29.md》《全局字体排版一致性设计方案-2026-09-05.md》

---

## 一、用户要求（原文）

1. **按钮折行的问题**：看看怎么设计让其不折行。
2. **右侧框起来的解释性内容去掉**：把地方留给数据展示。"都是专业的人员，都懂数据，不用解释。"

---

## 二、问题定位（截图 ↔ 代码）

页面：**档案整理 → 组卷工作台**
文件：`financeWeb/src/pages/archive-arrange/VolumeWorkspacePage.tsx`
布局：`grid grid-cols-[3fr_2fr]`（行 2861）——左池 60%、右案卷区 40%。

### 问题 1：按钮折行（左池工具栏）

结构（行 283–430，UnassignedPool 组件）：

```
外层行  flex items-center justify-between          （行 293）
├─ 左：label「全选 (N 条)」                        （行 294，text-xs，无 nowrap ← 会被挤压内部折行）
└─ 右：按钮容器  flex items-center gap-1.5 flex-wrap （行 306 ← flex-wrap 是折行直接原因）
     组卷（N）/ 加入已有▾ / 组件（N）或解挂（N） / │ / 元数据 / 建档 / │ / 删除（N）
```

根因：
- 按钮容器 `flex-wrap`：空间不足时按钮**逐个掉行**（截图中「删除（1）」单独掉到第二行，视觉破碎）；
- 「全选 (N 条)」label 无 `whitespace-nowrap`：被挤压时文字内部折行（截图中折成两行）；
- 2026-09-05 新增「元数据/建档」后按钮总数达 7 个，自然宽度 ≈ 700px+，已到临界点。

同类隐患：案卷卡片操作按钮行（行 845 起）同样用 `flex-wrap`（确认组卷/目录预览/元数据录入/合并/拆卷/删除空卷），右栏更窄（40%），一并根治。

### 问题 2：右侧解释性内容（案卷卡片内）

| # | 位置 | 内容 | 处置 |
|---|------|------|------|
| 1 | 行 836–843，draft 卷展开区 | 灰底边框提示框：「四性检测在『移交至档案保管』时按规定自动执行，未通过将阻断移交；检测明细见 档案整理→快速检测」 | **整块删除**（截图圈选目标）。规则本身由移交环节强制执行（代码已阻断），无需界面常驻解释；语义收进「确认组卷」按钮的 `title`（hover 才出现，不占版面） |
| 2 | 行 926–930，已确认卷状态行 | 「待移交时自动执行四性检测**（按规定：移交=检测节点）**」 | 保留状态行（这是状态不是解释），**删去括号内的解释性文字** |
| 3 | 行 688 附近，卷内条目区 `max-h-48`（192px） | — | 提示框删除后，把省出的空间**让给数据**：`max-h-48` → `max-h-64`（256px），卷内件列表可视区 +64px |

### 保留项（非解释性，明确不动）

- 「档号： xxx」档号展示行、四性检测通过/未通过结果行 —— 是**数据/状态**，保留；
- 展开行「所附原始凭证（N 张，随本凭证整体组卷）」—— 数据标注，保留；
- 空态引导（"暂无案卷，在左侧勾选凭证后点击组卷…"）—— 仅空数据时出现，保留；
- 确认弹窗内的规则说明（纯原始凭证组卷确认等）—— 用户操作当下的决策依据，保留；
- 左池筛选栏「未组卷 N 条 | 案卷 N 卷」—— 统计数据，保留。

---

## 三、事项一：按钮防折行设计方案

### 设计原则（在既有"防折行三件套"上升级为硬约束）

1. **容器 `flex-nowrap` 硬约束**：删除 `flex-wrap`，按钮在任何窗口宽度下绝不换行；
2. **隐形横向滚动兜底**：容器加 `overflow-x-auto` + 隐藏滚动条（`scrollbar-width:none; ::-webkit-scrollbar{display:none}`），极端窄屏（<1366px）时按钮组横向滚动而不是折行——**宁可滚动，绝不折行**；
3. **紧凑化保证常规屏单行放下**（不动字号，遵守字阶闭集：按钮保持 text-sm=14px / 卡片行 text-xs=12px，层级靠间距与字重）：
   - 按钮内边距 `px-3` → `px-2.5`，按钮间距 `gap-1.5` → `gap-1`；
   - 移除两个竖分隔条 `w-px h-4 mx-0.5`（省 ~10px，分组语义由间距承担）；
   - 「全选 (N 条)」label 加 `whitespace-nowrap shrink-0`。

### 宽度预算（紧凑后）

左池工具栏 7 按钮：组卷（N）96 + 加入已有 94 + 组件（N）96 + 元数据 80 + 建档 66 + 删除（N）96 + 间距 ~24 ≈ **552px**，加「全选 (N 条)」~85px ≈ **640px**。
常规 1920 屏左栏约 990px、1440 屏约 820px → **单行放下，滚动兜底几乎不触发**。

### 改动点清单

| 文件 | 行 | 改动 |
|------|----|------|
| `VolumeWorkspacePage.tsx` | ~294 | 「全选 (N 条)」label 加 `whitespace-nowrap shrink-0` |
| 同上 | ~306 | 按钮容器 `flex-wrap` → `flex-nowrap overflow-x-auto` + 隐藏滚动条；`gap-1.5` → `gap-1` |
| 同上 | ~316/365/391/401/413/427 | 7 个按钮 `px-3` → `px-2.5`（`whitespace-nowrap shrink-0` 已有，保留）；删除两处竖分隔条 |
| 同上 | ~845 | 案卷卡片操作行 `flex-wrap` → `flex-nowrap overflow-x-auto`（同款隐藏滚动条），`gap-2` → `gap-1` |

> 隐藏滚动条 CSS 以行内 `style={{ scrollbarWidth:'none' }}` + index.css 追加一个 `.no-scrollbar` 工具类实现（一处定义，两处复用）。

---

## 四、验收标准

1. **单行验证**（浏览器 DOM）：池工具栏按钮容器与卡片操作行 `getClientRects()` 行数 = 1；「全选 (N 条)」单行；
2. **解释框消失**：draft 卷卡内无灰底四性检测提示框；卷内条目可视高度 256px；
3. **字阶护栏**：`npm run check:typography` 保持 0 违禁（未引入任何非闭集字号）；
4. **回归冒烟**：`py -3 smoke-grouping-transfer.py`（组卷/移交链路）全绿——确认删除提示框不影响交互逻辑（纯展示层）；
5. 截图前后对比留档。

## 五、风险与回滚

- 纯前端单文件（VolumeWorkspacePage.tsx）+ index.css 一个工具类；无后端、无数据、无接口变更；
- 删除的解释文案语义全部有落点（title 悬浮 / 移交环节强制行为本身）；
- 回滚：git revert 单提交即可。

## 六、范围界定

本次**仅改组卷工作台页**（截图所指）。全站同类说明性静态框扫描结果如下，**本次不动**，如需同口径精简请确认后另开批次：

- `QuickCheckPage.tsx:418` 顶部横幅「四性检测结果中心 · 按规定检测在移交…时自动执行…」；
- `ApiReceivePage.tsx:50` 方式选项描述中的四性检测说明；
- `OpenApiReceivePage.tsx:492` 字段表说明列。

## 七、执行清单（2026-09-06 已执行完毕）

- [x] 1. index.css 增加 `.no-scrollbar` 工具类
- [x] 2. 池工具栏：label nowrap + 容器 flex-nowrap/overflow-x-auto/gap-1 + 按钮 px-2.5 + 删分隔条
- [x] 3. 卡片操作行：同款 flex-nowrap 改造（+实测发现的必要补充：卡片 8 个按钮补 `whitespace-nowrap shrink-0`——缺 shrink-0 时被 flex 压缩导致按钮内部文字折行 44/62px，此为实测新发现根因）
- [x] 4. 删除四性检测提示框（836–843），语义并入确认组卷按钮 title
- [x] 5. 已确认卷状态行删括号解释（926–930）
- [x] 6. 卷内条目 `max-h-48` → `max-h-64`
- [x] 7. 验收全过：tsc 0 错；check-typography 0 违禁；smoke-grouping-transfer **10/10**；浏览器 DOM 实测（池工具栏 7 按钮单行：容器高 32=按钮高、topSpread=0；卡片操作行单行：topSpread=1px、按钮 28/30px 无内部折行；提示框消失；全选 label 16px 单行）+ 截图留档
- [x] 8. 更新《系统缺陷审计与修复计划-2026-08-29.md》执行日志 + git commit

### 执行中的偏差说明（如实记录）

- 执行清单 3 原本只计划容器级改造；浏览器实测发现卡片按钮无 `shrink-0` 会被 flex 压缩到自然宽度以下、按钮**内部**文字折行（确认组卷 44px / 元数据录入 62px 高），据此为卡片 8 个按钮（确认组卷/目录预览/元数据录入/合并/拆卷/删除空卷/移交至档案保管/撤销确认）补齐 `whitespace-nowrap shrink-0`。池工具栏按钮此前已有该属性故未复现。
- 极窄视口（≤1280px）下卡片操作行按钮总宽超出右栏时按设计横向滚动兜底（隐形滚动条），不折行；常规 1440+ 屏幕完整单行显示。
